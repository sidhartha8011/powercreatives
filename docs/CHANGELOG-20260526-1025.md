# CHANGELOG 2026-05-26 10:25 — Client Review Board: Typography & Color Hierarchy Refinements

Polished and unified the comments drawer headers, asset info scales, empty state templates, and ad copy card body fonts to pixel-perfect premium standards.

Plugin: v1.4.8

## UX Refinement & Enhancements

- **Comments Drawer Header Hierarchy:**
  - Redesigned inspector titles (`.pcm-inspector-title span`) as refined micro-eyebrows (`11px` bold uppercase uppercase with `0.08em` spacing) in secondary dark slate color `var(--ink-3)`.
  - Upgraded inspector count badges (`.pcm-inspector-count`) with sleek low-contrast background `rgba(0, 0, 0, 0.05)` and `font-size: 10.5px`.
  - Elevated the main asset name under inspection (`.pcm-inspector-asset-name`) to `14.5px` semi-bold with `-0.01em` letter-spacing, forming a perfect visual anchor.

- **Comment Card Elements:**
  - Elevated author names (`.pcm-comment-author`) to `13px` semi-bold for clean readability and balanced size scaling.
  - Adjusted comment text (`.pcm-comment-text`) line-height to `1.55` to optimize reading spacing on all viewport widths.
  - Refined timestamps (`.pcm-comment-time`) to `11px` weight `500` under `--ink-4` for secondary metadata harmony.

- **Empty State Typography:**
  - Upgraded empty title (`.pcm-inspector-empty-title`) to `14px` semi-bold in `--ink-2` for a warmer, welcoming display.
  - Adjusted description (`.pcm-inspector-empty-text`) to `12.5px` with a premium `1.5` line-height.

- **Card Copy Typography:**
  - Elevated ad copy text (`.pcm-copy-text` and editor paragraphs `.ProseMirror p`) from `12.5px` to a highly readable `13.5px` with line-height `1.55`, making ad copy layouts read like high-end editorial briefs.

## Files changed
1. `app/src/modules/Approvals/client-review.css` — Refined comments headers, empty states, comment text spacing, and card copies.

## Verification
- `npm run build`: Successfully built client environment in 25.39s.
