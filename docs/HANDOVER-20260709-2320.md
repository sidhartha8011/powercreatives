# HANDOVER — 2026-07-09 23:20 — THE CLEANUP (C1–C5) IS CODE-COMPLETE

**Read with:** `docs/CLEANUP-GAP-ANALYSIS-20260709.md` (the assignment),
`docs/DYNAMIC-OPTIMIZATION-ARCHITECTURE.md` → **"Cleanup contracts v3"** (the
frozen law for everything below), `docs/CHANGELOG-20260709-2200.md` (per-step
detail), SESSION_LOG bottom entry.

## 1. What exists now (all LOCAL commits, never pushed — owner law)

| Commit pair | Step |
|---|---|
| 3e25bfc | C1 contracts v3 frozen (snapshot v1 · instruction stream v2.1 · config schema v1 · handle-resolution law · deletions ledger) |
| ff714ad→2e36774 | C2 hub parser: `parse_page_snapshot()` + ONE `GET …/inventory` read; HeadingsPanel single query |
| 01886b0→eb0d70e | C3 connector **3.0.0** + hub v3 branches + matcher serving-mirror deleted + **committed harness** |
| e219865→aca3b29 | C4 `migrate_site_overrides()` (verify-then-clear, idempotent) + Migrate button |
| 0903657→5457e23 | C5 hub config store + auto/on-demand push |
| 9b00e5c | session log |

**The five debts + the sixth:** D1 mirror → connector apply is the ONLY copy,
primitives machine-pinned by `tests/standalone/run.php` (37/37, runs the REAL
extracted template with bare PHP — `php tests/standalone/run.php`). D2 layers →
one serving pass (heading→section→insert→paragraph), overrides become
site-scope heading rules. D3 scanners → deleted; one snapshot, hub parses.
D4 tunables → hub-pushed config, code keeps defaults. D5 drift → v3 capability
+ the gated follow-up. D6 (found in pre-read): fossil v1.0.1 tenant template
deleted — the per-tenant ZIP now ships the real connector with baked HMAC.

## 2. Verification state

**Automated, all green on this box:** harness 37/37 (primitive parity hub↔
connector, full apply corpus, heading pass, ordering law) · php -l every
touched file + the extracted generated template · tsc 59 = baseline
throughout · build OK ×3. PHPUnit corpus trimmed to hub primitives (apply
fixtures moved to the harness); vendor/ intentionally absent here.

**Owner verification pending (in order):**
1. Sites → Connector: update powerleads to **3.0.0** → Ctrl+F5.
2. Outline opens (now ONE inventory call), sections group, editor works,
   section edit → live page serves (regression of the 2.8.3 flow).
3. Heading edits: content heading → source write; theme/menu heading → becomes
   a heading INSTRUCTION (site-scope rule, serves render-time).
4. SEO site settings → **Migrate legacy heading overrides** → expect
   "Migrated N — legacy list cleared"; page output unchanged.
5. Powerstock: manual reinstall ritual, same checks.

## 3. The gated follow-up (do NOT run until the WHOLE fleet reads 3.0.0)

One commit, pre-scoped: drop hub pre-3.0 fallbacks (`remote_get_headings`
scan/raw fallbacks, `remote_get_content_nodes` scan path, schema v1/v2 push
shapes, `remote_apply_heading_override`) + connector override buffer +
`/override-heading` route. Listed in contracts v3 → deletions ledger #4.

## 4. Incidents & lessons (this session)

- **Encoding regression (resolved, verified):** PowerShell `Get-Content`/
  `Set-Content` splices double-encoded UTF-8 in two PHP files. Recovered
  byte-faithfully via a cp1252 reverse-map script; audited every diff hunk.
  **LAW for this box: never splice UTF-8 sources with PowerShell — use the
  Edit tool or iconv.**
- This CLI (LocalWP php 8.2.29) lacks mbstring — normalize falls back to
  ASCII folding on BOTH sides (spec'd safe); harness fixtures account for it.

## 5. Also open (unchanged from the morning handover)

Draft→Publish revival (spec on file) · outline de-clutter proposal · stale
test data on powerleads (owner clears via UI — note the Hello! override, if
still present, is exactly what the Migrate button converts).
