# Changelog — 2026-07-07 09:52 — Deliveries table: card-parity columns + softer chrome

## Summary
Per user feedback on the table view: Client column removed (matches the card, where
Client was already dropped), Modules + ID columns added, column order now mirrors the
delivery card, and the wrapper chrome is flatter (no shadow) and rounder.

## Changes (all in `Deliveries/table/DeliveriesTable.tsx`)
- Columns now **Delivery | Status | Type | Brand | Modules | ID | Updated** — the
  card's property order (Status · Type · Brand · Modules · ID) with name first and
  the derived Updated last.
- **Client column REMOVED** (the `clientName` field, the Client bar filter, and
  search-by-client all remain — only the column went).
- **Modules column** — new `InlineModulesCell`: the card's multiToggle control
  (2 chips + "+N" overflow, checkbox dropdown, "Ads includes Copy + Image" label)
  with the table's optimistic-apply / revert-on-failure save contract. Sortable by
  grant count.
- **ID column** — `externalId`, inline text editable (empty → null), sortable.
- **Chrome** — `wrapperClassName="shadow-none rounded-lg"` on the Deliveries
  instance only: hairline border does the separation, 8px corners. The global
  DataTable default (shadow-sm / rounded-md) is untouched — Sites/SEO unchanged.
- Project sub-rows realigned to the 7-column grid (spacer cell before ✕; loading and
  add-project rows span 7).

## Verification
- tsc: 56 errors, baseline identical, 0 in touched files. vite build OK.
- Pending live (user): column order + chrome look, Modules chips + toggle save,
  ID edit, sub-row alignment under the wider grid.

## Rollback
`git reset --hard 85177bb` (BEFORE IMPLEMENTATION OF [deliveries-table-columns-chrome]).
