# CHANGELOG 2026-05-26 09:40 — Client Review Board: Premium Comments Integration

Eliminated the floating comments count banner above the card actions row, and integrated all comment count metrics, state indicators, and glowing notification dots directly into the "Comment" button itself to maximize clean UX alignment.

Plugin: v1.4.4 → v1.4.5

## UX Refinement & Enhancements
- Removed the separate floating banner block `.pcm-comment-badge` completely.
- Evolved the `Comment` action button to display an inline circular number badge when comments are present.
- If there are new comments (any comment in the thread has status `'New'`), we display:
  - A tiny, pulserande brand-blue (`#2563eb`) notification dot absolute-positioned over the Lucide `MessageSquare` icon.
  - A glowing, brand-blue bubble badge with white text (`background: #2563eb; color: #fff; box-shadow: 0 0 8px rgba(37,99,235,0.35)`) inside the button instead of the neutral slate grey bubble.

## Files changed
1. `app/src/modules/Approvals/components/ClientReviewPage.tsx` — Computed `hasNewComment` and passed it as a prop.
2. `app/src/modules/Approvals/components/CreativeAssetCard.tsx` — Removed `.pcm-comment-badge` block, added prop and overhauled the Comment action button structure.
3. `app/src/modules/Approvals/client-review.css` — Replaced `.pcm-comment-badge` with `.pcm-btn-comment-icon-wrapper`, `.pcm-comment-new-dot`, `.pcm-comment-count-badge` + `.is-new`, and `.pulse-blue` + `@keyframes` styles.
4. `power-creatives.php` — Version bumped from 1.4.4 to 1.4.5.

## Verification
- `npm run build`: Success in 22.68s.
