# CHANGELOG 2026-05-26 09:45 — Client Review Board: Premium Download Button Overhaul

Overhauled the "Download" button on media assets cards to match the copy button: icon-only, transparent background, and circular layout, creating an extremely cohesive visual style.

Plugin: v1.4.5 → v1.4.6

## Enhancements
- Converted `pcm-btn-download` on media cards to be icon-only (retaining Lucide `<Download>` icon and removing the text).
- Added `title="Download"` and `aria-label="Download"` directly to the button for screen reader support and native tooltips.
- Grouped `.pcm-btn-download` and `.pcm-btn-copy` CSS rules in `client-review.css` so both render as identical transparent `30px` circular buttons (`width: 30px; height: 30px; padding: 0; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center;`).

## Files changed
1. `app/src/modules/Approvals/components/CreativeAssetCard.tsx` — JSX modified to make the download button icon-only and add accessibility attributes.
2. `app/src/modules/Approvals/client-review.css` — Grouped and simplified download and copy button styling.
3. `power-creatives.php` — Version bumped from 1.4.5 to 1.4.6.

## Verification
- `npm run build`: Success in 28.60s.
