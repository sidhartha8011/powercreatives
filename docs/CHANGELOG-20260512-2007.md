# CHANGELOG — 2026-05-12 20:07

- **2026-05-12 20:07** — Removed z-index patchwork from `.pcm-fs-wrap` in `class-pcm-shortcode.php`. Removed z-index:99999 from wrapper, removed all data-slot/sonner CSS patches, lowered logout z-index from 100001→51. Root fix: wrapper now has no z-index, relying on `.pcm-fs-hide` to hide theme content so Radix portals render naturally above.
- **2026-05-12 20:45** — Replaced hardcoded "User / Free Plan" in Sidebar.tsx with dynamic user identity from `pcmConfig.user`. Shows real name, Gravatar avatar, role label (Administrator/Workspace User). Added simple "Sign Out" button with context-aware logout (admin→wp-admin, gate→cookie clear+reload). Same PowerKeys pill-style design.

