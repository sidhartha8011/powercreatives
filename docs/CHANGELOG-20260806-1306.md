# Changelog — 2026-08-06 13:06

- Added one Approval-owned portal and scroll-boundary resolver so the selection and slash menus share the same clipping-safe positioning contract.
- Kept the shared Writer toolbar behavior unchanged while giving Approvals an explicit text-only selection toolbar; image insertion now has a dedicated slash-command path.
- Added a keyboard-accessible Approval slash menu for text, headings, lists, checklist, quote, divider, images, image annotation, and whole-card drawing while reusing the existing editor and media workflows.
- Removed the permanent Approval card toolbar and connected its image, annotation, drawing, and checklist capabilities to contextual selection or slash interactions without changing persistence paths.
- Styled the slash palette as a bounded, scrollable Notion-like block menu with explicit labels, descriptions, keyboard selection state, and viewport-safe sizing.
- Aligned the Approval document to the supplied reference hierarchy: responsive 40px title, 13px metadata, 14px properties, 15/24px body text, restrained headings, 14px checkboxes, unified ink levels, and whitespace instead of a form-divider above prose.
- Updated the Approval editor’s inline architecture notes to describe the new contextual-only controls and whitespace-based document hierarchy accurately.
- Hardened keyboard and narrow-layout behavior: command selection follows its scroll position, IME input is not intercepted, menu buttons inherit document type, and property values can shrink without overflowing the card.
- Added the required functional, architecture, UX, regression, and shortcuts-audit checklists for objective static and browser validation.
- Recorded successful TypeScript, production-build, diff, live-browser, autosave round-trip, and clean-console checks without representing untested media mutations as passed.
- Replaced the slash menu's document-level keyboard listener with a precedence-safe ProseMirror plugin after live testing showed Enter could fall through to normal paragraph behavior.
