# Changelog 2026-05-13

## Angle 1 Raw Prompt Passthrough

| Time | Change |
|------|--------|
| 12:38 | `useImageGeneration.ts` — Angle 1 (Original) now always uses user's exact prompt. Only angles 2+ get AI-generated concepts. When numVersions=1, concept generation is skipped entirely. |
| 12:38 | `useImageGeneration.ts` — Prompt construction differentiates anchor vs AI angles to prevent prompt duplication. |

## Kie Timeout & Quality Fix

| Time | Change |
|------|--------|
| 15:17 | `class-pcm-kie-marketplace.php` — Image polling timeout bumped to 800s. Diagnostic logging added to wait_for_task(). |
| 15:51 | `kieMarketplaceModels.ts` + `.json` — GPT Image quality changed from `medium` to `high` (4 models). |
| 15:52 | `controller.php` — `set_time_limit(900)` added to `generate_single()` based on verified `php_max_execution_time=1200`. |

## Concept Suggestions User Prompt in Settings

| Time | Change |
|------|--------|
| 17:09 | `service.php` — Added `concept_suggestions_user` default template to `get_default_prompts()`. |
| 17:09 | `service.php` — Refactored `suggest_concepts()` to accept `$user_template` param and resolve via `resolve_prompt_placeholders()`. |
| 17:10 | `controller.php` — Added `concept_suggestions_user` prompt override lookup. |
| 17:10 | `prompts/controller.php` — Registered new "Angles (Scenes) — User" section in Settings. Renamed existing to "Angles (Scenes) — System". |
| 17:10 | `service.php` — Removed dead `build_concept_user_content()` function. |

## AI Enhance Toggle UX

| Time | Change |
|------|--------|
| 08:42 | `imageConfig.ts` — `autoOptimizeBrief` default changed from `true` to `false` (opt-in). |
| 08:42 | `ImageSidebar.tsx` — Replaced cryptic Checkbox + Info tooltip with Sparkles icon + "AI Enhance" label + Switch toggle. Icon glows when active. |
| 08:42 | `ImageSidebar.tsx` — Removed unused `Checkbox` import. |
| 08:42 | `ImageSidebar.tsx` — "Variations per Engine" renamed to "Images per Engine". |

## Image Bulk Action Bar

| Time | Change |
|------|--------|
| 09:13 | `useImageGeneration.ts` — Added multi-select state: `selectedAssetIds`, `toggleAssetSelection()`, `clearAssetSelection()`, `selectAllForModel()`, `bulkDownload()`, `bulkRemove()`. |
| 09:14 | `ImageResultsGrid.tsx` — Added Checkbox overlay on ImageTile (top-left, visible on hover + always when selected). Processing/failed tiles excluded. |
| 09:14 | `ImageResultsGrid.tsx` — Added Select All/Deselect toggle (CheckSquare/Square icon) per model header section. |
| 09:15 | `index.tsx` — Wired shared `BulkActionBar` with Download + Remove actions. Portal-rendered, slides in from bottom. |

## Tab Select All + Save to Project

| Time | Change |
|------|--------|
| 09:26 | `useImageGeneration.ts` — Added `selectAllForTab()` callback: selects/deselects all completed assets across all models for the active version tab. |
| 09:26 | `ImageResultsGrid.tsx` — Added tab-level Select All checkbox (CheckSquare/Square) inline before tab buttons. Computes `isAllTabSelected` from `allAssets` prop. |
| 09:27 | `index.tsx` — Wired `onSelectAllForTab` + `allAssets` props to `ImageResultsGrid`. |
| 09:28 | `index.tsx` — Added `BulkSaveToProject` Popover component: project picker + create new, sequential save via `assets.saveToProject` tRPC endpoint. |
| 09:28 | `index.tsx` — Added "Save to Project" action in BulkActionBar between Download and Remove. |
