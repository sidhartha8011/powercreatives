# Approval editor media boundary — 2026-08-06 22:30

- Centralized recognition of WordPress media and approval annotator portals in `approvalEditorOverlayBoundary.ts`.
- Prevented the admin approval preview, custom-set dialog, and share popover from treating editor-overlay interaction as outside dismissal.
- Kept Escape scoped to the active WordPress media/annotation overlay instead of closing the underlying approval document and preview.
- Distinguished rendered overlays from WordPress's hidden cached media frames so normal Escape dismissal still works after media use.
- Removed the over-broad `.wp-core-ui` target match, which could classify the whole WordPress admin page as a media interaction.
- Removed a duplicated editor-architecture comment while keeping `CardDocumentView.tsx` below the project's 400-line modularity threshold.
