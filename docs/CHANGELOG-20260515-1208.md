# CHANGELOG — 2026-05-15 12:08

## Final Prompt Template
- Added `final_prompt` template to `PCM_Image_Service::get_default_prompts()` — controls the actual prompt sent to image models (DALL-E, Flux, Kling)
- Registered `final_prompt` in prompts controller: sections registry, section metadata, default sections
- Updated `generate_single()` in image controller to resolve the `final_prompt` template when `brandContext` is provided
- Added `build_final_prompt_context()` and `resolve_final_prompt()` public methods to `PCM_Image_Service`
- Refactored `useImageGeneration.ts`: extracted duplicated `fullBrandContext` into single `brandCtx` (DRY fix)
- Frontend now passes `brandContext` to every `generateImageMutation` call
- Fixed stale comment in `format_brand_colors()` referencing deleted `buildColorContext()`
