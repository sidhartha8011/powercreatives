# CHANGELOG 2026-07-07 02:29 — Sites: Aligned header and filter bar vertical margins

Aligned the vertical margins surrounding the Sites page filter bar to match the Projects page layout standard (mb-6).

## UI Changes / UX Refinements

- **Symmetrical Margins:**
  - Updated the Sites page header container to use `mb-6` (was `mb-1`), ensuring the spacing between the header and the filter bar box is exactly the same as the spacing between the filter bar box and the table (`mb-6`).

## Files changed
1. `app/src/modules/Sites/index.tsx` — Aligned margins on header and filter bar container.

## Verification
- `npm run build`: Successfully built client environment in 28.12s.
