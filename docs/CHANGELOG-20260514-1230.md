# Changelog — 2026-05-14

## Brand Assets Fix (Bug 1 + Bug 2 + Provider Bug)

- **Fixed:** `setLogoConfig`/`setSubjectConfig` via "From Brand" picker now correctly sets base64 (removed dead `isActive` gating in `useImageAssets.ts`)
- **Fixed:** Added X-clear buttons on Logo and Subject thumbnails in sidebar (matches Trust Badge pattern)
- **Fixed:** Removed dead `logoBase64`/`subjectBase64` from `pipelineExtra` in `useImageGeneration.ts` (no backend provider ever handled these)
- **Fixed:** `provider` field in generation mutation now uses `resolvedProvider` instead of `model?.provider ?? ''` which could send empty string and trigger PHP 400 error
- **Changed:** Brand Colors injection via `buildColorContext()` confirmed working (already appends to prompt on frontend)

### Files Modified
- `app/src/modules/Image/hooks/useImageAssets.ts` — clearLogo(), clearSubject(), simplified payload gating
- `app/src/modules/Image/hooks/useImageGeneration.ts` — removed dead base64 pipeline, fixed provider bug
- `app/src/modules/Image/components/ImageSidebar.tsx` — X-clear buttons for logo/subject

## Sidebar UX Reorder

- **Changed:** Brand Assets (renamed from Asset Pipeline) moved to position 2, directly after brand dropdown
- **Changed:** Brand Colors moved inside Brand Assets collapsible section
- **Changed:** Reference Images hidden from UI (code preserved with architectural comment)
- **Changed:** Theme accordion defaults to collapsed
- **Changed:** ContextPanel `defaultExpanded={false}` in `ContextPanel/index.tsx`

### Files Modified
- `app/src/modules/Image/components/ImageSidebar.tsx` — sidebar reorder + collapsible changes
- `app/src/components/shared/ContextPanel/index.tsx` — theme collapsed by default
