# CHANGELOG — 2026-07-09 22:00 — THE CLEANUP (gap analysis C1–C5, owner GO)

One line per change, newest last. Companion: `docs/CLEANUP-GAP-ANALYSIS-20260709.md`,
architecture doc → "Cleanup contracts v3".

- C1 DOCS: froze cleanup contracts v3 in DYNAMIC-OPTIMIZATION-ARCHITECTURE.md — snapshot endpoint v1 (page-as-rules-input, tiers kept, connector never parses), instruction stream v2.1 (heading target = compiled override, site scope, heading-pass-first order, schemaVersion 3), config schema v1 (hub-pushed tunables + overrides read/clear — the C4 data path), handle-resolution law (builder walkers stay as writer intelligence), deletions ledger (scanners, hub apply mirror, fossil v1.0.1 template, post-migration overrides layer).
- C2 HUB: PCM_SEO_Service gains remote_fetch_snapshot() (v3 capability-gated), heading_instructions() (display-state source), parse_page_snapshot() (ONE PCM_Text_Matcher pass -> heading rows incl. chrome + chrome-stripped paragraph nodes with scan anchors, legacy shapes preserved), remote_get_inventory() (snapshot on v3, honest legacy-scan composition on 2.x fleet); GET /seo/sites/{id}/content/{post}/inventory; trpc seo.remoteGetInventory; HeadingsPanel: serialized headings->content-nodes chain replaced by ONE inventory query. VERIFIED: php -l x2, tsc 59 = baseline (0 in touched files), build OK.
- C3 CONNECTOR 3.0.0: snapshot endpoint v1 (tiers verbatim from scan-content, returns raw {html,tier}, ?pcm_snap serving-excluded, TTL config-driven) - config endpoint GET/POST (store + effective values + legacy overrides EXPOSED for C4 + clearOverrides) - pcm_conn_cfg() defaults for every former literal (snapshot TTL 600, loopback 8/15, chrome regions, stats throttle 300, overrides cap 200) - serving: heading pass (compiled-override semantics: normalized match, ALL occurrences, attrs kept, esc_html) runs FIRST, site-scope rules store pcm_conn_rules_site served on EVERY front-end render, post rules singular - rules API schemaVersion 3, postId 0 = site scope (heading only) - /replace-heading identity-only writer-side resolution (builder widgets -> inline markup -> shared templates; replaced:0 honest) - BOTH scanners + pcm_conn_parse_paragraph_nodes DELETED - tenant HMAC handshake folded into the ONE template (client-id-guarded), fossil v1.0.1 connector_php template DELETED (tenant ZIP now ships the real connector).
- C3 HUB: remote_get_headings/remote_get_content_nodes gain v3 snapshot branches; remote_update_heading_apply: snapshot rows = writer-first identity-only edit, fallback + override-row edits become SITE-SCOPE HEADING RULES via new save_heading_rule() (UPSERT like the legacy endpoint incl. re-edit-by-current-value, exact-original revert deletes, push-fail rollback); push_rules/rules_to_schema carry heading target + scope + v3 gating; connector_rules_schema_version memoized per request.
- C3 MATCHER: replace_block/apply_section_rule/apply_section_insert/locate_section/section_body_indices DELETED from hub PCM_Text_Matcher (zero hub callers, verified) - parsing/identity primitives stay.
- C3 HARNESS: tests/standalone/run.php COMMITTED (bare php, no vendor): evals the REAL extracted template; 37 fixtures = primitive parity hub-vs-connector + full apply corpus + heading pass + ORDERING law. TextMatcherTest.php trimmed to hub primitives (apply corpus moved to harness), + content_blocks chrome test.
- C3 INCIDENT (resolved): PowerShell file splices double-encoded UTF-8 in matcher + seohub service; recovered byte-faithfully via cp1252 reverse-map script; verified: no mojibake greps, diff hunks = intended edits only, all lints green.
- C3 VERIFIED: php -l x5 + extracted generated template lint, harness 37/37 ALL GREEN, tsc 59 = baseline (0 in touched files).
- C4 MIGRATION: PCM_SEO_Service::migrate_site_overrides() - reads the legacy list via the 3.0.0 config GET, upserts site-scope heading rules (originalText kept for exact revert), ONE push with rollback, VERIFIES the connector's stored siteRules round-trip, only then POSTs clearOverrides (legacy buffer goes inert via empty-list early-return; while both exist output is identical because instructions run after overrides and honestly miss). Idempotent. Endpoint POST /seo/sites/{id}/migrate-overrides + trpc seo.remoteMigrateOverrides + Migrate section in RemoteSiteSettingsPanel. VERIFIED: php -l x2, tsc 59 = baseline.
- C5 CONFIG PUSH: PCM_SEO_Service::get_connector_config() (ONE hub option pcm_seo_connector_config seeded with the connector's code defaults, filterable) + push_connector_config() (v3+ only, pushed:false honestly on older); auto-pushed after every successful remote_site_save (existing /site channel pattern) + on-demand POST /seo/sites/{id}/push-config + trpc seo.remotePushConfig. D4 dead: every tunable is hub-controlled data, code keeps defaults. Fleet convergence + dropping pre-3.0 fallbacks = the documented OWNER-GATED follow-up (fleet must read 3.0.0 first). VERIFIED: php -l, harness 37/37, extracted template lint, tsc 59 = baseline, build OK.
- P1 ALIGNMENT [scope+flip]: contracts v2.2 frozen (one mechanism, scope-decision law, occurrence law, inventory law) - inventory: chrome/content scope tag, content twins NEVER deduped (each row individually editable, own occurrence), dual identity spaces (original for rule matching, display for section keys), honest per-scope labels - save_heading_rule identity-based (postId 0/N + occurrence + matchLevel; current-value re-edit; exact-original revert; rollback) - routing flip: writer-first source-write path DELETED on v3, every heading edit is a dynamic rule - connector 3.0.1: occurrence-aware heading pass (content-blocks identity; allOccurrences keeps override semantics), /replace-heading route + 6 heading walkers DELETED (~360 lines) - C4 migration writes allOccurrences:true - harness: 39/39 (occurrence twin targeting, chrome-twin isolation). VERIFIED: php -l x3 + extracted template lint, harness 39/39, tsc 59 baseline, build OK.
- P2 ALIGNMENT [one-owner]: update_section_owned_heading() - a heading edit on a section-owned heading rewrites the section rule's FIRST replacement unit (unit attrs kept, identity untouched - it keys on the rules-input), versions + rollback ride the section mechanics; save_section_rule ABSORBS the post-scope heading rule feeding it (its original text/occurrence/level become the section's match key, rule deleted) - chains impossible to create, existing test chains resolve on next section save. Hub-DB logic (no serving change): verified by php -l + review; harness stays 39/39. tsc 59 baseline.
- P3 ALIGNMENT [served-truth editor]: connector 3.0.1 snapshot mode=served (tier-1 loopback serves rules naturally; tier-2/3 apply stored rules to fallback renders; version-stamped cache pcm_conn_view_ver bumped on every rules push + save_post; explicit view marker so a 3.0.0 connector can never pass off the input view) - hub served_inventory(): parse served view once, ATTRIBUTE every row to its producing rule (section/insert unit-section split incl. SLICES, heading rules by output, paragraph rules -> optimized flag), input-view fallback stays honest - remote_get_headings/content_nodes/inventory all serve the same served-truth rows - save_section_slice() (kind:slice: splices one unit range of the owning rule; empty slice deletes inserts, rejected for sections; versions + rollback ride) - update_owned_heading_unit() (attribution-routed heading edits swap ONE unit) - frontend: rows from served view, rule-born sections open their slice in the modal (kind:slice save), added sections are REAL rows (synthetic NEW rows off in served view), previews from served paragraphs only (concatenation bug dead), optimized dots from attribution. VERIFIED: php -l x3 + extracted template lint, harness 39/39, tsc 59 baseline (0 in touched), build OK.
- P3 HOTFIX (live-found via hub probe): parse_page_snapshot's paragraph-anchor walk still called the instruction closure with 2 args (fatal 500 -> empty outline); fixed + occurrence space reset for the second walk. Site-scope heading rules now imply allOccurrences BY LAW in rules_to_schema (pre-P1 rules 7/8 had no flag and stopped serving); site rules re-pushed. LIVE-VERIFIED on powerleads Sample Page: h1 Welcome->section#6, Tandl�kare i G�teborg = own slice row of section#6, site h2 -> heading#8 serving again.
- HOTFIX 2 (live-found, live-proven): re-key after heading edits now fires ONLY on source writes (2.x fleet) - a rule-mediated edit changes display only, and re-keying corrupted the rule's own match identity (rule 9 re-keyed to its own output -> permanently stale -> owner's 'edits not working'). Corrupted rule repaired + re-pushed. LIVE-VERIFIED: hello-world serves the H1 and paragraph edits.
- DATA REPAIR (live incident, timestamps proven): owner's title-cell edit ('Sample Page'->'Sample', slug -> /sample/) 16s after his heading edit broke the LEGACY pre-P2 chain (site rule 7 feeding section rule 6) - every dependent edit went honestly stale ('header disappeared'). Repair: junk rule 7 deleted, rule 6 re-keyed to the current title, re-pushed. LIVE-VERIFIED /sample/ serves h1 Welcome + Tandl�kare section. P2 prevents new chains; this was yesterday's leftover test chain.
- UX [status-dots]: the truncating amber sourceLabel chip on heading rows replaced by ONE status dot per row, same family as the paragraph dots - sky = site-wide element (full warning in tooltip), primary = optimized on this page (any rule attribution). LayoutTemplate import dropped. VERIFIED: tsc 59 baseline, build OK.
- FRAME-ONLY TIGHTENING (owner order): site/frame heading edits now carry frameOnly and serving applies them ONLY inside chrome spans - identical text in any page's CONTENT is never touched. Migrated legacy overrides keep whole-page semantics (no flag). Harness 41/41 (frame-only isolation + legacy reach fixtures).
- UX [shared-pills]: ONE house pill extracted to seo-table.tsx (Pill + PILL_CLASS - the table's Airtable-style rounded-full soft chip); reused by the table status cell AND all four outline chips (H1-H6 dropdown trigger, read-only H pill, P chip, NEW chip); H1-H6 colors moved to the soft-100/700 palette; duplicated square-chip styling deleted. VERIFIED: tsc 59 baseline, build OK.
- UX [global-pill]: Pill promoted to components/ui/pill.tsx (cva variants, house pattern): EVERY pill color/size lives in ONE variant map (publish/pending/private/future/draft, h1-h6, p, new); pillVariants exported for pill-shaped triggers; consumers render <Pill variant=.../> with zero inline styling; SEO-module Pill/PILL_CLASS, statusBadgeClass switch and TAG_STYLE map DELETED. VERIFIED: tsc 59 baseline, build OK.
- UX [pill-as-trigger]: override warfare deleted - SelectTrigger gains asChild (composition escape hatch: lends behavior, renders NO chrome); the H-level dropdown IS a Pill (variant only, chevron sized by the Pill's own icon law: 10px muted, defined once in pillVariants). No !important, no border-0/shadow-none soup, immune to future base-style changes. VERIFIED: tsc 59 baseline, build OK.
- V1 FULL-PAGE EDITOR [fullpage-editor-v1] (G1+G2+G3+G6, NO connector change): hub inventory gains contentHtml - a CLEAN block assembly of the served content region (original sections = bare h/p with inner HTML; rule-owned sections = their sliceHtml VERBATIM so rule lists survive the round-trip; EVERY img - gaps, block inners, slices - extracted as a locked data-pcm-locked block, chrome excluded) - save_page_edits() slices the edited doc with the SAME parser (split_unit_sections + section_runs refactored OUT of attribute_inventory, zero duplication), aligns 1:1-by-order/LCS-on-keys, unchanged-skip law = heading+level+paragraph fingerprint+raw visible text, and routes per pair through the EXISTING paths: whole-section-rule pairs via save_section_rule keyed on the RULE's OWN stored identity (clean revert deletes it - the slice path never could), partial slices via save_section_slice, originals via save_section_rule on the row identity (absorb law rewires heading-ruled rows), extras via save_section_insert anchored to the preceding served heading, FEWER = honest error before any write. F9 DEFUSED at assembly+save: imgs never persist (stripped every path, page images stay untouched between-content), original lists never enter the doc - remaining raw units are rule-own/user-created = engine-legal, KEPT (deliberate narrowing of the checklist's strip-all: assembly makes broad stripping both unnecessary and lossy for rule slices). Endpoint POST /seo/sites/{id}/content/{post}/page-edits + trpc seo.remoteSavePageEdits. Frontend: SectionModal mode:page (90vw/85vh centered, self-fetches contentHtml, StarterKit h1-h6 + LockedImage draggable:false, versions/AskAI/Re-write hidden, honest connector-3.0.1 message, section/insert byte-identical); SquarePen on connected rows opens page mode, WP-editor link demoted into the editor header, local rows keep the link. VERIFIED: harness 41/41 (engine untouched), php -l x2, tsc 59 baseline, build OK + LIVE on powerleads: no-op saves on 3 posts = ZERO writes; post 3 edit 2 paragraphs + 1 heading = EXACTLY 3 section rules, served through the connector apply, locked img in doc landed in NO rule; edit-back = all 3 rules DELETED (clean revert, state byte-identical); post 2 published: slice edit served on /sample/ publicly, revert restored rule 6 byte-identical (len 766); extra section = sectionInsert served then removed clean; section removal = honest error, zero writes; every pre-test owner rule untouched.
- UX [ux-pagemode-polish] (V2+V3 arc pair 1, owner GO): page editor 90vw -> 58vw (min 720px); page mode gets its OWN reading scale PAGE_TYPE_SCALE (text-sm, p my-3, stepped h1-h6 sizes with real top margins, first-child mt-0, roomier px-8 py-4) - the doc reads like the live page; section/insert mode byte-identical (compact TYPE_SCALE untouched). VERIFIED: tsc 59 baseline, build OK.
- PAGE VERSIONS [page-versions] (arc pair 2): record_section_version + list generalized to record_version/list_versions(target,...) - all 3 section call sites updated, ZERO schema change (target column existed); save_page_edits records ONE target=page row per changing save (duplicate-skip + cap 20 ride the shared laws); list_page_versions returns saved documents + originalHtml = the rules-INPUT snapshot through assemble_content_html (no rules -> no attribution -> true original; '' honest when input unavailable); GET page-versions endpoint + trpc seo.remotePageVersions; delete rides the existing target-agnostic endpoint. Frontend: ONE dropdown serves both modes - page mode rows Current (live served doc) / Original (hidden when unavailable) / dated saves with delete; picking loads the editor, Acceptera goes through the normal per-section save; label always names a state (Current default in page mode); after a page save both current doc + versions refetch. VERIFIED: php -l x3, harness 41/41, tsc 59, build OK + LIVE on powerleads post 3: record on change (v30), restore Original = clean reverts INCLUDING unwinding a paragraph rule's section honestly (v32), deletes, state restored to the owner's exact rule set (junk rule 21 clean-reverted, rule 2 dash serving verified). NOTE: owner tested V1 in production between turns - his page-mode edits on post 1 created rule 19 which ABSORBED heading rule 11 (one-owner law proven in real use).
- AI INLINE DIFF [ai-inline-diff] (arc pair 3, V2 the headline): in-house word-LCS diff (word-diff.ts ~140 lines incl. helpers, ZERO deps) - wordDiff (tokenize words+whitespace, LCS, merged runs), diffBlocksHtml (blocks paired 1:1 by order: word-diff on visible text; extra AI blocks all-green, dropped blocks all-red; untouched blocks keep real formatting), stripDiffHtml (THE GUARD: removed runs die, added runs unwrap - runs on EVERY page save), splitDocSections (same grouping law as the server's split_unit_sections; locked imgs lifted per section, never sent to AI, ride along untouched). Marks diffAdded/diffRemoved (page mode only, presentation only). AI Optimize + Ask AI in the page header: bounded pool (4 in flight) of per-section calls to the EXISTING remoteOptimizeSection, per-section failure honest ('AI failed - original kept'); review rail (code-review style): per-section Accept/Reject + Accept all/Reject all + live 'X of Y left'; review state pending/diff/accepted/rejected/clean/failed; editor READ-ONLY during review; Acceptera/Angra/versions dropdown blocked until every section resolved; review ends itself when resolved (summary toast), accepted content rides the V1 save path. VERIFIED: tsc 59 baseline, build OK, wordDiff sanity-proven in node via esbuild (swap/insert/full-remove/full-add/identity all correct). NOTE (honest): the full browser flow = the owner's pair test - no browser automation on this box (no-rabbitholes law); engine risk zero (no hub/connector change in this pair).
- IMAGE METADATA [image-metadata] (arc pair 4, V3, engine v2.3 FROZEN in the architecture doc): CONNECTOR 3.0.2 - pcm_conn_normalize_src (entity-decode+trim, NO case fold, query kept; parity-pinned), image pass runs LAST over the final buffer (reaches rule-output images) rewriting ONLY alt/title on the matched img (esc_attr'd via callback - no backref injection; (?<![\w-]) guard so data-alt is never confused with alt), identity = occurrence among same-src CONTENT images (chrome logos neither counted nor touched - the frame law), src miss = inert+counted; rules endpoint schemaVersion 4 (accepts 1-4; image rows: src-normalized match + replacement whitelisted to {alt,title} re-encoded). HUB - PCM_Text_Matcher::normalize_src, save_image_rule (UPSERT on src+occurrence; anchorContext stores ORIGINAL alt/title ONCE at creation; clean revert = both attrs back OR revert:true deletes the rule; capability-first honest 409 pre-3.0.2; push-fail rollback), needs_v4 push gating (other posts push v1/v2/v3 byte-identical), image-rule endpoint + trpc seo.remoteSaveImageRule. EDITOR - click a locked image -> side panel (src read-only, alt/title inputs, Save, Revert-to-original shown when a rule exists via the rules overlay), occurrence computed in-doc with the same src identity; images still never move (V1 strip law untouched - image rules live BESIDE it). HARNESS 51/51 (+2 normalize_src parity, +8 image fixtures incl. chrome-twin isolation, esc_attr quoting, data-alt guard, ORDERING image-after-sections). VERIFIED: php -l x4, tsc 59, build OK + LIVE on powerleads: honest 409 against 3.0.1 proven, connector updated to 3.0.2 (sha256-verified hub package; note: WP's bundled wordpress.org update check hangs on this offline box - /update-now + wp_update_plugins timed out, so the MANUAL ritual applied: package downloaded from the hub, sha verified against the manifest, file replaced, 3.0.1 backup kept in Temp/pcm-probes), then FULL round-trip: save alt+title on the post-1 gravatar avatar (entity-carrying src matched) -> served snapshot shows both -> revert -> rule deleted, original alt serves, zero image rows left, owner's rules untouched.
- MODAL POLISH 2 [modal-polish-2] (owner items 1-4 + INCIDENT FIX): (1) Spara button = save WITHOUT closing (page mode; Acceptera = save-and-close, outlined). (2) versions dropdown: standalone Current row DELETED - the newest saved version row carries (Current). (3) Original row shows the page date + (original) (SeoRow.date rides the page prop). (4) Open-in-WP-editor moved LEFT beside the page name; image-law note stays right. (5) INCIDENT FIX (root-caused live 2026-07-11, owner report 'applied one update, page looks deleted'): the after-save pageQuery refetch pulled a TIER-DEGRADED snapshot (powerleads loopback failing -> content-rendered fallback lacks comment/menu sections) and the content effect setContent CLOBBERED the full document in the editor - DATA WAS SAFE (v35 page version = complete submitted doc; rules 23/24 written correctly, rule 14 absorbed; the removal guard refuses gutted saves). Fix: content effect loads ONCE (ref-guarded), page save refetches the VERSIONS LIST only, image revert applies server-returned originals in place (save_image_rule revert response gains original {alt,title}) - the editor NEVER auto-replaces its content. VERIFIED: php -l, harness 51/51, tsc 59, build OK.
- REVIEW FINDINGS [review-findings] (architect self-review, approved fixes): (F-C) diffBlocksHtml validity - word-diff runs INSIDE a tag only for SAME-TAG p/h pairs; structural pairs (list<->paragraph etc.) render removed-line + new block with per-<li> added marks (text directly inside ul was invalid and the editor dropped it - view-only defect, resolved states were always correct). (F-B) image identity completeness - assemble_content_html gap scanning now covers the WHOLE non-chrome document (before-first-block + after-last-block regions included): the editor's image set equals the connector's counting set BY CONSTRUCTION, occurrence indexes align, edge images become visible/editable. PROVEN offline against the REAL extracted connector: synthetic page with 3 same-src content imgs (before/between/after blocks) + a same-src chrome logo -> editor doc lists exactly the 3 content imgs in document order, connector occurrences 0/1/2 hit the SAME imgs in the SAME order, chrome copy neither listed nor touched. VERIFIED: php -l, harness 51/51, tsc 59, build OK.
- PARA CONSOLIDATION [para-consolidation] (PAGE FREEDOM pair 1, G6+G7): DELETED the dead paragraph-edit creation paths (zero frontend callers, grep-verified): trpc seo.remoteSaveParagraphRule + seo.remoteOptimizeParagraph, REST /paragraph-rule + /paragraph-optimize routes + both controller handlers, service save_paragraph_rule (~110 lines incl. its bespoke rollback) + remote_optimize_paragraph. ABSORB TIGHTENED (one-owner airtight): a section save now consumes paragraph rules matching the passed texts by ORIGINAL identity OR by the rule's SERVED OUTPUT (normalized visible replacement) - the live-observed coexistence class (#2+#21) can no longer be created; legacy paragraph rules die on the next save touching their section. Read-side (display folding, connector Pass 2, attribution) intentionally alive until fleet convergence (ledgered). VERIFIED: php -l x2, harness 51/51, tsc 59, build OK, zero dangling references (grep).
- ENGINE v2.4 [engine-v24] (PAGE FREEDOM pair 2, contract FROZEN in the architecture doc): CONNECTOR 3.0.3, wire schema 5, both ADDITIVE - (G1) target sectionRemove: pcm_conn_apply_section_remove locates EXACTLY like a replace (fingerprint-verified candidates, occurrence among verified twins - a changed section can never be wrongly removed) then removes heading+body blocks whole; between-content survives; phase order section -> sectionRemove -> sectionInsert (removes before inserts: neighbor-anchored inserts land, removed-anchor inserts go honestly inert). (G2) image hidden:true = tag removed at render (media/storage never touched; rule deletion restores); image pass REWRITTEN as ONE buffer scan (occurrence per normalized src over content imgs, stable indexes, edits applied reverse-offset) - multiple hides/rewrites on the same src can never shift each other. Sanitizers: sectionRemove carries section{level,fingerprint} + empty replacement BY LAW; image whitelist gains hidden at schema 5 only (a 3.0.2 site never receives it); accepts 1..5, v1..v4 payloads byte-identical. With v2.4 the rule algebra {replace, insert, remove} is CLOSED over block-level transformations. HARNESS 61/61 (+10: remove hit/between-content survival/fingerprint miss/occurrence twin/empty-body/ordering-vs-inserts, hidden tag-removal/occurrence/chrome-isolation/single-scan stability). VERIFIED: php -l, harness 61/61 first run.
- PAGE FREEDOM UX [page-freedom-ux] (pair 3, G3+G4+G5): HUB - needs_v5 push gating (sectionRemove or hidden-image rules -> schema 5, honest 409 pre-3.0.3), rules_to_schema + list_dynamic_rules carry sectionRemove, save_section_remove (UPSERT on section identity, restore:true deletes = clean revert, absorbRuleId kills the owning rule in the same push, capability-first + rollback), save_image_rule hidden:true ({hidden} replacement, never an accidental revert, response carries hidden); save_page_edits v2.4: RENAME PAIRING (LCS leftovers pair positionally inside the same gap between matched anchors - renamed headings are replaces), RESTORE detection (extra sections matching active sectionRemove identities delete their rule instead of inserting), removal routing per kind (insert-born -> rule dies; whole-rule groups collapse to ONE remove of the rule's ORIGINAL identity + absorb; partial slice -> units spliced out; original -> sectionRemove), IMAGE reconciliation per normalized src (doc vs baseline counts: fewer -> hide rules from the visible tail with originals captured; more -> hide rules die; images in removed sections vanish from the doc and are hidden by the same arithmetic), CONFIRM GATE (destructive saves return needsConfirm {removals, hiddenImages} and write NOTHING until confirm:true), result gains removed/restored/hidden/unhidden. FRONTEND - confirm dialog inside the modal (lists sections + images, Cancel / Remove & save; outside-clicks defer to the open dialog), image panel gains Hide image (node leaves the doc, media library untouched), richer save summary toast, trpc carries confirm. VERIFIED: php -l x2, harness 61/61, tsc 59, build OK + LIVE on powerleads 3.0.3 (sha-verified ritual update, 3.0.2 backup kept): confirm gate returned needsConfirm with ZERO writes (rule count unchanged), confirmed removal killed the Media section on the served page (heading AND paragraph proven absent by raw-html needles), restore brought it back + deleted the sectionRemove rule, avatar image hidden (tag absent from serving) then un-hidden via server-returned originals, owner's rules byte-count-identical at the end. ENV NOTE (root-caused): intermittent multi-minute CLI/REST stalls on this box = the FIRST process after the hub's update-check transients expire pays the offline wordpress.org timeout tax; everyone after is fast - environmental, not plugin code.
- EDITOR POLISH [editor-polish] (owner list of 6): confirm protocol DELETED end-to-end (hub param+gate, controller, trpc, dialog - E1: near-zero value since the load-once fix); FULL WIPE legal (empty doc = all sections removed + all images hidden; the zero-sections rejection died; no-sections error now fires only when BOTH sides are empty); save_section_remove ABSORBS covered paragraph rules (original identity OR served output - they could never serve post-removal, only stale-leak); image hint text DELETED + PERMANENT LESSON recorded (features reveal themselves through interaction - no instructional UI chrome, ever); header: Edit (WP editor) + Open (live permalink, new tab), both left beside the name; footer order Save & close -> Save -> Undo; ENGLISH ONLY (Acceptera/Spara/Angra dead everywhere incl. tooltips). LIVE-CAUGHT + FIXED during the pass: restore-after-wipe was a ONE-WAY DOOR (empty served baseline hit the no-sections guard) - wipe must be reversible: guard moved behind the edited parse, restores need no anchor, new-section-on-wiped-page fails honestly (no anchor exists). VERIFIED: php -l x2, harness 61/61, tsc 59, build OK, zero confirm/Swedish leftovers (grep) + LIVE clean-room cycle on a disposable powerleads draft: wipe removed=2 with paragraph-rule ABSORB proven (seeded rule consumed), identities printed equal on both sides, restore restored=2 + rules self-deleted + Alpha serving again. NOTE: two disposable test drafts remain on powerleads (remote delete refused 405) - inert, wp-admin removable.
- INSERT PLACEMENT [insert-placement] (engine v2.4.1, connector 3.0.4, contract frozen; NO schema/hub/signature change - the fingerprint model was REJECTED as binding insert survival to text it never touches): CONTENT-REGION PRIMITIVE - a pure the_content observer (last filter position, singular main-query, first non-empty win per pid) + exact-substring locator; the serving callback injects a SENTINEL comment at the region end BEFORE the passes run (offsets shift under earlier passes; a sentinel survives every mutation), strips it before output, and the exception path returns the PRISTINE original. PLACEMENT LAW: one clamp - an insert whose anchor lies inside the region may never land past the region end (covers both the next-heading and after-last-block branches; comment-area anchors keep legacy). ORDERING LAW: before-inserts in rule order, after-inserts in REVERSE rule order -> pages read in creation order (forward provably served THREE,TWO,ONE). Insert phase extracted from the unified section phase loop (partition once, order, apply). HARNESS 70/70 (+9: recorder/locator round trip, unrecorded + unfound nulls, clamp-with-furniture, comments-heading never pulls the insert out, outside-region legacy, sentinel survives exactly once, creation-order after x3, before x2 forward). VERIFIED: php -l, harness 70/70 first run, tsc 59, build OK + LIVE on powerleads 3.0.4 (sha-verified ritual, 3.0.3 backup kept): the owner's UNCHANGED rules #39-42 (placement data untouched; replacements relabeled ONE..FOUR for observability) render on the FULL authenticated draft preview at 60362..60465 - immediately after First para. (60341), INSIDE the post-content container, in CREATION order, with the first real nav element at 60918 AFTER them; pre-fix the same rules rendered below the nav (owner screenshots). Every existing insert rule on the fleet heals by connector update alone.
- ADD IMAGES [add-images] (pair closed; endpoint/picker/KEEP-law shipped in the 9c16d71 WIP): TWO live-caught defects fixed. (1) UNCHANGED-SKIP was IMAGE-BLIND - all four inputs (heading norm+level, paragraph fingerprint, raw visible text) are text-only, so an image-ONLY edit (add OR later delete of a platform image) skipped and never saved; fix = FIFTH input: ordered normalized srcs of KEPT (data-pcm-added + client-host, same $keep_img law) imgs per side; baseline side parses the slice's own units, originals compare as no-imgs BY FACT (added imgs exist only in rule content); section IDENTITY (keys/rename pairing/restore match) stays image-blind BY DESIGN - images say whether a section changed, never what it is. (2) F9-GENUS DUPLICATION (exposed by the new idempotence probe step): assemble_content_html's gap scan lifted the SERVED added img as a locked context copy beside the slice's in-place copy -> duplicate in the editor doc AND the duplicate passed the keep law (marker+host) so every re-save would clone it into the rule; fix = gap scan skips data-pcm-added (served added imgs are rule content, not between-content) + keep_img rejects data-pcm-locked (assembly context can never re-enter a rule - defense in depth). VERIFIED: php -l x2, harness 70/70 (engine untouched), tsc 59 baseline, build OK + LIVE full cycle on a disposable powerleads draft: deliver (hub file -> client media library, client-native URL back) -> place save saved:1 -> editor doc holds the img EXACTLY ONCE -> unchanged re-save saved:0/skipped:2 (idempotent) -> full render serves the client-host img EXACTLY ONCE -> image-only removal saved:1 with the rule CLEAN-REVERT deleted (rules left: 0) -> img gone from serving -> attachment PERSISTS in the client library (files outlive rules). ENV NOTE: new .php probe files now get eaten minutes after creation on this box (Temp AND project tree; old probes untouched) - live probes run via STDIN heredoc into php, no file persisted. Disposable leftovers on powerleads (drafts + w-logo test attachments) - remote delete 405s, wp-admin removable.
- SLUG REDIRECTS [slug-redirects] (connector 3.0.5, DB 1.39.0): CONNECTOR - hub-managed EXACT-PATH redirect store (option, full-set replace like rules) + THREE pure harness-pinned functions (norm_path: URL-or-path -> decoded path, query/hash stripped, case KEPT, no trailing slash; match: exact path hit -> {to,code} with QUERY PASSTHROUGH; sanitize: front-page refused, non-http(s) targets dropped, one rule per path, codes whitelisted 301/302/307/308); handler at template_redirect PRIORITY 0 gated on is_404() - a WORKING page can never be hijacked, normal views cost one check, kill switch rides pcm_conn_rules_off; + GET/POST /pcm-conn/v1/redirects (GET = capability handle, honest 404 pre-3.0.5) + GET /url-usage (site-wide who-links-here: bounded LIKE over content + custom fields; base64 builder data invisible to SEARCH by design - the universal replace still reaches it). HUB - seo_redirects table (additive dbDelta); PCM_Text_Matcher::normalize_path harness-pinned parity with the connector matcher (6 pins); service: connector_supports_redirects (memoized), list/save/delete with capability-first honest 409 + push-fail SURGICAL rollback (UPSERT restores the exact prior row; delete re-inserts with the same id), save optionally loops the EXISTING builder-aware /replace-url per url-usage post hub-side (best-effort AFTER the redirect is live, reported as linksUpdated/postsTouched, never rolled back into); 4 REST routes + 4 trpc routes. FRONTEND - RedirectPopup (prefilled editable From/To + 301/302/307/308 dropdown + "update N internal links" checkbox shown only when N>0; No writes NOTHING) offered ONLY after a real slug change on a CONNECTED PUBLISHED row (both save paths: direct edit + AI-accept; local tab never, drafts never); RemoteSiteSettingsPanel gains the Redirects section (from -> to, code, delete - no black boxes). DEAD CODE KILLED: includes/modules/seohub/service.php.fixenc (tracked leftover encoding-fix copy, loaded by nothing). VERIFIED: php -l x3, harness 88/88 (12 redirect fixtures + 6 parity pins), tsc 59 baseline, build OK + LIVE on powerleads: 3.0.4 gave the honest 409 BEFORE any write (negative test), sha-verified 3.0.5 rollout (3.0.4 backup kept), then FULL cycle on disposable published posts: save {linksUpdated:1, postsTouched:1} -> old URL 301s to the new URL served BY pcm-connector (X-Redirect-By header, priority 0 beats core) -> ?utm=proof&x=1 passed through -> seeded internal link in post B rewritten (old absent, new present; url-usage total 0) -> settings list shows the row -> delete stops OUR redirect (core's own old-slug fallback then answers, by=WordPress - the documented B2 honesty fact) -> hub rows 0. Disposable published posts 37/38 remain on powerleads (remote delete 405s) - wp-admin removable.
- SECTION FRAMES [section-frames] (the section-aware editor, gap doc CG1-CG5): HUB - assemble_content_html now EMITS the attribution it always computed then threw away: data-pcm-origin="original|owned|insert" on each section's heading (slices get it injected into their first heading tag; zero new queries); save_page_edits STRIPS the marker on entry - content identity, rules and version snapshots never carry it (emit-only editor metadata). EDITOR (page mode only) - ONE extension: (a) data-pcm-origin as a real heading attribute (survives edits/renames/reordering; keepOnSplit off; a heading typed or pasted this session has none -> platform-added by definition); (b) ONE decoration plugin drawing the page as a stack of sections: left rail per block colored by origin (slate = the site's own, amber = platform-edited, sky = platform-added), the section under the cursor tinted + rail intensified (you-are-here), and blocks ABOVE the first heading - the server-refused dead zone - locked contenteditable=false so the refusal can never be reached (a doc with NO headings locks nothing: wiped pages must accept typing). Decorations never serialize - they cannot enter a save BY CONSTRUCTION. Zero hint text (the no-instructional-chrome law): structure and color state the facts. HONEST NOTES: dated version-row loads carry no origins so their sections draw sky (a saved version IS platform-authored content); rails show load-time state + session additions until reopen. VERIFIED: php -l, harness 88/88 (engine untouched), tsc 59 baseline, build OK + LIVE on a disposable powerleads draft: pristine page = 2/2 headings original -> after one section edit + one new section the reload reads Alpha=original Beta=owned Gamma=insert -> round-trip save WITH the markers still in the doc = saved:0 skipped:3 (strip proven, zero false diffs, byte-path unchanged) -> 0 rule rows / 0 version rows contain the marker -> served page marker-free with Gamma serving. Browser visuals (rails/active/dead-zone lock) = the owner's pair test; engine risk zero (render-only).
- FAQ BLOCK [faq-block] (gap doc DG1-DG3; DG4 JSON-LD stays phase 2 by owner order): NATIVE <details>/<summary> accordion - ZERO JavaScript ships to client sites, the browser provides the fold; both tags + the structural inline styles (border/radius/padding/margin/font-weight/cursor - sanitizer-safe, no CSS functions) survive wp_kses_post on hub AND connector (core allowlist, verified line-by-line); fonts and COLORS INHERIT from the site - the inheritance is the design: it looks native anywhere without builder access. ZERO engine change, ZERO connector change, ZERO new endpoints (the LEAN RULING: the editor IS the hub - the skeleton is one exported template constant, not a fetched resource). EDITOR (page mode) - two minimal nodes (faqItem = details with open+style; faqSummary = inline summary with style; Enter inside a question moves into its answer, a summary never splits) + an Add FAQ toolbar action inserting heading + 3 editable Q/A items at the cursor (a NORMAL section: sky rail via [section-frames], removable/versionable/AI-reviewable like any other; diff view treats details as structural blocks - verbatim untouched, line-diff when changed). VERIFIED: tsc 59 baseline, build OK, harness 88/88 (engine untouched) + LIVE full lifecycle on a disposable powerleads draft: template save inserted:1 -> full render serves BOTH details with summary+border styles INTACT and the question text -> editor reload holds both items (round-trip) -> unchanged save skipped:2 (idempotent) -> ANSWER-ONLY edit saved:1 and serves -> section removal removed:1, zero details served, zero rules left. Browser ergonomics (toolbar click, Enter flow, native toggle) = the owner's pair test; engine risk zero.
- SECTION BLOCKS + FAQ EDIT-OPEN [section-blocks] (owner UX rulings 2026-07-13, replaces the rails wholesale): (1) BLOCKS - every section draws as ONE always-visible bordered block (start/middle/end roles join borders; the inter-block margins that zebra-striped the rails become inner padding so the box never breaks), 3px LEFT EDGE = origin (slate own / amber edited / sky added), air between sections; NO hover states, NO active highlight, NO "you are here" (owner ruling: the cursor already answers that - decoration restating the obvious is bloat, same lesson as the hint text); rails + active styles DELETED, zero leftovers (grep 0). Images pause the side borders (natural width kept) but keep the origin edge - named honestly. Dead-zone lock + origin plumbing unchanged (pair-3 laws intact). (2) FAQ SPLIT - the LIVE page now serves a real accordion STARTING COLLAPSED (the `open` attribute left the template AND the data model entirely - one attribute deleted, none stored); the EDITOR displays every item force-open via a display-only node view (toggle neutralized, fold markers hidden, summary edits as plain bold text) - you edit content, not widgetry. VERIFIED: tsc 59 baseline, build OK, harness 88/88 untouched + LIVE on a disposable powerleads draft: new-shape FAQ inserted:1 -> serves <details> WITHOUT open (collapsed, correct) -> editor round-trip keeps the items -> unchanged save skipped:2. Browser layer (block visuals, force-open editing feel) = the owner's pass.
- AI MODEL DROPDOWN [ai-model-select] (owner order 2026-07-13) + the FULL factual root-cause chain of "AI keeps failing": (LAYER 1, fixed) the editor's AI calls carried NO provider so the server default routed to openai - the one text provider with NO key on this install; every call died in ~1s with the honest key error. FIX: ONE model dropdown in the editor header (both page and section modes), fed by the SAME unified model registry as Copy/Ads/Settings (useTextModels - registry rows exist only for providers with ACTIVE keys, so the resolved model always works); resolution = user's saved pick (persisted per browser) -> prop-passed model -> first registry model; passed into every AI action (AI Optimize review, Ask AI, Re-write/Generate). (LAYER 2, fixed) run_prompt_section KNEW the calling user but never forwarded it to PCM_LLM::invoke - key lookup silently leaned on the WP session user (absent in cron/CLI/automation contexts); one line: opts.user_id = caller. (LAYER 3, NOT ours to fix - owner action) Avast Web/Mail Shield MITMs ALL outbound HTTPS from PHP: openssl shows api.anthropic.com answering with a certificate literally issued "generated by Avast Antivirus for SSL/TLS scanning" - WP's CA bundle rightly refuses it (cURL error 60). Until Avast HTTPS-scanning is excluded for local dev, EVERY provider call from the hub fails at TLS - this also newly explains the "cold-window" wordpress.org timeouts. HONEST RETRACTION logged: the suspected backslash-corrupted page routes were a Grep DISPLAY artifact - the file bytes are correct, nothing was changed. VERIFIED: php -l, tsc 59, build OK, harness 88/88 + LIVE: with model+provider+user routed the call now reaches api.anthropic.com and fails ONLY at the Avast certificate (proven two layers deeper than before). Full green requires the owner's Avast exclusion, then the dropdown pick runs on the real key.
- DOCUMENT LOOK [doc-look] (owner order 2026-07-13, reference screenshot: Docs-style viewer): PAGE MODE ONLY, section mode byte-identical. Modal -> a document card: min(980px, 94vw) x 90vh, rounded-xl, deeper shadow; header -> doc identity block (blue file icon + title + date subtitle) with the SAME options preserved (Edit/Open escape hatches, versions dropdown, AI model select, Add image, Add FAQ, Ask AI, AI Optimize, Save row) restyled borderless/ghost; content -> centered readable column (max 820px, generous padding) with a DOCUMENT type scale: 15px/1.75 body, serif 27px h1, stepped h2-h6, roomier lists/paragraph air; section blocks kept per the standing ruling but lightened (slate-100 hairlines, larger inner padding, more air between sections) so the page reads like a clean document that still shows its structure + origin ticks. VERIFIED: tsc 59 baseline, build OK (no PHP, no engine, decorations still non-serializable). Visual acceptance = the owner's pass.
- EDITOR POLISH 3 [doc-look-2 + versions-truth + ai-scope] (owner list 2026-07-13, 10 items, page mode only, NO PHP): WINDOW - real backdrop (dim + blur; clicks bubble so the outside-click flow is byte-identical), borderless rounded-2xl card, taller/airier header; TEXT dialed back to the pre-doc-look scale but keeping the document feel (serif 22px h1, stepped headings); SECTION BLOCKS tightened (smaller paddings, less air, rounder xl corners - the "too big" ruling). VERSIONS TRUTH - the Original row's grey background DIED (it read as "selected" - the owner's exact confusion; it was only "undeletable" styling); the ACTUALLY SHOWN state now carries a check mark (draft/original/dated rows alike); NEW Draft state: unsaved edits flip the button label to "Draft (unsaved)" and a Draft row tops the list - the state on screen is ALWAYS in the list (the missing-13th mystery answered: the list was saved-rows-only). AI SCOPE - Ask AI + AI Optimize MERGED into ONE split control (one-owner law): main button always NAMES its scope - "Optimize (whole page)" (red/green review, as before) or "Optimize selected" when a text selection exists (NEW: serializes the selection, same optimize endpoint, the answer replaces the selected range in place - undo + versions as the net; zero server change); the caret opens the existing instruction field ("optimize for keyword X", "inject keyword Y five times") which steers BOTH scopes; Enter in the field runs the current scope. Selection/dirty are LIVE states (editor update + selection ticks). Section mode byte-identical. VERIFIED: tsc 59 baseline (all 59 pre-existing, none in SEO), build OK, harness untouched. Visual + selection acceptance = the owner's pass (AI runs still gated by the Avast HTTPS-scanning exclusion).
- AI SCOPE FIX [ai-scope-fix] (regression owned + rebuilt right, owner-tested live): the unreviewed selection-replace path is DEAD - EVERY AI run now goes through the red/green review; a text selection only narrows the SCOPE (the sections it touches are sent; everything else resolves 'clean' up front, never sent, invisible in the rail) - ONE pipeline shared by page + selection runs BY CONSTRUCTION (same rail, same Accept/Reject, same finish). Labels state scope with no noise: "Optimize page" (no selection) / "Optimize (selected text)". NEW OK button (replaces the scare-labeled "Reject all" which secretly WAS the finish): accepted changes stay, everything undecided keeps its original, rail closes, editor unlocks - the missing exit the owner reported. Rail counter counts only real rows; out-of-scope rows hidden. Instruction field steers both scopes (Enter runs the current scope). VERIFIED: tsc 59, build OK + OWNER-TESTED LIVE: selection run produced the rail (1 of 1), red/green inline, Accept/Reject + OK on the Cookies section. PROCESS NOTE recorded: the initial revert was made without owner approval - rule reaffirmed: no change without explicit GO.
- MODEL TRUTH [model-truth] (owner order 2026-07-13, three workstreams, one pair): (1) SHARED DROPDOWN - Copy's ModelDropdown (pill trigger, tier-grouped floating panel) EXTRACTED to components/shared (it was welded inside ResultsPanel - nothing else could use it); Copy now consumes the shared one (embedded copy deleted, zero dead code), the SEO page editor's bland native <select> replaced by the SAME component - one model picker everywhere, never module-welded again. (2) SELF-REFRESHING REGISTRY - the list was NEVER static (it syncs from the provider's LIVE /v1/models API) but synced only on key-add or manual resync: the owner's list was a May 12 photograph. NOW: reading the registry keeps it fresh - both read endpoints call maybe_schedule_refresh: any ACTIVE provider (openai/anthropic/google/fal - mirrors the validate_api_key map) whose rows are older than a DAY gets a background wp-cron resync (transient stampede lock, 6h; additions synced, vanished models marked unavailable). No cron babysitting, no manual ritual - the list stays live BECAUSE it is used. (3) GENERATED-WITH TRUTH - PCM_LLM always reported which model ACTUALLY answered; the SEO layer threw it away. run_prompt_section now returns {value, model, provider} (all 3 callers updated - heading endpoints pass it through additively), remote_optimize_section forwards it, and the review rail prints the actual generator under each section - the dropdown's claim is verifiable on screen. VERIFIED: php -l x4, harness 88/88, tsc 59, build OK + LIVE: registry age proven frozen (2026-05-12) -> one read scheduled the refresh (event + lock proven) -> second read did NOT double-schedule (1 event) -> cron hook registered via the module automations glob. The refresh itself executes web-side on the next hub visit (wp-cron; CLI TLS is Avast-walled) - the dropdown then mirrors Anthropic's live list. Duplicate opus-4-7 rows: userId 1 + userId 2 each own a row (per-user registry) - NOT a bug.
- LIVE PRICING [live-pricing] (owner order 2026-07-13: continuously-synced, accurate tier categorization "the easy smart way"): HARD FACT anchoring the design - AI providers publish prices on WEBSITES, never in their APIs; the one machine-readable continuously-updated feed is OpenRouter's PUBLIC catalog (no key). BUILD: models table gains inputPrice/outputPrice (USD per MILLION tokens, DB 1.39.0 -> 1.40.0 additive); the background self-refresh job now also stamps real prices onto every matched row (catalog fetched once, 12h transient, serves all providers); costTier becomes MATH - blended (in+out)/2 against thresholds stored as DATA (option pcm_model_tier_thresholds, defaults budget<=2 / standard<=15, editable without code); HAND-SET tiers (status != 'auto') are never touched; models absent from the catalog keep the old name-pattern tier - the hardcoded pattern maps in PCM_Providers demote from "the truth" to "the fallback". Identity across catalogs via a PURE normalizer (provider prefix + :variant stripped, separators folded, trailing date stamps dropped). VERIFIED: php -l x2, harness 88/88, no TS changes (59/build stand) + LIVE: migration applied (price columns exist), 5/5 real-world id shapes match (incl. date-stamped haiku + :beta variants), tier math exact on July-2026 price points, and editing the thresholds option changed the derived tier with ZERO code - data-driven proven. End-to-end price fill executes web-side in the next background refresh (same Avast-walled-CLI note as the registry refresh).
- RESTORE TRUTH + HEADER + PREVIEW [restore-truth] (owner report 2026-07-13 "restored original, reopened, got another version"): ROOT CAUSE PROVEN LIVE FIRST (data-first law) - the restore is FLAWLESS: instrumented cycle on a disposable post showed edit->rule, pick Original->Save = rule clean-reverted (0 left), served = original, REOPEN doc = original byte-normalized. The defect was the LABEL: the restore-save records a (correct) new dated version row and the dropdown crowned it "(Current)" with a fresh timestamp - a perfect restore READ as "another version". FIX: when the newest save equals the original (normalized compare, origin-marker-blind) the label says "(Current — original)" - the state names itself. + HEADER: title/date shrunk to one quiet baseline row (xs title, 10px date, smaller icon) - no more view distortion. + PREVIEW: third header button (Edit / Open / Preview) opening the table's EXISTING inline preview window (renders above the editor; no new preview machinery - the one we have, reused). VERIFIED: tsc 59, build OK, harness untouched + the live reproduction above; Save & close re-exercised in the same cycle (the save path IS the proven path).
- BUSINESS CONTEXT + PAGE TYPE [brand-context] (owner order 2026-07-13, per-paragraph history SKIPPED by owner): (1) SITE<->BRAND LINK - sites gains brandId (DB 1.40.0 -> 1.41.0 additive, FK only - brands own the business data per the registry law); sites update endpoint whitelists it (0/null unlinks). (2) THE FACT THE FEATURE STANDS ON: remote pages' {{business.*}} prompt placeholders were BLANK (address/phone/category/hours all '') - local-intent AI content had nothing real to use; now a linked brand fills them via the SAME resolver the local generator uses (brand name + GBP resolved details), site name/url fallback unchanged when unlinked. (3) PAGE TYPE - per-post setting (option map per user+site, whitelisted: local/blog/product/service/landing, '' = general) telling the AI WHAT it optimizes; rides remote_field_vars as page.type. (4) PROMPT APPEND LAW extended (same as topic): page type + business context reach the model even on templates predating the placeholders - with "never invent contact data" stated in the injected line. (5) ALL remote AI paths carry it: section optimize, field generate, heading optimize (user+post now passed through). (6) HEADER UI - Business dropdown LEFT of the document name (shows the linked brand or "No business" - you SEE what the AI gets; picking links/unlinks live) + Page type dropdown beside the model picker; BOTH reuse the shared ModelDropdown (one dropdown law); values seeded from the inventory payload (pageType + brandId now ride remote_get_inventory - first landed in the internal served_inventory by mistake, caught by the live probe, moved, dead fields removed). VERIFIED: php -l x4, harness 88/88, tsc 59, build OK + LIVE: migration (brandId column) -> brand "Profit Media" linked to powerleads -> business.name resolved from the BRAND in remote vars (phone/address empty because that brand has no GBP data - honest) -> page type save/read/vars = local -> bad type refused -> inventory carries pageType "local" + brandId 13 -> owner state restored (unlinked, type cleared).
- HEADER WORKBENCH [header-2rows] (owner UX session 2026-07-13): the page editor's ONE overloaded row (11 controls, title colliding with buttons) split into TWO rows each answering ONE question. ROW 1 - the document: icon + STACKED title/date (owner correction honored: the NAME keeps its size, only the DATE is small, NEVER on one line - the earlier flattening was my unrequested change, reverted), Edit/Open/Preview escape hatches, versions dropdown, close. ROW 2 - the workbench on a whisper-gray strip: LEFT the AI's always-visible context (Business pill + Page type pill - shared ModelDropdown extended with optional icon + role-as-placeholder props: a 14px icon names the control, the ROLE shows only while unset, the value takes over after; no floating label text per the no-chrome law), RIGHT the tools reading like a sentence: NEW [+ Insert] menu (Image / FAQ - the two occasional buttons collapsed into one scalable home) -> model picker (moved beside the button it parameterizes) -> Optimize split button. Versions + close hoisted to ONE shared definition used by both page row 1 and the untouched section-mode header (no duplication). VERIFIED: tsc 59 baseline, build OK, harness untouched. Visual acceptance = the owner's pass.
- HEADER CORRECTIONS [header-fixes] (owner list 2026-07-13, 7 items): (1+2) the Business and Page-type pills stripped to PLAIN dropdowns - the icon + role-placeholder experiment DIED (also rendered broken/overflowing): business shows "No business" or the brand name, type shows General/Local search/... and NOTHING else; the icon/placeholder props were REVERTED out of the shared ModelDropdown (no consumers = dead API, zero-dead-code law). (3+5) the workbench row's gray strip is GONE - white, properly. (4) dropdown triggers had transparent backgrounds leaking the page through - now SOLID white surface (shared fix). (7) dropdowns THINNER (3px vertical padding, xs text, smaller chevron - shared, so Copy slims identically). (6) the Optimize button now IS the shared blue generate pill: NEW shared PillSplitButton (PillButton's 'active' palette + a divided caret zone on the right, owner-sanctioned extension) replaces the bespoke gray split - every generate-class action across the app now looks identical; the caret opens the instruction field exactly as before. VERIFIED: tsc 59 baseline, build OK, harness untouched. Visual acceptance = the owner's pass.
- DROPDOWN AWARENESS + HEADER SPACING [dropdown-aware] (owner list 2026-07-13, approved checklist): (1) the shared ModelDropdown panel opened BLIND (hard right-anchor) and got clipped by overflow-hidden containers (the editor's rounded card) - now WINDOW-AWARE: on open it measures its trigger and grows the panel toward the side with room (left-half triggers grow right, right-half grow left) - every consumer app-wide fixed at once, zero new API. (2) title/date stack had zero breathing room (colliding lines) - proper line heights + a small gap. (3) versions trigger joins the shared pill family (rounded-full, white, xs/medium - visually identical species to the new pills); its LIST stays bespoke BY DESIGN (delete buttons, active check, Draft/Original rows = a version manager, not a picker - feature-flagging the shared component for one consumer is how tech debt starts, per the gap analysis's ruling). VERIFIED: tsc 59 baseline, build OK, harness untouched (no PHP).
- REVIEW CLARITY [inline-review] (owner-picked GitHub/Cursor + Google combo, 2026-07-13; heading word-diff UNTOUCHED by owner order): (1) INLINE DECISIONS - during an AI review every CHANGED section carries its own floating [checkmark Accept | x] chip at the top-right of its heading; you approve WHERE YOU READ - widget decorations only (can never enter content), wired to the SAME resolveSection the rail uses (one machinery, zero duplication); the rail stays as overview + bulk (Accept all / OK). (2) CLICK-TO-JUMP + FOCUS FLASH - clicking a rail card scrolls the document to its section and PULSES it (~2s: lane goes dark blue + soft wash, eased both ways) so the landing is always visible; lane colors at REST stay the origin system (grey own / amber edited / sky added - owner ruling: don't change the colors, flash only). (3) BUTTON FAMILY UNIFIED (owner order incl. "the button as well"): dropdown triggers bumped to PillButton's exact sidebar metrics; the Insert button and the footer joined the rounded family - Save & close now uses a NEW shared PillButton 'success' variant (solid green, the save semantic, app-wide reusable), Save (stays-open) and Undo became rounded pills of the same height (colors unchanged). VERIFIED: tsc 59 baseline, build OK, harness untouched (no PHP). Visual + interaction acceptance = the owner's pass (AI runs still need the Avast exclusion for red/green to materialize).
- REVIEW INTEGRITY [review-integrity] (owner GO 2026-07-13, gap doc GAP-ANALYSIS-REVIEW-INTEGRITY-20260713.md G1-G7): the review pipeline stops distorting content and lying with color. (G1) diffBlocksHtml is now ATTRIBUTE-PRESERVING - rebuilt same-tag blocks clone the NEW block (its own attrs survive) and a withOrigin helper carries data-pcm-origin from the ORIGINAL heading (the only reliable carrier - the AI reply echoes no attributes); origin loss was repainting every reviewed lane blue THROUGH the 500ms lane transition (the owner-reported "animation"). (G1b) accepting a CHANGED section restates identity via restateOrigin: original -> owned = the lane reads AMBER the moment it lands (owner-ratified semantics: amber = edited, sky = strictly platform-ADDED; insert stays insert; version-loaded docs keep their no-origin fact) - matches what the server emits on next load (service.php origin walk). (G2) stripDiffHtml PRUNES what it EMPTIES: only ancestors of removed runs are candidates (the user's own blank paragraphs untouchable), deepest-first so fully emptied lists collapse with their items, anything holding an image survives - phantom <li></li> bullets and <p></p> gaps die for every element type. (G3) ONE content oracle effectiveContent(i): per-section BASELINE captured from the editor at every suggestion landing (initial + each revise); live == baseline (same serializer both sides) -> the AI's stored clean s.ai = FULL formatting (links/bold/lists, zero distortion, all element types); edited -> stripped live (the user's words trump; word-diffed paragraphs plain THERE only - named, bounded edge). Accept AND the Revise draft consume the oracle - the plain-text VIEW is never the content source again (the widest distortion, F2/F6, dead); liveSection deleted (absorbed). (G4) shared PillButton resets its hover flag on disable (a button disabled under the cursor never fires mouseleave - browser fact - and re-surfaced stuck dark on re-enable). (G5) chip hover styles were CONTAINER-scoped (hover:[&_x] on the EditorContent wrapper = hovering ANY paragraph darkened EVERY chip; rail hover lit them) -> SELF-scoped ([&_x:hover]). (G6) hover LIGHTENS everywhere in the review: Accept green-600 -> green-500 hover (chip, rail row, Accept all, Adjust, image Save joined the grammar); Revise = the AI-action family: white pill, primary/40 outline, primary text, #e7f5ff hover fill (chip + rail row + Revise all); decline ghosts hover slate-50; shared PillButton palette untouched (owner scope). (G7) the ONE global busy boolean that dimmed ~15 controls on any click is now a NAMED action (busyAction: save/saveClose/remove/ai/imageAdd/imageSave/imageHide/imageRevert) - only the INVOKED control shows the working state (its own spinner), everything else keeps its resting face while still functionally guarded (save() refuses concurrent runs; runAi/addImage/saveImageMeta/Undo guard busy; model + page-type dropdowns un-disabled - picks are config for the NEXT run, harmless anytime). VERIFIED: tsc 59 baseline (0 in touched files), build OK (22.4s), harness 88/88 (no PHP in the pair - engine untouched). Regression surface held: reject byte-faithful (s.html untouched), orphan zone untouched, section mode untouched. Visual acceptance = the owner's browser pass - LOCAL ONLY
- AI CONTENT GATE [ai-content-gate] (owner GO 2026-07-13, gap doc GAP-ANALYSIS-AI-CONTENT-GATE-20260713.md A1-A4): the empty-bullets-on-generate root cause closed as a LAW, not a patch. ROOT (proven at its lines): the pipeline speaks ONE dialect - the editor's normalized serialization - everywhere EXCEPT the AI reply, the only stored RAW input; insertContentAt defaults preserveWhitespace "full" and the diff view passes AI lists/structural blocks through verbatim, so a pretty-printed reply (<ul> with newlines between items) forces the parser to coerce each stray newline into a listItem = one empty bullet before every real one, surviving Accept too (the G2 strip can't catch them - no diff marks). BUILD: canonicalAiHtml(editor, html) - THE gate: every AI reply is parsed through the editor's OWN schema (createNodeFromContent + getHTMLFromFragment, shipped + exported in @tiptap/core, ZERO new deps) with normal whitespace rules and re-serialized at its two birth points (initial-run worker + Revise); everything downstream (diff view, stored s.ai, untouched-Accept, Revise draft, baseline compare) is editor-canonical BY CONSTRUCTION - the whole raw-output artifact class (whitespace phantoms, alien markup) dies at one visible point; schema-unrepresentable content is dropped AT THE GATE, exactly what the editor would do on insert, never mid-review. Section mode verified safe by construction (setContent normalizes, stores nothing raw) - untouched. REJECTED as debt: parseOptions patch at applySection (tolerates dirty input at one of N consumers), server-side normalization (a second parser in eternal lockstep). VERIFIED: tsc 59 baseline (0 in touched files), build OK (22.2s), harness 88/88 (no PHP). Empty-reply/clean/failed branches keep their meaning ('' stays ''). Visual acceptance = the owner's browser pass on a bullet-list section - LOCAL ONLY
- DIFF VIEW FIDELITY [diff-view-fidelity] (owner GO 2026-07-13, gap doc GAP-ANALYSIS-DIFF-VIEW-FIDELITY-20260713.md B1-B2): the review view stops destroying the shape of what you HAD. ROOT: removedBlock flattened EVERY removed block to one run-on red paragraph (a 4-bullet list read as a red blob) while the green side kept shape - and pairing was POSITIONAL, so ONE AI-inserted intro paragraph misaligned every later pair and cascaded whole sections into all-red/all-green noise. BUILD: (B1) ONE shape-preserving markBlock(b, kind) replaces BOTH addedBlock and removedBlock (both deleted - single renderer, the asymmetry cannot return): the block renders in ITS OWN shape with every text-bearing LEAF (p/h/summary/bare li) wrapped in the diff span - a removed list is a RED LIST bullet by bullet, a removed FAQ a red Q&A box; image-holding leaves stay verbatim (the view never destroys an image); attributes survive on both sides (identity law, symmetric with G1). Safe BECAUSE of the G2 strip-prune: fully-red wrappers vanish completely on accept/save. (B2) pairByClass - LCS over block PAIRING CLASSES (h1-h6 = one class so a level tweak never unpairs a heading from its origin; same in-house DP family as wordDiff, zero deps) replaces positional indexing: equal-class blocks pair (text tags word-diff inside the NEW tag; structural pairs = red shape + green shape), unmatched blocks render added/removed in shape - an inserted paragraph shows as ONE green paragraph and nothing else misaligns; identical class sequences pair exactly as before. VERIFIED: tsc 59 baseline (0 in word-diff), build OK (25.7s), harness 88/88 (no PHP). View-only by construction - Accept/Reject/oracle/strip untouched. Visual acceptance = the owner's browser pass on a bullet-list rewrite - LOCAL ONLY
- THE OPTIMIZER I1 [optimizer-spine] (owner GO 2026-07-13, arch doc GAP-ANALYSIS-OPTIMIZER-ARCHITECTURE-20260713.md): the teacher/catalog/basket system is ALIVE end-to-end. NEW MODULE includes/modules/optimizer (auto-discovered; config + thin controller GET /optimizer/teachers + POST /optimizer/analyze taking ONE teacherId so the rail's per-purpose re-analyze IS the endpoint + service = ENGINE ONLY: lazy teacher glob-registry mirroring the automations pattern, checklist DATA as read-through-seeded editable option pcm_optimizer_checklists per the hub-controlled-data law, dispatch) + teachers/ with the interface (pure context-in/items-out - teachers structurally cannot talk to each other) and the FIRST teacher class-pcm-teacher-search (page-type checklist judged in ONE PCM_LLM::invoke_json call, verdicts mapped in DATA order never model order, every verdict carries EVIDENCE - quote on pass, the concrete missing thing on fail; model-skipped checks logged + omitted, recoverable via re-analyze). ONE catalog contract forever: {id, teacherId, found, label, evidence, instruction}. NEW FRONTEND DIR app/src/modules/SEO/optimizer (types + useOptimizer: parallel per-teacher runs failing ALONE w/ stale-run drop guard, basket pre-ticks found gaps, buildDirectives compiles the basket + OptimizerRail: purpose sections in registry order, FOUND tickable / NOTHING-TO-FIX quiet, per-purpose re-analyze, Optimize-selected footer + sections.ts = the custom-body socket I2 fills). SectionModal touched at TWO SEAMS ONLY (arch law): the Analyze button - new shared PillButton OUTLINE variant (white/blue-outline generate-family sibling, visually distinct from the filled Optimize; + title prop forwarded, a shared gap) placed LEFT of Optimize - and the rail mount handing basket directives to the EXISTING startAiReview topic (every change still lands as red/green; zero new write paths). trpc routes optimizer.teachers/analyze. VERIFIED: php -l x5, tsc 59 baseline (0 new), build OK 22.4s, harness 88/88. Live AI acceptance = the owner's browser pass (Analyze -> tick -> Optimize selected -> red/green) - LOCAL ONLY
- OPTIMIZER FIX [optimizer-json-contract] (owner live report 2026-07-13: Analyze spun, then NOTHING): root cause PROVEN from the server log (data-first) - the Anthropic call SUCCEEDED (~15s, claude-opus-4-6) and returned valid JSON, but PCM_LLM drops response_format for Anthropic BY DOCUMENTED DESIGN (class-pcm-llm.php:126 - the prompt must carry the schema there) and the search teacher's prompt described semantics without the exact JSON SHAPE -> the model chose its own shape -> 0 of 10 verdict ids matched (log: 10x model skipped check) -> empty SUCCESS -> the rail rendered blank. THREE-LAYER FIX: (1) the exact output contract now lives IN the prompt ({"checks":[{"id":"<echoed EXACTLY>","passes":true,"evidence":"..."}]}) - provider-agnostic, harmless where the schema is also enforced; (2) ZERO matched verdicts now THROWS an honest failure (the rail shows the red row + its retry - an empty success is the silent fallback the law forbids); (3) the rail states an empty done-result explicitly (never a blank section) and surfaces the real error text on failures. VERIFIED: php -l, tsc 59, build OK 21.9s - LOCAL ONLY
- OPTIMIZER S1 [optimizer-positives] (owner GO 2026-07-14, batch1 gap B1): rail found-rows now lead with the ACTION (the instruction - "do this"), the finding becomes the quiet subtext and the check name moves to the tooltip - a to-do list, never a fault list. Seed instructions audited: already clean imperatives, no data change needed (stated honestly). VERIFIED: tsc 59, build OK - LOCAL ONLY
- OPTIMIZER S2 [optimizer-compiler] (owner GO 2026-07-14, batch1 gap B2-B4): THE BASKET COMPILER - ticked suggestions are never dumped raw at the rewriter again. POST /optimizer/compile: ONE invoke_json (JSON contract IN the prompt per the Anthropic law) merges overlaps, resolves collisions preserving both intents, orders by execution sense; SERVER-SIDE CERTAINTY CHECK: every input index must appear in the output sources union - a dropped intent THROWS (shown red in the rail footer, the run never starts partial); single-item baskets bypass the model deterministically (nothing to merge = no cost without function). Output {text, purposes, sources} = the run topic AND the provenance. REVIEW PURPOSE VERIFICATION: startAiReview carries the compiled directives; each suggestion row now shows purpose PILLS (SEO/TOPICS/KW/LINKS/AI) + the compiled to-do bullets (first 3 + tooltip with all) REPLACING the model line (model moved to tooltip - data still consumed); plain Optimize runs keep the model line honestly (no basket = no pills). Rail footer: Compiling-the-order state, compile errors shown in place. VERIFIED: php -l x2, tsc 59, build OK - LOCAL ONLY
- OPTIMIZER S4 [optimizer-subtopics] (owner GO 2026-07-14, batch1 gap B11): the TOPIC COVERAGE teacher - ONE new server file, ZERO frontend change (the add-on law proven in practice). One invoke_json (contract in prompt): derive the page's core topic, list the 4-7 subtopics a COMPLETE page covers, judge each against THIS content (covered needs substance, not a mention; evidence = quote or what the missing section should say); missing subtopics land as tickable suggestions whose instruction adds a coverage section through the normal compile->optimize->red/green pipeline; off-contract answers throw honestly (rail retry). The rail gains the section automatically via the registry. VERIFIED: php -l, harness 88/88 (frontend untouched) - LOCAL ONLY
- OPTIMIZER S3 [optimizer-keywords-drawer] (owner GO 2026-07-14, batch1 gap B5-B10): THE LEFT KEYWORD DRAWER + the keyword ride. SERVER: PCM_GSC::query_stats - per-QUERY stats for ONE page (dimensions [query] + exact page filter, same 2-day-lag window law, reuses the private sa_rows INSIDE the class) - POST /optimizer/keywords/stats mirrors the proven integrations candidate-try flow (www/non-www/sc-domain twins) - the bucket = option map keyed site:post (GET/POST /optimizer/keywords, sanitized, deduped, capped 50, empty deletes the entry). DRAWER (left aside, Keywords toggle on the workbench row): primary + supporting keyword inputs seeded from the SEO row (props through the call site) and saved via the EXISTING seo.remoteSaveCell route (one write path per field) - the additional-keywords bucket as removable pills - days-back (default 30), related-only (named transparent v1 heuristic: shares a meaningful token with the primary), >=100-impressions noise filter (default on, removable) - the shared DataTable compacted (add/keyword/clicks/impressions/position, default sort impressions desc) - honest states for no-URL/GSC errors/empty windows. THE RIDE (owner law): primary + bucket keywords join EVERY optimize run (one-click, instruction and basket runs alike) as natural-fit targets, never stuffing orders; primary/supporting live in ONE SectionModal state so the drawer and the runs read the same truth. HONEST DEVIATIONS from the arch sketch: no keywords TEACHER file (the auto-ride ruling supersedes it - a rail section would double-inject) and sections.ts DELETED (the drawer is the keywords surface; an empty forever-socket is dead code). VERIFIED: php -l x3, harness 88/88, tsc 59 (0 new), build OK 23.1s - LOCAL ONLY
- KEYWORD DRAWER GEOMETRY [keyword-drawer] (owner GO 2026-07-14, gap GAP-ANALYSIS-KEYWORD-DRAWER-GEOMETRY-20260714.md, rejected first attempt REDONE senior-grade): the drawer now slides OUT from under the card's LEFT edge - the content column is never squeezed and the card never moves. ONE geometry constant PAGE_CARD_WIDTH = max(720px, min(980px, 94vw, 100vw-32px)) - provably equivalent to the old width/minWidth/maxWidth trio (CSS min-over-max resolution) - consumed by BOTH the card (its width) and the drawer's anchor (right: calc(50% + W/2)): derived, never mirrored, no magic numbers, changing the card moves the drawer by construction. The drawer is a fixed SIBLING rendered BEFORE the card (the card is overflow-hidden and its shadow owns the seam), 86vh at top 7vh (no inline transform - the slide animation owns transform), animate-in slide-in-from-right via the ALREADY-imported tw-animate-css (one class, zero deps), drawerRef exempts it from the outside-click save. PLUS the supporting-keywords rewire: the drawer now reads/writes the SEO table's REAL supportingKeyword field (index.tsx:253, rows carry it) instead of metaKeywords - seeded from the row, saved via the existing remoteSaveCell route. Named bound: viewports under ~1050px clip the drawer (v1 accepted). VERIFIED: tsc 59 (0 new), build OK 28.6s, no PHP - LOCAL ONLY
- KEYWORD DRAWER POLISH [keyword-drawer-polish] (owner order 2026-07-14, gap bcb461a): the bucket's bare instructional paragraph DIED (no-instructional-chrome law - owner refinement recorded: hover popover/info-icon is sanctioned, bare surface text never) - in its place a type-and-Enter INPUT (zero new plumbing, bucket.add existed) above the pill collection; guidance lives in the input's hover title only. Drawer widened 300->360px for longer keywords (right-edge anchored - widening extends OUTWARD, the one-constant geometry holds). VERIFIED: tsc 59, build OK - LOCAL ONLY
- KEYWORD STATS CACHE [kw-stats-cache] (owner GO 2026-07-14, gap 7394473): the drawer's keyword table gains an HONEST stored layer - a production feature whose seedability is a data side effect, ZERO test-specific code. Server: every successful live GSC fetch stores {property, rows capped 300, fetchedAt} in option map pcm_optimizer_kw_stats_cache keyed site:post (the bucket's proven pattern); live failure (no integration / Google error) serves the stored rows LABELED source=stored + fetchedAt (never passed off as live - the no-silent-fallback law); no store either -> the honest error stands; the live path was extracted into one private fetch_live_keyword_stats (candidate-try flow unchanged). Drawer: sends site/post identity, shows a quiet stored-<date> chip beside the controls when rows are stored (status fact, hover explains), refresh still attempts live. SEEDED as pure data: 10 plausible keyword rows per page for all 16 powerleads pages - the table/sort/filters/+ flow fully testable on this GSC-less machine; rows deletable, the code cannot tell they are fake. VERIFIED: php -l x2, harness 88/88, tsc 59, build OK 26.2s - LOCAL ONLY
- REVIEW SECTION IDENTITY [review-section-identity] (owner GO 2026-07-14, gap d37f10d, master 50b201b): section identity moves from COUNTED ORDINALS to STABLE ANCHORS - the Accept-All corruption (unresolved all-green sections + one mangled overlap after the AI legitimately ADDED many sections mid-review, ordered by ticked subtopic suggestions) dies at the root. data-pcm-review-id registered beside data-pcm-origin (same proven heading-attr pattern); stamped on every section heading in ONE transaction at review start (the ordinal is trusted only at t0); the ONE writer (applySection) re-stamps the anchor on every landing/resolution so identity survives by construction; sectionRange finds by id and a section now ENDS at the next ID-CARRYING heading - AI-added id-less headings BELONG to the section that produced them (accept keeps, reject removes, revise resends: multi-section replies correct by construction, never forbidden); new splitReviewSections (word-diff, id-boundary walk) consumed by the oracle + both baseline captures; chips map by the heading anchor (counter died); focusSection locates by anchor and translates to the current ordinal for the flash; anchors are REMOVED in one transaction when the review completes + the server save-strip belt now covers both editor attrs (service.php:4695 regex); deleted-anchor edge resolves honestly (no write, status rejected, toast). Single-heading replies byte-identical; lanes/oracle/strip/compiler/teachers untouched. VERIFIED: php -l, harness 88/88, tsc 59, build OK 32.4s - LOCAL ONLY
- DRAWER FILTERS + PAGE PICKER [kw-drawer-filters-pagepicker] (owner GO 2026-07-14, gap b71d565, master 50b201b): (B1) the shared ModelDropdown gains an OPTIONAL searchable prop - filter input at the panel top, groups hide while emptied, query resets per open - additive, every consumer can adopt it. (B2) the drawer table now uses THE shared DataTable header filters (layoutKey optimizer-kw-drawer + FilterDefs: keyword contains, clicks/impressions >= N, position <= N with junk input matching all) - the bolted-on >=100 impressions checkbox DIED; noise filtering is per-column and user-owned like the SEO table. (B3) the PAGE PICKER: a searchable dropdown of the site's pages above the controls chooses WHICH page's GSC data is read (default = the edited page; picking re-points postId+pageUrl and rescans - the stats cache key follows, so the seeded pages are switchable TODAY); the + always feeds the EDITED page's bucket - the picker changes the data source only. (B4) index.tsx passes the pages from the rows it already holds (id/title/permalink, permalink-less rows excluded) - zero server work. Also fixed in passing: the refresh button would have passed the click event into scan(t) - explicit arrow. VERIFIED: tsc 59 (0 new), build OK 27.3s, no PHP - LOCAL ONLY
- REMOTE DELETE 405 FIX [remote-delete-405] (owner demand 2026-07-14, gap e563a15): the long-documented fleet mystery (handover: remote delete 405s -> wp-admin removal) PROVEN and KILLED. Proven live on a disposable post: status 405 with a NULL body = the web server in front of the client site refuses the DELETE METHOD before WordPress ever sees it (WP answers REST failures as JSON - a bare bodyless 405 cannot be WP). FIX at the single transport choke point: PCM_Sites_Service::remote_rest tunnels DELETE as POST + WP-core-native X-HTTP-Method-Override (harmless on servers that allow DELETE; every present and future remote-DELETE consumer fixed at once, zero per-caller patches, zero connector change). PROVEN THROUGH THE REAL PATH post-fix: remote_delete_content trashed disposable post 43 -> {id:43, trashed:true}; bulk delete now works from the table (the toast already counted honestly). VERIFIED: php -l, harness 88/88, live proof, no frontend change - LOCAL ONLY
- KW COMPARE + NUMBER FILTERS [kw-compare-trends] (owner GO 2026-07-14, gap 91bae41): (C1) the SHARED filter mechanism gains a real NUMBER kind - above/below/between as ICONS (no words, owner ruling) + numeric input(s), one shared encoded value (gt:N/lt:N/bt:N:M) + one shared numberMatch predicate (unparseable input matches all - a filter never hides rows by accident; null metrics satisfy no bound); every table in the platform can now filter numbers mathematically; the drawer's clicks/impressions/position switched over (the junk-tolerant contains predicates died). (C2) PERIOD COMPARE: query_stats gains offset_days (previous period = same call shifted back); the endpoint's compare flag fetches both windows on the SAME property and serves rows MERGED per keyword {clicks, impressions, position, prev, d} - vanished keywords stay in with zeroed current + negative deltas (the trend signal), new keywords carry d.position null (dash, never fake zero); failing compare fetch = honest error; the drawer's compare checkbox (previous period implied, no second input) adds three SORTABLE delta columns (green = improving, red = declining, position inverted) - sorting a delta column IS the trend view; the stored layer keeps the merged rows + {days, compare} meta. (C3) COMPLETENESS: live is complete by construction (25k x 4 pages); the stored cap rises 300->1000 (clicks-desc keeps significance). RE-SEEDED as data through the REAL merge law: compare-merged trend rows for the remaining powerleads pages (only 4 - the owner's bulk delete now works and trashed the rest) incl. the new-keyword null-delta case. VERIFIED: php -l x3, harness 88/88, tsc 59, build OK - LOCAL ONLY
- DRAWER UX [kw-trend-cells] (owner GO 2026-07-14, senior-UX round): (1) the stored chip DIED - freshness now lives ON its remedy: live data = the normal silent refresh icon; saved-copy data = the refresh icon turns AMBER with a dot and the tooltip tells the plain-words story (Google could not be reached - showing the last saved results from <date> - click to retry); zero space, the status and its fix are one object. (2) COMPARE TRANSFORMS the columns instead of multiplying them: the three delta columns died; in compare mode the metric columns become TREND CELLS - the change is the loud colored number (green improving / red declining, position inverted), the current value the quiet "now N" second line; headers say so (Clicks -> Delta Clicks); SORTING AND FILTERING follow what is shown (deltas in trend mode, values otherwise - one honest mode switch, never hidden behavior). Five columns in BOTH modes - no sideways scroll in the 360px drawer. VERIFIED: tsc 59, build OK, no PHP - LOCAL ONLY
- KW COLUMNS + CARD TAPER [kw-columns-taper] (owner rulings 2026-07-14, gap 9088128): the trend-cell transform DIED (it hid what you sort by - explicit columns = explicit sorting): the three sortable delta columns are BACK (green/red, position inverted, number-kind filters incl. deltas), amber refresh kept. GEOMETRY, senior-grade: page mode now renders the [drawer][card] PAIR inside ONE centered fixed flex wrapper - the anchor calc DIED entirely (layout derives all geometry, zero math); the card TAPERS by 280px while the drawer is open (plain 300ms width transition, tapers back on close - the owner-described in/out) and the drawer widened to 520px so all 8 columns fit with no sideways scroll; the pair centers as one unit so 980+520 tapered fits a 1366 viewport. Section mode byte-identical (the wrapper is page-mode only; one card JSX tree, conditionally wrapped); outside-click semantics preserved (rootRef/drawerRef guards unchanged). VERIFIED: tsc 59, build OK, no PHP - LOCAL ONLY
- KEYWORD DRAWER V2 [kw-drawer-v2] (owner design 2026-07-14, gap 739de38 + two-zone amend 31e37e5): TWO ZONES. TOP - SELECTED: the page keywords as ONE table (Role tag w/ click-menu P/S/A where assigning Primary demotes the old to Additional - a role change is a MOVE between the three EXISTING stores via the same write paths, no new storage - live Uses + Density per keystroke via the existing edit tick - Ahrefs search VOLUME per keyword - remove); the primary/supporting inputs and pill collection DIED (the table replaced their function); add-keyword input under the table. BOTTOM - THE FINDER: one table, two tabs. RANKING = the GSC list (compare ALWAYS on - the checkbox died, delta columns permanent; page picker, days, related, amber-refresh unchanged). IDEAS = seed input -> the keyword ENGINE (keywords/search autocomplete, consumed read-only) top-20 suggestions with volumes; same + gesture, one destination. VOLUMES: new POST /optimizer/keywords/volumes - cache-first option map (30d TTL, capped 500, most-recent kept), misses batch through keywords ahrefs_enrich; no key -> honest dashes + hasKey false, NOTHING cached on failure (never a poisoned cache). THE SMART BUTTON: the Keywords toggle became a LIVE display right of Page type in the dropdown-family look (DROPDOWN_TRIGGER_STYLE exported from ModelDropdown): key icon + primary + (+N) + density% updating as you type. keywordStats util shared by button + table (phrase-density formula stated). BACKLOG: entity/KG mapping logged (Google KG Search API ids + NL entity salience - belongs to the knowledge-graph feature) + phrase-match GSC discovery. VERIFIED: php -l x2, harness 88/88, tsc 59, build OK - LOCAL ONLY
- DRAWER PORTAL CLICK FIX [drawer-portal-click] (owner live report 2026-07-14, gap af12566): clicking the drawer's Role menu (or any column filter menu) closed the whole editor - Radix DropdownMenuContent PORTALS to document.body (ui/dropdown-menu.tsx:38-48) so those menus sit OUTSIDE rootRef/drawerRef in the DOM and the outside-click flow read them as outside -> save+close. ONE-LINE fix on the toast-exception precedent: pointerdown targets inside [data-radix-popper-content-wrapper] are exempt - covers the role menu, every filter funnel, and any future Radix popper permanently. VERIFIED: tsc 59, build OK - LOCAL ONLY
- SELECTED TABLE PARITY [kw-selected-parity] (owner report 2026-07-14 - never a lesser table): the selected-keywords table was shipped WITHOUT layoutKey = no drag-resize/reorder, no header filters, plain headers. Fixed to FULL parity: layoutKey optimizer-kw-selected + header filters (keyword text-contains; uses/density/volume number-kind with live-value predicates living in the component beside their dependencies) - resize by dragging header edges, reorder by dragging headers, sort by clicking titles, filter via the funnels, exactly like every other table. VERIFIED: tsc 59, build OK - LOCAL ONLY
- KW DRAWER FIXES 2 [kw-drawer-fixes2] (owner tasks 2026-07-14, gap b72840d, both root causes PROVEN live): (Z1 DELETE) the live bucket dump proved adds persisted while deletes never reached the server - the x->persist chain had no .catch and no optimistic update so ANY failure was invisible; the bucket is now OPTIMISTIC BY LAW: add/remove move the UI immediately via a local mirror, the save confirms against the server, failure REVERTS and says so out loud (a silent no-op cannot happen). (Z2 SEARCH) the log proved Google-suggest TIMEOUTS at the exact minute of the owner searches were served as empty successes - google_suggest now returns WP_Error on transport failure, the single-search endpoint surfaces the real message (the drawer SHOWS Google-unreachable instead of zero results), the batch keeps its by-design per-prefix tolerance. (Z3 VOLUMES) the log proved Ahrefs enrichment WORKS here - the ranking table gains a Vol column, scans auto-fill volumes CACHE-ONLY (credit-free by construction: new cachedOnly mode), and ONE header UPDATE button re-fetches all listed keywords with refresh:true (cache bypass; deliberate press, spinner, credits respected). (Z4 PARITY) the ideas table gains layoutKey + filters (kw contains, volume number-kind); shared COL width constants align add/volume columns across all three tables; the volume cell is ONE renderer everywhere. VERIFIED: php -l x4, harness 88/88, tsc 59, build OK 23.5s - LOCAL ONLY
- PREVIEW + OPEN FIXES [preview-open] (owner order 2026-07-14, gap 94cb9e4, both proven): (V1) the inline preview popup painted BEHIND the page editor - it was NOT portaled while the editor is, so an app-tree stacking context capped its z-50 under the body-level editor; the preview now PORTALS to document.body and wins globally (preview above the modal, as expected). (V2) Open on the Privacy Policy showed ?page_id=3 - proven SITE DATA: the page is a DRAFT and WordPress hands drafts exactly that link (published pages open pretty slugs, verified); the Open anchor now appends preview=true for non-published rows so the REAL page renders as a logged-in preview (tooltip says so); page.permalink itself stays the true permalink - the GSC drawer keeps filtering the real URL. VERIFIED: tsc 59, build OK, no PHP - LOCAL ONLY
- KW VOLUMES MANUAL TRIGGER [kw-volumes-trigger] (owner order 2026-07-14, gap 6936f69 + addendum bcaecca): the Ideas tab LIVE-FETCHED volumes on every search (the credit leak, KeywordsDrawer:339) - now CACHE-ONLY like Ranking; NEW small GET button in the finder tab row fetches the ACTIVE tab's volumes on a deliberate press, cache-first (credits only for keywords the hub lacks or whose 30-day cache expired) - the header update button KEEPS full scope as THE fresh-pull (volumes drift, owner ruling reversed D3); the selected table keeps its frugal auto-fetch of empties. DEFECT KILLED: cachedOnly misses were returned as null and POISONED the client dedupe (scan-then-add never live-fetched) - misses are now OMITTED from the response (dash renders identically, state stays clean). SOLIDITY: cache confirmed hub-GLOBAL by construction (keyword-keyed, no siteId - one pool for every site/page/version), cap raised 500->2000 (one constant; kw_volumes table named as the deferred fleet-scale path). VERIFIED: php -l, harness 88/88, tsc 59, build OK 27.7s - LOCAL ONLY
- THE RESEARCH SPINE [research-spine] (owner GO 2026-07-14/15, gap f9adfc7, hard-lanes ruling 8c34788): EVERY teacher now receives THE CONTEXT PACKAGE - keywords {primary/supporting/additional} from the editor's LIVE state (bucket fallback server-side), the FULL business record (site->brand->GBP resolved, server-trusted), pageType, site pages - built ONCE in the controller, formatted by ONE context_block(); the COMPILER merges hierarchy- and business-aware (D6). TAXONOMY: teachers declare group() 'search'|'ai'; the rail renders TWO headed groups (owner ruling). THE PEEK: every analyze returns contextUsed - the rail's info icon shows exactly what the run was given (owner confirmation tool, templates deferred); items carry source (ahrefs / gsc:stored(date) / provider:model) rendered "via X". FIVE NEW RESEARCHERS (one file each, glob registry untouched): onpage (DETERMINISTIC word count/density/stuffing/placement vs seeded tunables, zero LLM), serp (Ahrefs serp-overview top-N winners vs our content, honest no-key/no-primary errors), demand (striking-distance from stored GSC rows, honest stored-label + no-data error), interlinks (in-context FROM-links, anchors verified VERBATIM server-side, one-per-target, GSC page-map relevance when key exists else labeled 'page titles'), answerability + mention + facts (AI group: quotability checks; MULTI-ENGINE panel - one text model per keyed provider, SEQUENTIAL per the 2-worker law, google grounded, deterministic mentions_brand verdict, per-engine failure isolated; entity/fact authority judged against the REAL business record). Subtopics ANCHORS on the primary keyword (invention killed); search teacher context-aware. Tunables = seeded pcm_optimizer_research option (hub data law). Rewrite append law gains hours+description (+business.description var both builders). Informational items (no directive) render but never tick into the basket. VERIFIED: php -l 13 files, NEW optimizer_research_test.php 34/34, harness 88/88, tsc 59 zero new, build OK 37.5s - LOCAL ONLY
- KW INDEPENDENT FETCH + ROLE SORT [kw-independent-fetch] (owner orders 2026-07-15, gap 3f9942d): TABLES FETCH INDEPENDENTLY - the header volume button now updates the SELECTED keywords ONLY (the update-everything ruling superseded, honest deviation; disabled when empty), the finder button keeps its active-tab scope; each button gets its OWN spinner via pendingSource (the shared isPending made every press animate both - the "double fetch" look, F2); auto-fills animate neither. HONEST DASH: probed live - the owner's selected keywords WERE fetched and Ahrefs has no data for them; the cached-null dash now says so ("Ahrefs has no volume data for this keyword") instead of press-the-button (which stays for never-fetched). ROLE SORT: role column gains sortAccessor (P0/S1/A2) + the selected table default-sorts role asc = P -> S -> A on open. ONE file (KeywordsDrawer.tsx), other dev's in-flight set untouched. VERIFIED: tsc 59 zero new, build OK 32.8s, harness 88/88, no PHP - LOCAL ONLY
- RAIL VISUAL HIERARCHY [rail-hierarchy] (owner screenshot order 2026-07-15, gap 042e0a5): the Analyze rail was a text wall - every found row printed its FULL multi-line instruction + evidence. NOW three levels: group micro-label -> purpose sections as WHITE CARDS on the slate rail (border+shadow, the background the owner asked for) -> each suggestion ONE scannable line (checkbox + the SHORT label + chevron). The full story lives in the row's disclosure: evidence (grey) + THE FIX that rides the basket (primary-tinted block) + the data source tag. Passed checks collapse to one quiet "N passed" summary row (list a tap away). Informational items (no directive) drop the dead checkbox for an amber dot. NOTHING removed - everything one tap deep (owner law). POSITIVES law moves to the disclosure level per this ruling. VERIFIED: tsc 59 zero new, build OK 22.3s - LOCAL ONLY
- AHREFS CASE-LOOKUP FIX [ahrefs-case-fix] (owner report 2026-07-15, gap 71d3cde): "Privacy" showed no volume while lowercase keywords worked - PROVEN by the live cache dump (every capitalized entry null, every lowercase real): Ahrefs echoes keywords LOWERCASED and ahrefs_enrich keyed its result by the echo, so the optimizer callback's original-casing lookup missed and cached a false 30-day no-data null. FIX at the source: ahrefs_enrich re-keys every echoed item to the CALLER'S input casing (one lowercase map, same transform both sides - non-ASCII consistent by construction); contract preserved for every consumer, optimizer callback untouched (his in-flight file). Poisoned nulls self-heal: one press of the selected update button (refresh bypass) re-buys them. VERIFIED: php -l, harness 88/88, tsc 59 zero new, build OK 22.5s - LOCAL ONLY
- THE SMART COUNTRY CHAIN [kw-country-chain] (owner GO 2026-07-15, gap dda4545 + addendum 2cf0a44 P1): keyword volumes STOP defaulting to the US market. resolve_country per site, cached with source: (1) the brand's GBP address (country-name data map) -> (2) ONE quick invoke_json language/market check on the site's own content sample -> (3) the hub-editable default option pcm_kw_default_country seeded 'se' (Sweden, owner ruling); a default reached WITHOUT content is not pinned so a later content-carrying call can improve it. The volume cache is now MARKET-KEYED ("country|kw" - one market never serves another) and the old market-blind keys are DELETED on first write (they were US numbers - wrong data, not migrated). ahrefs_enrich/ahrefs_serp_dr lose their silent 'us' signature defaults - every caller passes explicitly (verified all four). The volumes endpoint takes siteId+contentSample and answers with the resolved country; the drawer sends both. VERIFIED: php -l x4, harness 88/88, research suite 34/34, tsc 59 zero new, build OK 25.7s - LOCAL ONLY
- THE BACKBONE + THE ACTION MATRIX [kw-backbone-matrix] (owner GO 2026-07-15, gap 2cf0a44 P2+P3): the DRAWER IS THE SELECTION BACKBONE - the selected table gains the shared DataTable checkbox selection (header select-all included), ticked count shows in the drawer header, and ONE upward contract (onKeywordSelection, [] = none/closed) carries the selection to the editor. THE ACTION MATRIX on the split button: ticked keywords transform it to "Insert keywords (N)" - one run weaves EXACTLY the ticked keywords per THE HIERARCHY LAW (primary threads headings+body, supporting some headers+text, additional one paragraph max), a text selection narrows either run, nothing selected = everything; the injection run suppresses the generic ride (one keyword order per prompt, never two) and carries a keywords-purpose directive so the review states its order. THE RIDE GROWS TO ALL ROLES: supporting keywords now join every ordinary run (they never did - the old two-store line REPLACED). VERIFIED: tsc 59 zero new, build OK 40.2s, harness 88/88, no PHP - LOCAL ONLY
- THE VALUE BATCH [value-batch] (owner GO 2026-07-15, gap e8fcae5, ALL live-verified): (D1) rail = SEO/AI group names + PLAIN category labels (Structure & language / Keyword placement / Topic coverage / Competitor gaps (SERP) / Untapped searches (GSC) / Internal links / Direct answers / AI recommendations / Business facts), cards 4px + hairline shadow, rows 10px grey clamp-2. (D2) review pills collapse to the TWO purposes (SEO blue / AI violet) + a grey ITALIC plain-category line per section (the WHY next to the WHAT; click-to-scroll already existed). (D3) REVISE FIDELITY: note+draft travel separately; the human-editor contract rides the prompt; sentence-retention check vs the draft (tunable revise.minRetention 0.6) -> dynamic broad-rewrite judgment (LLM, never keyword-hardcoded) -> ONE retry -> honest error; the 80%-text-loss defect closed. (D4) SERP researcher fetches the top winners' REAL page content (scraper, tunable fetchTop/chars/timeout, per-winner degradation marked) + market from the live country chain; labels lose the repeated prefix (demand rows too - keyword leads w/ rank+impressions). (D5) RESULTS LOOP MVP: finishReview with accepted sections stamps POST /optimizer/history (purposes + stored-GSC snapshot); GET serves then-vs-now clicks/avg-position; the rail tops with the honest result card ("stored GSC" label; no-snapshot stated). (D6) research_tunables MERGE fix - stored edits win, NEW keys reach seeded installs. VERIFIED: php -l x6, research 34/34, harness 88/88, tsc 59 zero new, build OK 32.6s - LOCAL ONLY
- HEADER STATUS DROPDOWN [header-status] (owner order 2026-07-15, quick fact analysis in-chat: verdict EASY, pure reuse): the page editor header gains the SAME status control as the table's status cell - Select trigger rendering the global Pill (statusPillVariant MOVED to SEO/types.ts, one source for both consumers - the index.tsx copy deleted same commit), saved through the SAME seo.remoteSaveCell path, sits right of the title. OPTIMISTIC BY LAW: status flips instantly, failure reverts + toasts; publishing toasts "saved changes now render on the live page" - the draft/preview confusion (gap e16bd93) gets its one-click exit exactly where it happened. Hidden read-only or when the page carries no status. VERIFIED: tsc 59 zero new, build OK - LOCAL ONLY
- PAGE VERSIONING W0-W4 [page-versioning] (owner GO 2026-07-16, gap dbdc786, THREE sub-agent lanes + orchestrator verify): W0 the editor OPENS FROM THE SAVED VERSION (versions[0], anchors stripped; assembly = fallback only when no versions exist; never-clobber law kept) - the "Current version suddenly changes on pick" bug dies; W4 quiet drift line + "Load live view" (honest draft semantics); W1 pcm_page_state record {version,fingerprint,savedAt} bumped with every successful save; W2 save REPLACES the page's net rule set (pile cannot regrow; per-section versions + rollback law unchanged); W3 connector stores + echoes pageState, patch bump (sites reinstall). VERIFIED by orchestrator: php -l x2, harness 88/88, NEW page_versioning_test 26/26, research 34/34, tsc 59 zero new + build green (lane C post-edit). Lanes A/B terminated during their final report (session limit) - work complete, verified here - LOCAL ONLY
- BULK VERSION DELETE [bulk-versions] (owner order 2026-07-16, in-chat fact check: EASY, one file): the versions dropdown gains a cleanup bar - per-row checkboxes (history rows only), select-all, ONE Delete (N) button running the EXISTING delete mutation sequentially (bulk-status precedent) w/ one refetch + one honest summary toast (failures counted, never hidden). THE GUARD: the Current version (idx 0) carries NO checkbox and never joins select-all - deleting what the editor opens from (W0) would recreate the old-version trap; its deliberate single-row delete stays. No confirmation dialogs (standing law). VERIFIED: tsc 59 zero new, build OK 22.7s - LOCAL ONLY
- INSTANT OPEN + SYNC GLYPH [instant-open] (owner GO 2026-07-16, gap e48b1ff, the Ive/Woz ruling): the editor OPEN no longer waits for the client site to render its page - with saved versions the doc loads INSTANTLY from versions[0]; the heavy inventory fetch turns LAZY (fetched only on the no-versions first-open, or on the deliberate "load live view"). NEW featherweight chain: connector GET /pcm-conn/v1/page-state (3.0.6->3.0.7, version+fingerprint only, zero render; version 0 = the true never-pushed baseline) -> hub GET .../page-state (page_state_compare: local record vs remote answer, drifted honest tri-state, unanswered reported as unanswered; timeout = hub data pcm_seo_state_check seed 5s; brandId+pageType ride along so the fast path needs no inventory for header context) -> ONE header glyph with four TRUE states: checking spins - in-sync renders NOTHING (silence = success) - drift amber (click = load live view, version-pick semantics) - unreachable muted red (click = retry, error named in the title). Lane-C's drift LINE superseded and DELETED (one UI for one truth). VERIFIED: php -l x3, harness 88/88, page_versioning 26/26, research 34/34, tsc 59 zero new, build OK - LOCAL ONLY
- SEO WHITE SHELL [seo-white-shell] (owner order 2026-07-16, gap 82c1c05): the SEO module reads as ONE white canvas - the grey was the SHELL's (Shell.tsx:79), not the module's; the Deliveries white opt-in precedent generalized to a whiteShellModules list (the existing fullBleedModules idiom - the ternary special-case folded in, no second pattern), seo added. Every other module byte-identical; ZERO touches to the SEO module files (the other dev's active surface - conflict-free by construction). VERIFIED: tsc 59 zero new, build OK 25.6s, harness 88/88 - LOCAL ONLY
- GLYPH BLIND SPOT [instant-open fix] (owner live check 2026-07-16): a failed HUB state call rendered NO glyph at all (silent nothing - the exact thing the four-true-states rule forbids); stateQuery.isError now joins the unreachable state (red, retry). Route registration verified LIVE (pcm/v1 lists page-state). VERIFIED: tsc 59, build OK - LOCAL ONLY
- ONE CANVAS ZEN PASS [one-canvas-zen] (owner order 2026-07-16, gap 10dd009): THE DOUBLE BACKGROUND DIED at the root - .module-container painted var(--main-bg) near-grey OVER the shell's per-module color (the white shell showed as a ring around a grey interior); the container background is GONE, the Shell is the one canvas owner, and the orphaned --main-bg var deleted with it (sole consumer). Grey-shell modules: visual no-op (shell grey = the old var); Deliveries: same hidden double-paint cured free. SEO surfaces per the design rulings: the section-nav card chrome died (items float, the blue pill IS the state), the table and the View control lost their at-rest shadows (shadow = floats law - the bulk bar and modals rightly keep theirs), the table keeps its hairline border as THE one data object. 4 touches, 3 files. VERIFIED: tsc 59 zero new, build OK 24.5s, harness 88/88 - LOCAL ONLY
- SITE HEALTH DOTS + HONEST CONNECTOR ERRORS [health-dots] (owner GO 2026-07-16, gap 89ef71a): the SEO tab bar's 14px globes are now the SHARED ~6px SiteStatusDot (new component, bg-current inherits the tab color - active stays blue; RED only when the connector test FAILED w/ the real error in the title; NO green state by owner ruling; local tab never tests). Behind it: NEW batch GET /sites/health in the SITES module (boundary law) - REAL connection tests (the existing test_connection, now w/ an additive timeout param) run SEQUENTIALLY server-side (2-worker law) under a seeded pcm_sites_health_check timeout (5s, hub data); useSiteHealth hook = ONE query, 60s stale. HONESTY FIXES in page_state_compare: remote_rest returns {status,body} for EVERY HTTP status - only 200 now counts as an answer; 404 = "connector outdated (needs 3.0.7+) - update from Sites module" (proven probe: powerleads answered 404 on /page-state while alive - the glyph called a reachable site unreachable); other statuses named by number. VERIFIED: php -l x3, harness 88/88, versioning 26/26, tsc 59 zero new, build OK 24.1s - LOCAL ONLY
- SITES ACTIONS KEBAB [sites-kebab] (owner GO 2026-07-16, gap 618dd16): the Sites table's four inline action buttons (Test/GSC/Auto/Delete) collapse into ONE three-dots menu on the SHARED dropdown-menu component (MoreHorizontal trigger, the app's established kebab icon) - same handlers, same disabled states, the in-flight test/GSC spinner rides the TRIGGER so it stays visible with the menu closed, full-word labels (Test connection / Verify in GSC / Auto-content schedule / Delete site destructive after a separator), title hints moved onto the items, column 10%->6%. Zero logic changes. VERIFIED: tsc 59 zero new, build OK 43.8s - LOCAL ONLY
- TITLE GESTURE [title-click] (owner order + honest catch 2026-07-16, gap cad42df - was gapped but NEVER built until the owner caught it): the SEO table's TITLE cell only - single click OPENS the page editor (connected rows -> the dynamic editor; local rows -> their WP editor in a new tab, same gesture different destination per the uniformity ruling); DOUBLE click enters the inline title edit. Implemented as the OPT-IN onOpen prop on the shared EditableCell w/ an internal 220ms click/dblclick discriminator + unmount cleanup - every other column byte-identical (no onOpen = today's click-to-edit). Tooltip names both actions. VERIFIED: tsc 59 zero new (after fixing my own 4 - destructuring miss), build OK - LOCAL ONLY
- FLEET HEALTH P1-P3 [fleet-health] (owner GO 2026-07-16, gap fad81ea, researched MainWP-pattern + heartbeat edge): P1 HEARTBEATS - remote_rest (the single choke point) records per-site health on EVERY real interaction free of charge (transport failure or 5xx = unhealthy; any HTTP answer = alive); ONE map option pcm_site_health, autoload off. P2 STORED READS - /sites/health now answers from the map in milliseconds at any fleet size w/ per-site age ("checked N min ago" rides the dot tooltip); THE DEBT DELETED: this morning's sequential in-request test loop (the proven SEO-mount blocker) removed in the same commit, replacement live, sole consumer (useSiteHealth) contract kept additive (ok may be null = never measured, dot stays quiet). P3 PROBE QUEUE - cron pcm_sites_health_probe (interval/staleness/batch/timeout all hub data, seeds 300/900/3/5s) probes ONLY stale-heartbeat sites oldest-first, bounded per run. SEO mount no longer performs ANY remote work for health. P4 (SWR content cache) = next pair, gap section ready. VERIFIED: php -l x3, harness 88/88, tsc 59 zero new, build OK 36s - LOCAL ONLY
- FLEET HEALTH P4 - SWR CONTENT TABLES [fleet-health-p4] (owner GO 2026-07-16, gap fad81ea): the connected-site content table now renders INSTANTLY from the hub's stored copy (?cached=1 answers in milliseconds from option pcm_remote_content_cache, capped 500 rows, autoload off) while the live remote fetch runs in parallel and SWAPS IN when it lands - the live truth always wins, the stored copy only bridges the wait; every successful live fetch refreshes the store. Cache MISS answers {miss:true}, never an empty list (an absent copy must not render as "no content") - the normal loading state shows. Frontend: second cached query in useRemoteSeoContent, rows prefer live>cached, isLoading only when NEITHER exists; the optimistic saveCell patch covers both copies (shared query-key prefix, verified). Frozen bare-array reply contract kept (honest deviation from the gap's source-field idea, named). VERIFIED: php -l, harness 88/88, tsc 59 zero new, build OK 41.6s - LOCAL ONLY
- OPEN PHASES + HONEST LOADING [open-phases] (owner critical 2026-07-17, gap 02d3cb7, root cause MINE and named): the instant-open gated EVERY editor open on the page-versions query - which fetched a REMOTE snapshot inline to build originalHtml the open never needed; that phase had zero UI = the owner's 30-second WHITE editor. FIX: (D1) page-versions gains ?rowsOnly=1 - the open path is now a pure DB read in milliseconds; the Original loads LAZILY only when the versions dropdown opens (its sole consumer; duplicate versionsOpen declaration removed - one owner). (D2) THE STAGED OVERLAY: every pre-content phase says what it is doing - "Loading your saved version..." then "Pulling the page from your site - the first open takes longer while the local copy is built..."; keyed on a reactive docLoaded state (a ref cannot re-render - the lesson). A blank editor can no longer be silent. (D3) the remote table's first load (no stored copy yet) says "Fetching content from the site - the first load builds the local copy, next opens are instant...". Seniority sweep: no other dead/old code found in the arc's files. VERIFIED: php -l x2, harness 88/88, tsc 59 zero new, build OK 48.2s - LOCAL ONLY
- SELF-AUTHORIZED LOOPBACK [loopback-auth] (owner order 2026-07-17 full ownership, gap f285ced): THE SHORTCUT KILLED - the connector's content pull was an ANONYMOUS fetch of the public page URL (seohub loopback), so drafts/private/future posts (no public render) pulled EMPTY, always - the login-correlated white pulls on the draft Privacy Policy. NOW: every loopback fetch mints a one-time 32-char token (60s transient, pid-bound, SINGLE USE - replay changes nothing) and fetches the query-var permalink; the connector's pre_get_posts validator widens post_status for that one request only. ONE code path for every post status; grants one page render and nothing else; no session, no cookie, no app-password-on-frontend anywhere in the chain. Connector 3.0.7 -> 3.0.8 (sites reinstall/update). Hub untouched (its empty-snapshot honesty paths stand). VERIFIED: php -l, harness 88/88 - LOCAL ONLY
- IDENTITY-CORNER STATUS [identity-status] (owner/Jony order 2026-07-17, gap d4aa30b w/ Definition of Done): the editor header's decorative blue FileText square is GONE - the same 7x7 spot is now the page's ONE connection status: checking = spinning sync on slate - LIVE/in-sync = calm GREEN dot on green ("Live - the site serves your saved version") - drifted = amber sync (click loads live view) - unreachable = red CLOUD (click retries, error in the title) - no verdict (read-only/no check) = quiet neutral dot, never a fake state. The duplicate glyph beside the status pill DELETED same commit (one indicator, one home); FileText import dropped (orphan hygiene); all state logic untouched - presentation re-homed. VERIFIED: tsc 59 zero new, build OK - LOCAL ONLY
- VERSIONED CONNECTOR FILENAMES [conn-filename] (owner order 2026-07-17): all three connector downloads now carry the version in the filename (pcm-connector-3.0.8.zip / pcm-connector-{clientId}-3.0.8.zip) - you always know what you downloaded. ONE new PCM_SEOHub_Service::connector_template_version() parser REPLACES the two duplicated Version-regex spots (dedup, single source). VERIFIED: php -l x2, harness 88/88 - LOCAL ONLY
- CONNECTOR FILENAME - THE REAL CONSUMER [conn-filename fix] (owner-caught 2026-07-17): the Download-connector button fetches the zip client-side and HARDCODED a.download='pcm-connector.zip' (Sites/index.tsx:274) - it silently overrode the server's versioned Content-Disposition; my stamp never traced the actual consumer (accountability noted). NOW the frontend reads the SERVER's filename header (one source of truth; explicit fallback name only if the header is absent). App-wide sweep: the two other a.download spots are client-GENERATED files (Meta export zip, SEO config JSON) - legitimately client-named, untouched. VERIFIED: tsc 59 zero new, build OK - LOCAL ONLY
- RED CLOUD ROOT CAUSE + STATE-CHECK TIMEOUT [state-check-timeout] (owner critical 2026-07-17, gap GAP-ANALYSIS-STATE-CHECK-TIMEOUT-POWERLEADS-SLOW-20260717): probes proved the 3.0.8 update WORKED (installed file on disk) and the authed page-state call ANSWERS - in ~12s, against the hub's 5s timeoutS -> guaranteed red cloud; every powerleads PHP request costs ~10s on this machine (static files 0.3s; 2 static fpm workers both sites; opcache on) - environment, R4's poster child. FIX = DATA not code: hub option pcm_seo_state_check.timeoutS 5 -> 15 (measured 12.5s max + margin; seed default in code untouched - correct for healthy installs; worker-hold trade-off named in the gap). Also proven: pcm_conn_page_state_3 never stored (pre-3.0.6 push) -> honest AMBER until one re-save; versions/rules/site all userId 1 = owner's own user, scoping ruled out of the white; white-open mechanism = rowsOnly starved behind the 2-worker queue (inferred, owner Network tab confirms). ZERO code changed - no build needed. VERIFIED: option before/after read back from hub DB; no object-cache drop-in (value live immediately) - LOCAL ONLY
- STATE-CHECK CRASH - THE TRUE RED CLOUD [state-check-500] (owner critical 2026-07-17, gap addendum in GAP-ANALYSIS-STATE-CHECK-TIMEOUT-POWERLEADS-SLOW-20260717): the featherweight page-state check has CRASHED ON EVERY RUN since it shipped - service.php:4052 passed array() as the GET body, remote_rest wp_json_encode()s any non-null body to the STRING '[]', WP's cURL transport http_build_query()s GET data -> TypeError 500 before the request ever left the hub; the SPA read the 500 as stateQuery.isError -> red cloud 'unreachable'. Proven in WEB context (temporary admin-ajax mu-plugin probe, deleted after use) + debug.log carries the same fatal on the owner's own 08:41 open. FIX: array() -> null (one token; sweep proved 4052 was the codebase's ONLY array()-body GET) + tunable pcm_seo_state_check.timeoutS -> 30 in hub DATA (powerleads measured 12-15.7s per answer today; R4 is the real cure). PROOF AFTER FIX: the hub's real page_state_compare(2,3) in web context returned local v24 / remote v0 / drifted true / error null in 13s - the red cloud is dead, honest AMBER until the owner re-saves once. VERIFIED: php -l, harness 88/88 + 26/26 + 34/34, probe before/after - LOCAL ONLY
- CONTENT FIRST, QUESTION AFTER [open-ordering E1] (owner GO 2026-07-17, gap GAP-ANALYSIS-EDITOR-OPEN-PIPELINE-20260717 w/ checklist): the corner's version check no longer competes with the editor open - stateQuery relocated BELOW the doc-load effect (declaration order = the truth; docLoaded in scope) and gated on docLoaded, so the one hub request that pins a PHP worker for the site's whole round-trip (12-30s measured) fires only AFTER the document painted; the false 'never blocks anything' comment rewritten to the worker-pool truth, edge named (a page that never loads keeps a neutral glyph, its error line speaks). Zero new machinery, zero logic changes to glyph states/drift/consumers (sweep pinned in the gap). Stored-first verdict (C2, fleet-health pattern) remains the priced end-state. VERIFIED: tsc 59 zero new, build OK 46.6s, served bundle byte-identical to the build (5,081,340) - LOCAL ONLY
- MODULE VERSION STAMP [module-version-stamp] (owner order 2026-07-17, gap GAP-ANALYSIS-MODULE-VERSION-STAMP-20260717): every module now signs itself - quiet light-grey '{module} v{version}' bottom-left of the active module (ONE Shell mount covers all 21 module ids incl. the text->copy alias; absolute in the relative <main>, 10px slate-400/60, select-none); hover shows the BUILD stamp baked at build time (__PCM_BUILD__ vite define + vite-env declaration) - the machine truth that makes a stale SPA tab visible at a glance, the blindness that burned this whole day. ONE registry app/src/lib/module-versions.ts seeds all modules 1.0.0; RITUAL LAW ADDED: any pair touching a module's frontend bumps that module's version in the same commit. PCM_VERSION untouched (owner-ask law). VERIFIED: tsc 59 zero new, build OK 1m2s, served bundle byte-identical (5,082,295), stamp strings present in served JS - LOCAL ONLY
- THE REAL WHITE - SCREEN GATES COMPLETE THE INSTANT-OPEN [E1b] (owner critical 2026-07-17, gap EDITOR-OPEN-PIPELINE addendum, full consumer trace): the instant-open redesign (e48b1ff/W0/02d3cb7, all UNVERIFIED-IN-BROWSER) migrated the open's DATA source to the saved version but left the ENTIRE working layer keyed to pageReady - a flag only the intentionally-skipped inventory fetch can set: EditorContent never MOUNTED (the white), save guard returned early, selection-AI/drawer/toolbar/analyze rail/3 action buttons dead, Draft state dead - on EVERY saved-version open since the redesign shipped. FIX completes the redesign: NINE gates rewired to docLoaded (true on BOTH load paths); pageReady remains at exactly FOUR inventory-only sites (definition/pageError/fallback branch/live-view swap, grep-pinned); docLoaded+pageLoadedRef declarations moved above the editor (TDZ, one declaration). seo module 1.0.1 (first stamp-ritual bump - the fix is VISIBLE bottom-left). VERIFIED: tsc 59 zero new, build OK 49.6s, served bundle byte-identical (5,082,371); owner browser check = the last box - LOCAL ONLY
- THE ATOMIC SAVE + THE CORNER'S TRUTH [atomic-save] (owner GO 2026-07-17, gap GAP-ANALYSIS-ATOMIC-SAVE-20260717 w/ checklist + built-proof section F): one page save = ONE transaction = ONE push = ONE version. TWO chokepoint deferrals (push_current_rules_or_rollback stubs, record_version queues - every routed path flows through them, grep-proven), transaction opens at the pre-mutation snapshot, $fail = TOTAL ABORT (restore + clear + 'Nothing was saved - the site still serves its previous state'), commit = single real push + queued history flush; the old O(N)-pushes-per-save and the half-saved-live-site failure mode are DEAD. Reply gains pushed + pageState (the connector-echoed record). CORNER TRUTH: saving = its own spinner state ('Saving - pushing to your site...'); a pushed save seeds the verdict GREEN from the push's own acceptance (no 15-30s re-ask); a no-op save touches nothing; a failed save keeps the previous verdict + named error. Harness section 4 added (26->30: deferred push stubs w/o state write, record_version queues, clean reset). seo module 1.0.2. VERIFIED: php -l x2, 88/88 + 30/30 + 34/34, tsc 59 zero new, build 26.2s, served byte-identical (5,083,452), LIVE web-context save = 1 push / version +1 exactly / no dup history / 17s (probe deleted, 400-checked). Named deviation: explicit flag-clear on all return-scanned exits instead of try/finally. Open observation logged in gap F: served baseline still exposes a 'privacy policy' H1 section saved docs omit (pre-existing) - LOCAL ONLY
- BUSINESS SPINE P1 - UNITS + MIGRATION + FACADE [business-spine-p1] (owner GO 2026-07-17, gap GAP-ANALYSIS-BUSINESS-SPINE-P1-20260717, architecture docs/ARCHITECTURE-BUSINESS-SPINE-20260717.md): the malleable backbone lands. NEW wp_pcm_brand_business_units (a brand's 0..n locations, exactly one PRIMARY; two-layer storage: fetched JSON + manual JSON + per-key sources - manual always wins and survives every refresh) + sites.businessUnitId (null = brand's primary; sites CONSUME brands, never the reverse - owner dependency ruling) + DB 1.43.0 w/ idempotent migrate_gbp_to_business_units (moves pcm_seo_gbp_* options into primary units, deletes options after copy; LIVE: zero options existed - honest no-op, proven). Brands service gains the static unit API (list/get_business_record/save/delete w/ primary handoff). PCM_SEO_GBP becomes a pure FACADE over the primary unit - exact old shapes kept, all five consumers (keywords lane included) untouched. VERIFIED: php -l x5, 88/88 + 30/30 + 34/34, live migration probe (table + column + version 1.43.0), web-context facade round-trip on brand 13 (override -> snapshot -> overrides SURVIVED -> resolved merge correct -> unit primary w/ gbp source tags -> cleanup to pristine), probe deleted 400-checked - LOCAL ONLY
- THE CHANGE-CARD REVIEW T1 [change-cards] (owner critical 2026-07-17, gap GAP-ANALYSIS-CHANGE-CARD-REVIEW-T1-20260717 all boxes): the review now shows DECISIONS, not diffs. The rewriter's reply carries a per-section CHANGES LIST via an append-envelope JSON contract (server-side like the revise contract - no template migration); parse_section_reply VERIFIES every claim (quote must exist verbatim-normalized in the produced text, why must be one of the run's purpose ids, capped by tunable) - the model's confession is checked, never believed; parse failure = raw reply + no cards = byte-identical legacy behavior (THE FLOOR). Server judges REWRITTEN (sentence_retention vs original under tunable review.rewriteRetention) -> the doc renders ONE calm struck group + ONE green group (diffBlocksHtml consolidated mode, same marks/strip/oracle) instead of word confetti. Rail cards show the section's OWN changes (what + plain-name why; hover lights the EXACT words via char-accurate normalized search + inline decoration, honest section-flash fallback; 'Section rewritten' chip) - the repeated-global-directives bug is dead; sections without verified changes keep the exact old display. FOUND PRE-EXISTING DEFECT FIXED: the trpc transform dropped 'draft' - revise fidelity retention was dead on the wire since it shipped; one body key restores it. Tunables review{maxChanges:12, rewriteRetention:0.35} merge-seeded. Acceptance semantics + everything before the review UNTOUCHED. seo 1.0.3. VERIFIED: lint x4, 88/88 + 40/40 (10 new contract tests) + 34/34, tsc 59 zero new (2 mine found+fixed), build 26.1s, served byte-identical (5,088,855); live browser run = the owner's next Optimize (expected: per-section cards w/ why, hover highlight, calm rewrite blocks, retention now active) - LOCAL ONLY
- THE CHANGE-CARD REVIEW T2 - PER-CHANGE CONTROL [change-cards-t2] (gap GAP-ANALYSIS-CHANGE-CARD-REVIEW-T2-20260717, frontend-only): every change card row gains a TICK (default all); a partial selection shows 'Update proposal (N of M)' which rebuilds the section through ONE bounded re-run of the EXISTING revise path with a generated remove-these-changes note - the fidelity contract + retention check (live since T1's draft fix) guard everything else verbatim; the fresh proposal lands for review with fresh verified cards. NAMED DEVIATION from the blueprint sketch: deterministic splice-out REJECTED as false determinism (no old<->new mapping exists) - correct over clever. All-ticked Accept / Reject / Revise / Accept-all / OK byte-identical to T1; acceptance stays the ONE per-section gate. All changes now visible when cards render (per-change controls forbid hiding); instructional-hint line removed (chrome law); T1 lingering quote-highlight cleared on resolve. seo 1.0.4. VERIFIED: tsc 59 zero new, build 33.1s, served byte-identical (5,091,028), harnesses 88/88 + 40/40 + 34/34 (PHP untouched, ritual run) - LOCAL ONLY
- THE BUSINESS CARD - SPINE P2+P3 [business-card] (owner GO bundle, gap GAP-ANALYSIS-BUSINESS-SPINE-P2P3-20260717, arch docs/ARCHITECTURE-BUSINESS-SPINE-20260717.md): the ownership law lands - brands own truth, SITES OWN THE CONNECTION (PATCH businessUnitId; exact-host auto-map on site create + v1.44.0 idempotent backfill - deterministic fact, never a guess), SEO consumes. ONE pure LADDER (merge_business_ladder, harness-tested: site override > unit manual > unit fetched w/ per-key sources > brand basics > site basics; empty never lands) under business_record_for_site(); remote_field_vars + optimizer business_context DELEGATE to it - unit pinning + per-site SEO overrides now reach EVERY generation (live proof: powerleads generations now carry address/language/description that were empty before). Site layer = pcm_seo_biz_site_{id} (open sanitized keys, caps, empty clears). mergeFetched multi-source door on units (per-key tags). parse_maps_url pure fn (cid incl. exact 64-bit hex->dec string math, lat/lng, embed URL; named errors). Four SEO routes (GET card w/ units + exact-domain suggestion / overrides / refresh-scrape / maps). FRONTEND: RemoteBusinessCard (FIELD_REGISTRY drives rows - add a field = one entry; per-field source hover; inline edit = this-site layer; map/link/create-from-site via existing brand routes; unit picker; Maps paste) replaces the connected-site placeholder; local BusinessPanel stays until P4 (named scope). seo 1.0.5. VERIFIED: lint x7, 88/88 + 52/52 (12 new ladder/maps tests) + 34/34, tsc 59 zero new, build 50.8s, served byte-identical (5,105,135), LIVE web-context probe (DB 1.44.0, resolved record w/ sources, override round-trip set/clear), probes deleted - LOCAL ONLY
- GOOGLE NATIVE PHASE A + THE TWO-ROOM CARD [google-native-a] (owner orders, gap GAP-ANALYSIS-GOOGLE-NATIVE-A-20260717 + REFERENCE-GBP-REVIEWS-LOCATIONS-20260717): THE N8N MIDDLEMAN IS DEAD - its provider class + BusinessPanel webhook UI deleted, replaced in the same pair by the native PCM_SEO_GBP_Google_Provider (Places New v1 searchText/details, THE WIDE MASK: addressComponents->postal/city/region/country, intl phone, googleMapsUri->CID, short address, hours, rating/count, category, top-5 public reviews w/ text). Key = the NEW google_places Integrations provider (regular flow, per user; 'google' slug was taken by Gemini). normalize() widened additively + empty-fields-never-land filter law (6 new harness tests, suite 58/58). SHARE-URL RESOLVER (manual redirect follow -> ChIJ id -> details; name-segment -> ONE searchText fallback; named errors) - one pasted Maps link now fills the WHOLE unit tagged gbp (no key = local cid/geo parse stands alone, reason NAMED in the reply, never a silent half-result). THE TWO ROOMS: card splits Business (identity, white) / Local SEO (whisper amber tint, Maps-paste row inside); indexedurl field added (Website != Indexed URL, the cheat-sheet law); ASK-FIRST refresh popover - the confirmed URL is what gets scraped AND becomes the site's Indexed URL (business_refresh accepts url). Owner's missing-locations mystery ANSWERED factually in the reference doc (no pagination, placeId-less listings filtered, single hardcoded account) - Phase B fixes, reviews machinery preserved for the future reviews plugin, no keys/ids stored. seo 1.0.6. VERIFIED: lint x4, 88/88 + 58/58 + 34/34, tsc 59 zero new, build 33.3s, served byte-identical (5,106,806); live Google call = the owner's first key entry (no key exists on this install - honest) - LOCAL ONLY
- APIFY - THE GOOGLE SURFACE, ONE KEY [apify-google] (owner GO 2026-07-18, gap GAP-ANALYSIS-APIFY-GOOGLE-SURFACE-20260718): the apify Integrations provider (regular key flow) + PCM_SEO_GBP_Apify_Provider through the EXISTING abstraction - ONE actor run returns the place record + ALL FOUR SCHEMA IDS (placeId / cid / fid / KGMID - kgid was manual-only until now) + reviews. ZERO HARDCODE: actor ids (main + the future reviews-only top-up tap), timeout, search limit, review caps, language ALL live in the merge-seeded option pcm_seo_apify - switching actors or providers is data, never code (seo_gbp_provider default -> 'apify'; the native google provider stays registered as the data-selectable alternative). normalize() gains shape auto-detection -> normalize_apify() (pure, 7 harness tests: schema ids, address parts, hours flattening, embed from cid, review capping, v1 routing intact). Card gains the fid row (one registry line). Sync-run worker-hold named: tunable timeoutS=90 default. seo 1.0.7. VERIFIED: lint x3, 88/88 + 65/65 + 34/34, tsc 59 zero new, build 42.0s, served byte-identical (5,106,850); live Apify run = the owner's key entry (none on install - honest, request path harness-pinned) - LOCAL ONLY
- MAPS PASTE - THE share.google WALL + FIND ON GOOGLE [maps-paste-fix] (owner live report, gap GAP-ANALYSIS-MAPS-PASTE-SHARE-GOOGLE-20260718, 3 diagnostic + 2 verify probes w/ the owner's real key, all deleted): FACTS - the Apify chain was NEVER broken (live: real place returned w/ kgmid via our placeIds form); share.google is a JS-only wall (server, Chrome-UA and Apify startUrl all land on Google's error page - proven); MY flow-order bug returned the local-parse error FATALLY before the resolver ran (the owner's toast). FIXES: business_maps treats unparsed as NON-fatal (stores mapsShareUrl, continues; foreign hosts stay fatal); resolver names the share.google wall specifically WITH the way around it (Share->Copy link / address-bar URL - the named-errors law) and carries resolvedUrl so salvageable cid/geo from long URLs still land; NEW /business/place route + card FIND ON GOOGLE (search -> pick -> the whole record incl. schema ids) - the share-link-independent fill. LIVE VERIFIED: named wall answer + throwaway-brand fill (Estetikcenter: kgid /g/11hdpmd271 tagged gbp, cid, fid, 19 reviews stored, then deleted - brand 13 untouched); one transient Avast SSL-timeout observed and named (environment, retry clean). seo 1.0.8. VERIFIED: lint x2, 88/88 + 65/65 + 34/34, tsc 59 zero new, build 39.5s, served byte-identical (5,110,654) - LOCAL ONLY
- BUSINESS CARD - NATIONWIDE LISTINGS + THE UX PASS [card-ux-sab] (owner live run, gap GAP-ANALYSIS-BUSINESS-CARD-UX-SAB-20260718): DB fact from the owner's real fill - a nationwide service-area listing carries ZERO address keys (Google publishes none; our empty-law already refused to invent a city) but Google's country-CENTROID lat/lng (Sweden 62.03/17.38) landed as fake geo -> THE CENTROID GUARD: geo lands ONLY beside a real address component (2 harness tests, suite 67/67); Service areas + Primary location stay the owner-declared SEO truth for nationwide clients. UX (Jony pass, text sizes unchanged per owner ruling): the two rooms sit SIDE BY SIDE (md grid, max-w-5xl); every value cell is a REAL visible field (white, bordered, shadow, focus ring - an editable field must look editable); a WORKING status line during any fetch ('fetching from Google via Apify; up to a minute') - the card never looks frozen. seo 1.0.9. VERIFIED: lint, 88/88 + 67/67 + 34/34, tsc 59 zero new, build 47.1s, served byte-identical (5,111,315) - LOCAL ONLY
- CARD POLISH + THE RANDOM RED DOT [card-polish-reddot] (owner orders + corrections, gap GAP-ANALYSIS-CARD-POLISH-REDDOT-20260718): THE RED DOT ROOT FOUND AND FIXED - record_heartbeat flipped the stored verdict on ONE transport error (this machine's proven Avast SSL blips + slow-site cURL 28 made random reds routine); THE FLAP GUARD: verdict flips only after failThreshold CONSECUTIVE failures (hub data, merge-seeded 2; additive fails counter), one success heals instantly - LIVE PROVEN (blip->still healthy fails=1, second blip->red, success->healed, throwaway cleaned). Shadows root = my hand-rolled shadow-sm (shared-components-law violation, single site) - DELETED; fields now the owner's spec: gray rooms, WHITE hairline-bordered no-shadow inputs, text sizes untouched. Real room headers w/ divider; the working state lives IN the header's reserved slot (zero layout shift; global banner DELETED same commit) naming the ACTUAL provider DYNAMICALLY from the Integrations registry via the card reply (providerName 'Apify' live-proven through the real route - never hardcoded, owner correction). Brand mapping = the shared searchable CreatableCombobox PRE-SELECTED w/ the mapped brand (unknown names guarded; connection still writes through sites); unit Select + Unlink + suggestion + Create-from-site kept. seo 1.0.10. VERIFIED: lint x2, 88/88 + 67/67 + 34/34, tsc 59 zero new, build 29.9s, served byte-identical (5,111,702), live flap-guard + providerName probes (deleted) - LOCAL ONLY
- THE OPTIMIZE CONSOLIDATION, SLICE A [optimize-consolidation-a] (owner GO 2026-07-18, gap eeec6b9): ONE BUTTON, THREE MODES - the Analyze pill DIED (icon import with it) into the Optimize caret menu (additive PillSplitButton `menu` prop, Radix, other consumers byte-identical): Quick optimize (main click, the action matrix unchanged - a TYPED instruction now runs strict), Super optimize (opens the analysis rail), Custom instruction (opens the order box). THE ROUTER: compile takes the live page outline and assigns every directive `targets` (additions target their anchor section -> the sky lane; unrouted = broadcast floor, sources certainty contract untouched; the single-item bypass survives only outline-less). THE SCOPED RUN: each section receives ONLY its own directives + broadcasts, untargeted sections are NEVER sent (the whole-order-to-every-section rewrite-everything cause died); per-section envelope purposes follow. The frontend already sends outline/sectionIndex/strict per call - SLICE B (server page map + strict contract/retention in seo/service+controller) awaits the other dev's in-flight landing there (honest split, zero conflicts). VERIFIED: php -l x2, harness 88/88, research 34/34, tsc 59 zero new, build OK 35.3s - LOCAL ONLY
- REVIEW EXPERIENCE ROUND, GROUPS A+B [review-round-ab] (owner GO 2026-07-19, gap e533bc5 + plan 669d4bc): F2 toasts now sit ABOVE every modal layer (explicit z-index on the shared toaster - the blurred-behind-the-editor toast died); F3 ONE LANE for rewritten sections - every new heading in a consolidated rewrite inherits the original's origin so the group draws one color (the amber-first-chunk + sky-rest mix died) and after Accept every sub-heading correctly reads as part of the edited section. VERIFIED with group C below - LOCAL ONLY
- REVIEW EXPERIENCE ROUND, GROUP C [review-round-c] (owner GO 2026-07-19, gap e533bc5 + plan 669d4bc): THE PER-SECTION CARDS - ReviewSection carries its OWN routed orders (the router's targets captured at run start); pending and card-less sections now show what was ordered HERE and the identical run-level dump under every card DIED - the run's order renders ONCE at the rail top; ONE format for quick and super runs. THE FILTER (F9): SEO/AI tabs + the run's own purpose tags as multi-select chips + a Clear per level + a stated empty state - sections AND individual changes filter live by why->teacher->group; failures always show (honesty law). Hierarchy fixed (F5): section titles lead 12px semibold, details quiet 10px, tags smallest. Checkboxes 14px and aligned (F7). THE ORIGINAL ROW ALWAYS EXISTS (C7): loading state with spinner, unavailable-with-retry state (amber, refetches), pickable when loaded - the silent vanishing row died (the versions-delete "fix" was a race). RAILS-OUTSIDE (F6/C6) deliberately deferred to its OWN next pair - the 250-line relocation is read-in-full-first work, stated not hidden. VERIFIED: tsc 59 zero new, build OK 41.7s, harness 88/88, research 34/34 - LOCAL ONLY
- REVIEW EXPERIENCE ROUND, GROUP C6 [review-round-c6] (owner GO 2026-07-19, gap e533bc5/669d4bc): RAILS BECOME OUTSIDE ADD-ONS - the analyze rail, the review rail, and the image panel now PORTAL into an outside host on the card's right (the keyword drawer's mirror: rounded, shadowed, self-centered, invisible when empty), the JSX staying in place while the DOM moves (zero logic relocation); the card TAPERS by the rail width when a rail is open (RAIL_TAPER_PX joins the drawer taper in ONE width formula - one source of truth) so the document keeps its reading width instead of being squeezed; the outside-click save exempts the host exactly like the drawer (closure-safe ref mirror). Section mode byte-identical (all three mounts were page-only). VERIFIED: tsc 59 zero new, build OK 1m3s, harness 88/88 - LOCAL ONLY
- THE LANGUAGE LAW, INTERIM GUARD [language-law-interim] (owner ruling 2026-07-19: English is NEVER acceptable unless the content is English): ONE shared LANGUAGE_LAW constant appended LAST to EVERY AI order - all three call paths (the review runs incl. quick/super/injection, the section-mode rewrite, the revise flow): "write in the SAME language as the current content, NEVER translate". Last-position outranks the server's broken Write-in-en instruction (the hub-locale masquerade, e533bc5 F1) until its root fix lands with the held server group - the root fix removes the broken variable, never this law. VERIFIED: tsc 59 zero new, build OK - LOCAL ONLY
- CHANGE-ROW GRID FIX [change-row-grid] (owner report 2026-07-19): the change-card checkbox row rebuilt as a FIXED two-column grid (12px box column + text column) - the box sits centered on the FIRST text line by construction, wrapped lines stay in their own column and can never slide under the box, nothing pushes content down. Geometry that cannot drift, replacing the flex row whose taller box shoved the first line. VERIFIED: tsc 59 zero new, build OK 58.9s - LOCAL ONLY
- THE EDITOR DECOMPOSITION S1 [editor-decomposition-s1] (owner mandate 2026-07-19, gap 325d280): SectionModal.tsx 2,864 -> 2,361 lines - the ~550 module-level lines moved VERBATIM into editor/: types.ts (SectionData/InsertData/ReviewSection/... one concern), layout.ts (widths+tapers+LANGUAGE_LAW+the three type scales, one source per number), content-laws.ts (the AI-content gate, identity stamping, origin restating, composeSectionHtml - pure and testable), extensions.ts (the six TipTap extensions incl. blockDecorations), ToolButton.tsx. The component now only COMPOSES. Public contract preserved by re-export (HeadingsPanel/index.tsx untouched, consumers verified); TEN dead imports removed symbol-by-symbol grep-proven (tiptap core/pm/Image/Fragment/Decoration/DocSection/Editor). Behavior byte-identical by construction. NEXT SLICES gapped: S2 run-state machine (kills the flag-leak bug class), S3 review-rail component, S4 versions module, S5 panels. VERIFIED: tsc 59 zero new, build OK 31.6s, harness 88/88, research 34/34 - LOCAL ONLY
- THE EDITOR DECOMPOSITION S2 [editor-decomposition-s2] (owner 700-line law, gap 10a0233): THE REVIEW RAIL IS ITS OWN COMPONENT - editor/ReviewRail.tsx (361 lines, explicit 16-prop contract; THE FILTER's state moved IN with it - pure view state left the monolith); SectionModal 2,361 -> 2,063 lines. TWO OPEN FIXES RODE ALONG: the analyze rail SCROLLS again (root cause at the line: OptimizerRail's aside had no height inside the overflow-hidden outside host, so its list's overflow-auto never engaged - h-full restores it; blueprint fix 2) and the rail HIERARCHY leads with the researcher title (12px semibold slate-800, same scale as the review rail; evidence drops to 9px slate-400 subtext; blueprint fix 3). A rail bug now lives in ReviewRail/OptimizerRail by construction. VERIFIED: tsc 59 zero new, build OK 43.9s, harness 88/88 - LOCAL ONLY
- THE EDITOR DECOMPOSITION S3 [editor-decomposition-s3] (owner 700-line law, gap 10a0233): THE RUN ENGINE IS ITS OWN FILE - editor/useAiReview.ts (581 lines, explicit args contract): every AI run (quick/super/custom/keyword-insert/revise) + the whole review lifecycle (surgery engine, oracle, decision path, completion watcher, chips/flash/quote wiring) moved VERBATIM behind ONE hook; SectionModal 2,063 -> 1,560 lines. THE LEAK FIX RODE ALONG (blueprint fix 4): closing a review now CLEARS the run's compiled order - a later custom edit can never wear a stale "super optimize" identity (runDirectives dies with its review). Dead sweep grep-proven: nine engine-only imports + the orphaned mutation left the composer. A run bug now lives in useAiReview by construction. VERIFIED: tsc 59 zero new, build OK 41.1s, harness 88/88 - LOCAL ONLY
- THE EDITOR DECOMPOSITION S5a [editor-decomposition-s5a] (owner 700-line law, gap 10a0233): THE IMAGE MACHINERY IS ITS OWN FILE - editor/useImagePanel.ts (the locked-image selection watcher, the alt/title rule form, add-image delivery into the client's own media library, save/revert/hide) moved VERBATIM behind one hook with an explicit args contract; SectionModal 1,560 -> 1,450 lines. An image bug now lives in useImagePanel by construction. VERIFIED: tsc 59 zero new, build OK 49.2s, harness 88/88 - LOCAL ONLY
- THE EDITOR DECOMPOSITION S4a [editor-decomposition-s4a] (owner 700-line law, gap d0d063c): THE VERSIONS MACHINERY IS ITS OWN FILE - editor/useSectionVersions.ts (version rows page+section, the lazy Original with its honest states, pick/labels incl. the Draft-unsaved and Current-original laws, single+bulk delete with the W0 current-row guard) moved VERBATIM behind one hook; the editor resolves by REF at event time so the hook mounts before useEditor (the instant-open block needs its rows). SectionModal 1,450 -> 1,340. A versions bug now lives in useSectionVersions by construction. VERIFIED: tsc 59 zero new, build OK, harness 88/88 - LOCAL ONLY
- THE EDITOR DECOMPOSITION S4b [editor-decomposition-s4b] (owner 700-line law, gap d0d063c): THE VERSIONS DROPDOWN IS ITS OWN COMPONENT - editor/VersionsMenu.tsx (the trigger + menu: Draft state, THE ORIGINAL ROW THAT ALWAYS EXISTS with loading/unavailable-retry states, bulk cleanup with the W0 current-row guard, per-row pick/delete) moved VERBATIM; its contract IS the hook's return type (one source, zero drift). SectionModal 1,340 -> ~1,240; the composer keeps only its own six consumers of the machinery. VERIFIED: tsc 59 zero new, build OK, harness 88/88 - LOCAL ONLY
- THE EDITOR DECOMPOSITION FINAL PAIR [editor-decomposition-final] (owner 700-line law, gap d0d063c): THE HEADER STACK, THE IMAGE PANEL, THE SAVE BAR are their own components - editor/EditorHeader.tsx (445: both page rows incl. the identity corner, status/brand/page-type owned end-to-end inside, the smart keywords button, the insert menu, the one AI entry, section-mode row, the Ask-AI box, the placement row), editor/ImagePanel.tsx (77), editor/EditorFooter.tsx (75); the brandId cache-splice keeps served values (the header owns the link now). SectionModal 1,226 -> 855 - the composer composes. VERIFIED: tsc 59 zero new, build OK 29.9s, harness 88/88, research 34/34 - LOCAL ONLY
- THE EDITOR DECOMPOSITION FINAL SQUEEZE [editor-decomposition-squeeze] (owner guideline: quality over the number): THE SAVE FLOW is its own hook - editor/useSaveFlow.ts (171: the page save with per-kind honest summary + the corner's seeded verdict + the never-auto-replace law, and the section/insert/slice rule saves) - and THE EDITOR BODY its own component - editor/EditorBody.tsx (the staged-open overlay, the honest page error, the select-text bubble toolbar, the content surface). The save-flow + outside-click pair relocated AFTER the run engine (save reads the review gate - order is truth, not accident). SectionModal: 2,864 -> 702 across the whole arc; fifteen editor/ files, every one a single concern with an explicit contract. VERIFIED: tsc 59 zero new, build OK 26.2s, harness 88/88, research 34/34 - LOCAL ONLY
- BRAND-CONTEXT REGRESSION FIX [brand-context-fix] (owner GO 2026-07-20, gap ee0d8a5): the decomposition's one behavioral delta closed - EditorHeader's own pageState query DELETED; brandId flows through the new brandContext prop carrying the composer's original two-source truth (featherweight check OR inventory fallback, whichever answers first) - the business picker can no longer blank on the no-saved-version path, and the composer is again the ONE page-state query owner. VERIFIED: tsc 59 zero new, build OK, harness 88/88 - LOCAL ONLY
- THE CONTENT-INTEGRITY LAW [content-integrity-law] (owner report 2026-07-20, gap 795298f D2): shortcodes, markup, and code are UNTOUCHABLE tokens - one shared CONTENT_INTEGRITY_LAW constant (beside the language law, editor/layout.ts) appended on EVERY AI order across all three run paths (review runs incl. quick/super/custom/injection, the section rewrite, the revise flow): square-bracket shortcodes, HTML tags/attributes/classes/ids, entities, and embedded code reproduce byte-identically unless the order names them. D1 (the raw-envelope dump - broken JSON replies land as literal wrapper text in the document, parse_section_reply:6104-6119) is pinned and HELD for the Group D server pair (his in-flight file); D3 (schema-gate drops for tables/divs) named as its own future gap. VERIFIED: tsc 59 zero new, build OK, harness 88/88 - LOCAL ONLY
- THE APPROVALS FILTER BAR IS THREE SEARCHABLE DROPDOWNS [approvals-filter-dropdowns] (owner order + GO 2026-08-04, gap 30f87f1 PART A): the free-text search box and the "Approval set" dropdown are GONE - the bar is Brand / Delivery / Project, each a search box inside its own dropdown. New shared control components/shared/SearchableSelect.tsx (Popover + Command, clearable, NO create row - the filter counterpart to CreatableCombobox, which always offers "Create <text>" and is wrong for filtering); the Kanban barrel declares filter UI out of scope, so it lives in the shared layer per UI RULE 1 and the Deliveries board can adopt it in one line. Filtering is now by ID with names resolved from the registries the board already loads (brands.list / deliveries.list / assets.getProjects) - which FIXES the dead Brand + Project filters and the missing card brand prefix: list_sets_by_user ships neither snapshot nor reviewFeedback (service.php:69, since eafdb1e 2026-06-13) while both filters read snapshot.brandName/projectName. deliveryId added to the SELECT (snapshot stays OUT - longtext, can carry embedded images). Options are narrowed to ids actually present on the board, so no choice returns nothing; an id with no registry row is labelled honestly rather than hidden. persistKey bumped .v2 because activeFilterCount counts raw STATE, so a leftover search/set key would strand "Clear filters" forever. The notification jump (NotificationsPanel -> AppContext -> board), which used to pin the now-deleted set filter, opens that set's preview card instead (owner's choice). VERIFIED: php -l, harness 88/88, tsc 59 zero new, build OK 35.8s, live SELECT probed against the hub DB - LOCAL ONLY
- ONE MAPPING: THE SET BELONGS TO A PROJECT [approvals-project-only-mapping] (owner order + GO 2026-08-04, gap 30f87f1 PART B): the competing delivery mapping is deleted from every create path - the Delivery picker in the shared SendToApprovalSetDialog, in Ads/CreateApprovalSetDialog and the deliveryId send in CreateCustomSetDialog are replaced by ONE shared components/shared/ProjectPicker.tsx: search the projects, or type a name to create one, and pick that NEW project's delivery inline ("No delivery" is a real answer). PCM_Hierarchy already WAS this law - projects.deliveryId and deliveries.brandId are both nullable and its docblock forbids storing derived ids - so this only stops the UI writing a second truth. create_project (assets/controller.php) accepts an optional, ownership-checked deliveryId so create+attach is ONE insert instead of a pair that can half-fail leaving an unattached project. format_set_row: when a set HAS a project the chain is the whole truth INCLUDING its nulls, so a project deliberately left with no delivery reports exactly that instead of falling back to a stale stored id; sets with no project keep their stored ids (legacy rows). approval_sets.deliveryId and its enrich_context fallback are KEPT for those rows - no migration, nothing dropped. VERIFIED: php -l both files, harness 88/88, tsc 59 zero new, build OK - LOCAL ONLY
- THE CARD APPEARS INSTANTLY + EVERY CREATE FLOW ENDS ON THE CLIENT LINK [approvals-create-feedback] (owner report + GO 2026-08-04, gap ebe6501): TWO defects closed. (1) NOTHING in the app ever invalidated approvals.listSets - grep-verified zero call sites - and the tRPC adapter's useMutation does no cache work at all (lib/trpc.ts:176-208), so with staleTime 30s + refetchOnWindowFocus FALSE (main.tsx:17-26) a page reload was the ONLY way a new set reached the board. New components/shared/approvalSets.ts owns the list query key, the public-board URL builder, and useApprovalSetsCache().registerCreated(row): the created row (the create response IS the full row, controller.php:151-152) is spliced into the cache for an instant card, THEN invalidated to reconcile - the invalidate also covers a board that was never mounted, where setQueriesData has nothing to update. Wired into all THREE create paths, not just the reported one. useApprovalSets now IMPORTS that key instead of declaring its own copy - a second copy is exactly how the dialogs' writes would have landed nowhere - and its hand-rolled link builder is gone for the shared one (the URL was built in three places). (2) The custom-card flow was the ONLY create path that closed on a toast, silently dropping the client link the whole feature exists to produce, while Copy/Image/Ads all stayed open on a share screen. The share screen is now components/shared/ApprovalSharePanel.tsx - extracted VERBATIM from SendToApprovalSetDialog, adopted by the Ads dialog (which carried a near-identical copy incl. its own 55-line clipboard fallback, now deleted) and by the custom dialog, which stays open on it with a Done & Close footer. buildDefaultInviteMessage moved to the panel (the one place an invite is composed) and is re-exported from its old home so importers keep working. Dead code swept in the same commit: copied/inviteSent/shareMutation/handleSendInvite/handleCopyLink + Textarea/Mail/Copy/Check imports. VERIFIED: tsc 59 pre-existing ZERO new, build OK 52.0s, harness 88/88 - LOCAL ONLY
- THE LANE "+" + THE OPTIONAL DELIVERY LINK ON AN EXISTING PROJECT [approvals-lane-create] (owner order + GO 2026-08-04, gap 36e7d07): every lane header gets a "+" that creates straight into THAT lane, pre-filled from the live filters. The "+" is an ADDITIVE prop on the shared primitive (KanbanBoardProps.onColumnCreate, KanbanBoard renders the button + one .columnCreate style) - any future board gets it free, and the Deliveries board is a one-line adopt. THE LANE IS ONE INSERT, NOT CREATE-THEN-MOVE: create_set takes an optional status validated against STATUSES (absent -> 'draft' exactly as before, invalid -> named 400). Deliberate: create-then-update_status would write a phantom Draft row AND fire approvals.set_status_changed, which per the seeded rules would fire the Launch webhook for a set merely CREATED in Launch - a creation is not a lane change. Born in 'client' still stamps clientSentAt, or the pending-client reminder scanner would silently never fire for those sets. The share dialogs keep their two-call flow untouched (there the move IS a real transition). PRE-FILL WITHOUT A SECOND MAPPING: the project filter pre-selects the project; the delivery filter seeds only the PROJECT's delivery, never the set; the brand filter pre-fills brandId, which the live chain overwrites the moment a project exists - so the pre-fill can never contradict the one-mapping law. THE OPTIONAL LINK: ProjectPicker now offers the delivery row for an EXISTING project that has none (GET /assets/projects already returned deliveryId - no backend needed), labelled optional, defaulting to No delivery; submitting without it is a first-class outcome (owner explicit). A project that ALREADY has a delivery is left alone - re-pointing belongs to the Projects registry. resolveProjectId stays the ONE choke-point and grows the attach step (assets.setProjectDelivery), so all three dialogs behave identically. Also killed: the identical projects/deliveries normalisation copied into all three dialogs -> useProjectPickerData() (that triple copy is exactly how deliveryId went missing from the options in the first place). VERIFIED: php -l x2, harness 88/88, tsc 59 zero new, build OK 40.6s, schema probed live (status NOT NULL default draft, clientSentAt + projects.deliveryId nullable; 2 of 5 projects currently unlinked, so the new row is reachable) - LOCAL ONLY
- THE APPROVAL-SET DIALOG: TWO-ROW MAPPING, GENERATE SHARE LINK, MULTI-RECIPIENT SEND [approvals-share-flow] (owner order + GO 2026-08-04, gap 270356c): the dialog is now Row 1 = Name / Recipient Email (renamed from Client email; it seeds the send step) / Lane, and Row 2 = Project / Delivery / Brand with the delivery permanent instead of a conditional reveal. ANY OF THE THREE NARROWS THE OTHER TWO: brand -> its deliveries -> their projects; delivery -> its projects (+ brand fills in); project -> delivery + brand fill in from its own chain. All derived client-side from lists already on the wire (deliveries.list carries brandId, getProjects carries deliveryId) - no new endpoint, no new query. A picked project is always kept in its own dropdown even when the current filters would exclude it, so a field can never show a value its list doesn't contain. GENERATE SHARE LINK sits top-right: it saves the set and reveals the link WITHOUT closing anything, and copying stays a deliberate second click. THE SEND STEP (ApprovalSharePanel, one surface for all four send paths): recipient PILLS (Enter/comma/paste to commit, Backspace to remove), the editable default message, a "move to lane after sending" dropdown whose options come from setColumns - the SAME registry Row 1's Lane reads, so the two can never disagree and adding a lane stays one line in one file - and three actions: Cancel (nothing), Save (send + move + close the step, editor stays), Save and Close (send + move + close everything). The lane move goes through updateSetStatus so it keeps ownership scope, the clientSentAt stamp and the trigger chain; it runs only AFTER the send succeeded, so a set can never report "sent" with no email out. MULTI-RECIPIENT IS ONE SHARE EVENT WITH N EMAILS: share_set now takes string|array, de-dupes, dispatches the invite per recipient (overriding context.clientEmail, which is what render_email_for_event reads) and fires the set_shared TRIGGER exactly once - firing it per recipient would run the seeded move-to-client rule and any user webhook N times. clientEmail on the set and the brand's remembered address stay single-valued: multi-send is an action, not new state. The route accepts emails[] with the original single `email` still working; nothing valid in the list is a named error, never a silent no-send. VERIFIED: php -l x2, harness 88/88, tsc 59 zero new, build OK 33.0s, bundle grep-confirmed to carry the new UI - LOCAL ONLY
- THE BOARD DEFECTS ROUND: THE "+", SELECT MODE, THE EMPTY DROPDOWNS, THE LOAD TIME [approvals-board-defects] (owner report + GO 2026-08-04, gap 875fa13): four fixes, three of them my own defects from this session. (A) THE "+" IS ON EVERY LANE NOW: it always was, but my .columnCreate opacity:0 + .column:hover rule meant only the hovered lane showed one - it is now always visible, drawn with the shared lucide Plus instead of a hand-rolled inline <svg>, and it wears its OWN lane's colours: the header publishes --pck-column-accent-bg/-text from the column's accent, so the stylesheet colours the button per lane instead of one neutral hardcoded in TSX. (B) SELECT IS A MODE, NOT A HOVER SURPRISE: the hover reveal of the checkbox and the hover title-shift are gone; a Select toggle now sits in the toolbar beside Sort, select-mode is explicit state (it was inferred from "is anything selected", so the only way in was a checkbox that only existed on hover), and leaving the mode clears the selection. (C) THE DROPDOWNS FIND THINGS AGAIN: they were built from ids PRESENT ON THE BOARD - live-probed, the only set maps to a project with no delivery, so Delivery and Brand came back with ZERO options ("No deliveries match") while the registries held 3 deliveries and 2 brands. They now offer the full registry, plus any id a set carries that the registry no longer explains so that card stays reachable; a zero-match choice falls through to the existing "No sets match your filters" notice. (D) LOAD TIME, MEASURED NOT GUESSED: ver=time() on a 5,281,031-byte script (class-pcm-admin.php:129, comment: "for dev testing") meant the browser could NEVER cache the bundle - 5.28 MB re-downloaded on EVERY admin page load. Now filemtime, matching the stylesheet beside it: still busts on every build (fixed filename, version does the work), cacheable otherwise. THAT is the slowness. Secondary: get_projects gained basic=1 (id/name/deliveryId/siteId/status) skipping the ungated GROUP_CONCAT of every asset URL + the copy_results COUNT, used by the board and the pickers - but MEASURED it is worth only ~2ms of SQL and 1,456 bytes here, so it is honest housekeeping, not the fix. VERIFIED: php -l x2, harness 88/88, tsc 59 zero new, build OK 32.0s, query timings probed live - LOCAL ONLY
- THE QUALITY ROUND: THE x, THE SHARE POPOVER, THE "+", THE BAR, THE PROSE SCALE [approvals-quality-round] (owner order + GO 2026-08-04, gap bebda90, plan 19ff403): STEP 1 MEASURED FIRST and killed the guesswork - the share button's ENTIRE server cost is ~120ms (share_set 81.8ms, create_set 17.9ms, build_share_url 3.4ms, update_status 18.8ms, plus one ~440ms WP boot per REST call), and every stored snapshot is 262-885 bytes with no base64 - so the 15-30s the owner sees is NOT the server and NOT the payload. Probe was secret-keyed, web-context, deleted, re-requested and confirmed 404. What I could fix without his Network trace: registerCreated no longer forces an immediate refetch (refetchType 'none') - the row is already spliced in, so the refetch showed nothing new while firing another REST call into the exact moment of the click, on a box with two PHP workers. STEP 2 THE x: root cause was structural - it lived INSIDE the Radix PopoverTrigger, which toggles on POINTERDOWN, while the x handled CLICK, so the list opened before the clear registered and the control read as dead. It is now a SIBLING of the trigger in both SearchableSelect and ui/creatable-combobox (the latter pre-existing), keyboard-reachable and labelled. STEP 3 THE SHARE STEP IS A REAL POPOVER: rebuilt as PopoverAnchor + PopoverContent anchored to the Generate Share Link button - it floats, it never expands the dialog body again - with the media-frame/annotator outside-interaction guard carried over; ALL 14 inline style={{}} blocks replaced with semantic classes (the orphaned design-tokens import removed with them), and "Message to client" now sits on bg-card so it is legible instead of grey-on-grey. STEP 4 BOARD CHROME: the lane "+" has NO background at all now - glyph only, in its lane's own accent, resting opacity 0.3 to 1 on hover/focus; the Clear-filters control moved into the right-hand group so the flex-wrap bar can no longer wrap and push the board down when a filter is chosen; SetsBoard's 11 raw Tailwind palette literals (bg-blue-50/border-blue-200/text-slate-*) are now semantic tokens - 0 remain. STEP 5 THE PROSE SCALE: measured the two surfaces - the Writer canvas is 11px/1.7 and the card editor was a bespoke 13px/1.6, which is exactly the "font got bigger" report. Introduced ONE source (--pcm-prose-size/-leading/-color/-p-leading/-p-color) consumed by BOTH .writer-editor and .pcm-card-editor; the Writer's rendered values are unchanged (the vars resolve to what it already had), the card editor now matches it. VERIFIED: harness 88/88, tsc 59 zero new, build OK 48.2s - LOCAL ONLY
- SHARING ACTUALLY SENDS NOW + SEND VERBS, CLEARABLE LANE, ONE EMAIL FIELD [approvals-share-send] (owner report + GO 2026-08-04, gap bc4a66f): THE SEND WAS BROKEN 100% OF THE TIME AND IT WAS MY DEFECT. The panel sent {id, EMAILS: [...], message}; the tRPC transform rebuilt the body as { email: input.email, message } (trpc-routes.ts:1151) - hard-picking the SINGULAR key, so input.email was undefined, JSON.stringify dropped it, and the request carried only `message`. The server found neither emails[] nor email and answered "A valid email is required." for a perfectly valid address. I had extended the server AND the panel to emails[] in the same round (e58f1e0) and never touched the transform between them: a hand-built body is an ALLOW-LIST, so a field added at both ends vanishes in the middle with no error anywhere. Transform now carries emails AND email (legacy callers). Audited the rest of the approvals surface: appendToSet and updateSetStatus also hand-pick, but {snapshot} and {status} are exactly what their handlers read - correct; every other approvals route is body:input pass-through. COROLLARY WORTH KNOWING: since no send ever succeeded, the "move to lane after sending" step had never executed either - it is sequenced after the send - so that path was untested, not merely unfixed. ALSO: the two actions now read SEND / SEND AND CLOSE, because that is what they do - the set was already saved by Generate Share Link, nothing is "saved" at that moment. The lane dropdown became the shared SearchableSelect so the choice can be CLEARED (clearing = leave the set where it is); it was a plain Select with no clear affordance, opening pre-selected on `client` with no way to un-choose. And the Recipient Email field is GONE from the card view: it existed twice (Row 1 + the pills, where Row 1 only seeded the first pill) and its value also made the SERVER email during create_set. Row 1 is now Name + Lane, creating a set emails nobody, and the popover is the ONE route an invite leaves by - the dead alreadySent seeding went with it. VERIFIED: harness 88/88, tsc 59 zero new, build OK 48.7s, bundle grep-confirmed ("Send and Close" present, "Recipient Email" gone) - LOCAL ONLY
- THE FILTER STATE NO LONGER MOVES THE BOARD [approvals-filter-no-reflow] (owner report 2026-08-04, second pass): the previous round moved the Clear-filters BUTTON into the right-hand group but missed the actual culprit - a separate "No sets match your filters. Clear filters" notice rendered as its own row ABOVE the board (SetsBoard :490-501), which pushed every lane down the moment a filter matched nothing. That row is deleted. The same message now occupies the bar's existing counter slot, which is always present and simply reads either "Showing N sets" or "No sets match your filters" - same element, same row, so the layout cannot move because of filter state. Also swept the last bg-white literal on the sort trigger to bg-card. VERIFIED: tsc 59 zero new, build OK 47.2s - LOCAL ONLY
- THE DROPDOWN BLINK — ROOT CAUSE, BOTH CONTROLS [dropdown-blink-root] (owner report 2026-08-04): selecting a filter made the dropdown flash twice. TWO defects, both introduced by my own x fix, both now fixed at the root rather than masked. (1) THE DOUBLE PAINT: the search query was cleared by a `useEffect` on [open]. An effect runs AFTER the commit, so closing produced TWO renders - the first began the popover's exit animation with the list still filtered, the second re-rendered that SAME still-visible list unfiltered, so the full option set visibly flashed back before fading out. Clearing now happens in the same batched update as the close (setOpenState), giving exactly one render and no flash; the now-dead useEffect and its React import went with it. (2) THE TRIGGER RELAYOUT: pr-14 (pr-11 compact) was applied ONLY when something was selected, so the instant you picked a value the label's width shrank by 56px and re-laid-out at the same moment the clear button mounted beside it - a second visible jump in the same interaction. The padding is now unconditional, reserving room for the chevron and the clear whether or not a value is set, so selecting changes nothing about the trigger's geometry. Fixed identically in components/shared/SearchableSelect (Brand/Delivery/Project/Lane) and components/ui/creatable-combobox (Project), so no dropdown on the surface can blink. VERIFIED: tsc 59 zero new, build OK 30.6s - LOCAL ONLY
- STEP 1 — ONE DOCUMENT SCALE, AND THE BACKDROP THE OPENED CARD NEVER HAD [approvals-document-surface] (owner order + GO 2026-08-04, gap 0c8dc6b): TWO scales now exist and are deliberately distinct, documented as such so nobody merges them again: --pcm-doc-* (approval-card documents: 16px on a 708px measure, Notion-like) and --pcm-prose-* (the Writer canvas, which documents its own 11px as "compact fit with admin UI"). I had collapsed these into one earlier today, dragging the card editor 13px -> 11px: sharing a definition was right, sharing it with ADMIN CHROME was not. Declared on BOTH :root and #pcm-root on purpose - the card viewer createPortals to document.body, OUTSIDE #pcm-root, so a #pcm-root-only declaration would never have reached it. The card VIEWER (.pcm-notion-col/-title/-prose) and the card EDITOR (.pcm-card-editor) now consume the SAME variables, so authoring and reading are one surface by construction rather than two lookalikes; the editor also gained the centred measure and bottom breathing room it lacked, and keeps its two-branch selector (#pcm-root .pcm-card-editor at 1,1,0 beats the #pcm-root :where() resets at 1,0,1 in-page; the bare 0,1,0 branch applies inside a portal where the reset never runs - disjoint contexts, identical values). AND THE BACKDROP: .pcm-notion-overlay was a FLAT SCRIM (rgba(15,15,15,0.55), no backdrop-filter), so the page behind stayed sharp and the document never separated from it - the single biggest reason the opened card read as an unfinished panel. It now carries scrim + blur(12px) saturate(120%), while the card TILE already had blur(30px): the document was the least considered surface in the module and the thumbnail the most. VERIFIED: exactly ONE --pcm-doc-size definition in the tree, 15 consuming references across both stylesheets, tsc 59 zero new, build OK 34.8s, and the variables are present in the built dist/index.css - LOCAL ONLY
- STEP 4 (PART) — THE CLIENT SURFACE STOPS LYING AND STOPS SPEAKING SWEDISH [approvals-client-truth] (owner order 2026-08-04, gap 0c8dc6b §5.4-5.6): THREE debt items closed, each a correctness issue rather than polish. (1) SEVENTEEN HARDCODED SWEDISH STRINGS across ClientReviewPage (hero, progress, status, loading, the asset summary builder), ClientStatusToolbar (filter chips, deadline, Approve all/Confirm/Sending/Done) and CreativeAssetCard (three editor placeholders + the article hint) are now English - they had been sitting beside English "Approve"/"Comment"/"Awaiting" on the SAME screen, against the standing English-only law. (2) THE INVENTED DEADLINE: REVIEW_PERIOD_MS was a hardcoded 4-day literal shown to the client as a real response deadline nobody had agreed to - a business rule living in a component. It now reads a hub-supplied approvalsReviewWindowDays and, when none is configured, renders NO deadline at all: silence is honest, an invented date is not. (3) SUBMIT_REVIEW IS NOW IDEMPOTENT: a set already in a post-submit lane returns true without re-running, so a refresh, a double click or a replayed request can no longer re-fire the outbound webhook. approve_assets had always carried that guard; submit_review did not, and the client UI was the only thing standing between a reload and a duplicate dispatch - its own type comments admitted as much. VERIFIED: php -l, harness 88/88, tsc 59 zero new, build OK 33.5s, grep confirms 0 Swedish strings left in the three components - LOCAL ONLY
- SELF-AUDIT FIXES: I BROKE MY OWN RULES AND FIXED THE CAUSES, NOT THE SYMPTOMS [approvals-self-audit] (owner order 2026-08-04, gap 2102271): (C1) THE WORST ONE - trpc-routes.ts shareSet still hand-built { email, emails, message } AFTER I had diagnosed that a hand-built body is an allow-list and written a nine-line comment saying exactly that. I had added the missing key instead of removing the failure mode, so the NEXT field added at both ends would have vanished identically and silently. It is now `body: input`: the handler already reads only what it needs and sanitises it, so the client has no business deciding which of its own fields survive the trip. (C3) TWO NORMALISERS FOR THE SAME THREE REGISTRIES - SetsBoard carried its own toNamedRows() for brands/deliveries/projects beside ProjectPicker's useProjectPickerData(), both written by me in the same session, and that exact duplication is how deliveryId went missing from the picker options the first time. toNamedRows and nameMap are deleted; the board consumes the shared hook and keeps no copy of that boundary code, which also removed its last direct trpc import. GATES: the shareSet transform names zero fields, exactly ONE useProjectPickerData definition exists, and toNamedRows survives only as prose in a docblock explaining why it is gone. VERIFIED: harness 88/88, tsc 59 zero new, build OK 36.4s - LOCAL ONLY
- FACT-CHECK OF THE HANDOVER + THE FIXES IT EXPOSED [approvals-factcheck] (owner order 2026-08-04, gap 4a29e62): the owner asked whether the previous session's claims were true. Re-verified every one at file:line. MOST HELD - crumb 95-98, Approve 147-155, window token 349, portal 85, editability 401, sole CSS importer ClientReviewPage:13, 53 hex / 8 z-index / 1 !important, 729 lines, harness 88/88, tsc exactly 59 - and the document-scale work and submit_review idempotency guard are genuinely sound. THREE WERE MATERIALLY WRONG, all the same failure: stopping at the first negative result. (D1) CHECKBOXES WERE NEVER BLOCKED - the handover parked the checklist "pending an owner decision" because @tiptap/extension-task-list / -task-item are absent. Those are the TipTap v2 names; @tiptap/extension-list@3.22.3 is ALREADY installed via starter-kit and exports TaskList + TaskItem through ./task-list and ./task-item. No install, no decision. (D2) THE CLIENT DUE-DATE WAS DEAD CODE WHOSE COMMENT CLAIMED OTHERWISE - the 4-day literal was correctly removed, but the replacement read pcmConfig.approvalsReviewWindowDays, a key NEITHER config builder emitted, so the value was always 0 and no deadline could ever render for anyone. FIXED at the root: approvals_review_window_days joins PCM_Settings::$defaults (array_merge reaches old installs) and is emitted by BOTH PCM_Admin::get_js_config() and PCM_Shortcode; default 0 still means no deadline, because a date nobody agreed to is an invented promise - the difference is the path is now real instead of silently dead. (D3) THE BLUR IS NOT ARCHITECTURALLY TRAPPED - ClientReviewPage renders as the WHOLE PAGE from App.tsx:21-26 on ?pcm_public_token= with no iframe anywhere, which is the real client surface and where the blur already covers the viewport; only the admin's in-app preview iframes it (PreviewDialog.tsx:61-66). ALSO FIXED: the Row 2 regression the owner screenshotted - 57ad9cb changed Row 1 AND Row 2 from sm:grid-cols-3 to -2 in one commit, Row 1 rightly (the email field had gone) and Row 2 as collateral, which wrapped Brand onto a third line; Row 2 is back to three columns and the SERVED bundle now carries both `grid gap-3 sm:grid-cols-2` and `sm:grid-cols-3`. C4 from the previous session's own self-audit had never landed and had fallen off the open list - laneOptions now lives once in setColumns.ts in the shared SearchableSelectOption shape and ShareLaneOption aliases it, deleting BOTH conversions (the call site mapped {value,label} to {id,label} and the panel mapped it straight back). --pcm-notion-ink deleted - it held the same #37352f as --pcm-doc-ink, one colour under two names in two files. sv-SE locale to en-GB in ClientStatusToolbar and Brands - the Swedish cleanup had translated the strings and left the date locale, so clients still read Swedish-formatted dates. CLAUDE.md corrected: DB 1.39.0 to 1.45.0 and 18 modules to 23, both wrong against the constants. Debt restated with true numbers: 181 colour literals in client-review.css not 53 (128 rgba the hex-only count missed), 34 further raw hex and 49 inline styles (38 colour/size) across the module, 11 `type ===` lines not 4, 65 hand-built bodies. C2 flagged STILL OPEN rather than quietly dropped. MY OWN ERROR, NAMED: I first measured CreativeAssetCard at 678 lines and nearly reported the 729 claim as false - PowerShell Measure-Object -Line skips blank lines; his number was right and my method was wrong. NOT VERIFIED and said so: every behavioural claim remains browser-untested, and the 15-30s share click still needs a Network trace. VERIFIED: php -l x3, harness 88/88, tsc 59 zero new, build OK 27.5s, served bundle grep-confirmed for both grid classes - LOCAL ONLY
- THE PROPER NOTION CARD [approvals-notion-card] (owner order 2026-08-04, gap 37e1a6a, C1-C7): the opened custom card is now a Notion page, and it is reachable. (C1) THE DOCUMENT VIEW IS ONE DEFINITION - ArticleViewerDialog was a private 118-line function inside CreativeAssetCard.tsx with one consumer; it is now components/CardDocumentView.tsx with two, because rendering the same document from two copies is how two surfaces drift. Extraction was verbatim first, so every visual change after it is attributable to C2-C4 alone. (C2) THE CRAPPY DOCUMENT ICON IS GONE - the topbar carried a FileText icon plus the title, and the title already renders at 40px directly below it, so the crumb was repeating it; .pcm-notion-crumb is deleted and grep-confirmed 0 in the served CSS and JS. (C3) APPROVE MOVED INTO THE STICKY TOPBAR - it sat after the prose inside the scrolling column, so on any document of real length it scrolled out of reach; .pcm-notion-actions is deleted with it. (C4) IT IS A PAGE, NOT A CARD ON A TRAY - the overlay was align-items:flex-start with 4vh padding and the sheet capped at max-height:92vh, so it hung from the top with a hard band above AND below; it is now align-items:stretch with the cap removed, so the sheet takes the full height it is given. THE TYPOGRAPHY WAS NOT TOUCHED: 708px column, 40px/1.2/700 title, 16px/1.5 prose were already the Notion reference and stay exactly as they were - the gap was chrome and placement, not type. (C5/C6) THE PROPERTY AREA WAS ALWAYS EMPTY ON A CUSTOM CARD - it was gated on metaTitle||metaDescription, which are ARTICLE fields that CustomAsset does not declare, so the block never rendered anything; CardDocumentView now takes a typed properties array and the hosts build it from our own data - Brand, Project, Created on the client page, plus Lane on the board. Deliberately fed from RAW snapshot values through new setBrandName/setProjectName props, NOT the existing brandName, because that one carries a display fallback ('Client Board') that would have printed as a fact in a property row. (C7) YOU CAN FINALLY SEE IT - clicking a set on the board opened PreviewDialog, an iframe of the whole public client board, so a document just authored was never reachable AS a document from the admin; a set whose snapshot.custom is non-empty now opens CardDocumentView directly, everything else keeps the iframe untouched. The branch reads the set's own data - there is no list of set kinds to keep in step. Opened read-only with NO Approve action passed: approving is the client's act and the admin holds no public token. Also: CustomAsset.overlay declared (it was carried at runtime, written by CreateCustomSetDialog:176, never typed), and two inline style blocks replaced by .pcm-notion-prose-wrap / .pcm-notion-draw. NOT in this round, named so scope cannot creep: admin editing (needs the save(patch,ctx) contract), the card-type registry and its 11 type=== lines, and the task-list checklist. VERIFIED: harness 88/88, tsc 59 pre-existing ZERO new and none in Approvals, build OK 42.4s, served bundle confirms crumb 0 / new classes present / modal max-height removed - LOCAL ONLY
- BROWSER-VERIFIED THE NOTION CARD + THE SWEDISH STRING TWO GREP PASSES MISSED [approvals-notion-verify] (owner order 2026-08-05): the owner asked whether anything was actually checked. It is now. Real Chrome driven over the DevTools Protocol using Node 24's built-in WebSocket (nothing installed), loading the PUBLIC client page with a REAL set (id 17, snapshot.custom = 1 document), clicking the tile the way a person does, and reading COMPUTED styles off the opened document. PROVEN BY MEASUREMENT: crumb count 0, legacy bottom actions 0, Approve present in the sticky topbar, overlay align-items stretch, modal max-height none, modal 1039px of a 1105px viewport (ratio 0.94 - it is a page, not a card on a tray), radius 12px, column max-width 708px, title 40px/700, prose 16px/24px, backdrop-filter blur(12px) saturate(1.2), property row "Created 4 Aug 2026". The blur covering the whole page behind ALSO independently confirms fact-check D3 - it was never architecturally trapped; only the admin preview iframes the page. WHAT EXECUTION CAUGHT THAT GREPPING DID NOT: the rendered header read "UPPGIFT - GRANSKNING". ClientReviewPage.tsx:476 held a hardcoded Swedish "Granskning" that survived BOTH the 2026-08-04 cleanup of 17 Swedish strings AND my own fact-check which reported "zero Swedish left, verified" - because both searched for a-ring/a-umlaut/o-umlaut and this word has none. That claim of mine was wrong. Now "Review", re-rendered and confirmed as "UPPGIFT - REVIEW"; a diacritic-free Swedish sweep of the module now returns zero. STILL NOT VERIFIED AND NOT CLAIMED: C7, the admin board opening the document instead of the iframe, has not been executed - the shortcode shell is behind a password gate and wp-admin needs credentials I do not hold. Set 17 does carry snapshot.custom[0] so the branch will be taken, and CardDocumentView itself is now proven, but the SetsBoard wiring is unproven. VERIFIED: harness 88/88, tsc 59 zero new none in Approvals, build OK 25.8s, client path executed in Chrome against real data twice - once to find the defect, once to confirm the fix - LOCAL ONLY
- THE FONT, THE CHECKLIST, AND C7 - ALL THREE CLOSED AND MEASURED IN THE ADMIN [approvals-close-the-gap] (owner order 2026-08-05: "close the gap so that I can test it and that it works"): the admin surface was unverifiable all session because it sits behind a login. Cleared that with the project's standing mu-plugin probe protocol (secret-keyed, temporary), measured everything in real Chrome, then DELETED the probe and confirmed mu-plugins is empty, the key inert, and wp-admin 302s to wp-login again. (1) THE FONT. .pcm-card-editor was bound to --pcm-doc-* and MEASURED 16px/24px/#37352f on a 739px-wide editor with max-width computed as "none". It now reads --pcm-prose-* - the SAME variables .writer-editor reads - and measures 11px / 18.7px (= 11 x 1.7) / rgb(26,26,46). One definition, one owner. The client-facing viewer deliberately STAYS on the document scale, because 708/40/16 are the Notion metrics of the opened card; authoring and reading now differ BY DECISION, recorded in the CSS. (2) THE DEAD MEASURE. max-w-none was removed from the editor's class list: Tailwind ships with the `important` flag (index.css:8) so .max-w-none{max-width:none!important} had been beating .pcm-card-editor's max-width since it was written. Editor now measures max-width 708px, width 708px - the centred measure finally applies. (3) THE CHECKLIST. TaskList + TaskItem registered from @tiptap/extension-list (already installed via starter-kit - nothing added to package.json), TaskItem nested, a Checklist toolbar button beside the existing image/annotate/draw buttons, and ONE styling block selected on [data-type="taskList"] rather than a surface class so it serves every editor AND the portalled viewer. MEASURED: button present, task list renders, checkbox renders 14x14, list-style none, li display flex. (4) C7 WAS BROKEN AND IS NOW FIXED. My first version branched on previewSet.snapshot.custom - but list_sets_by_user selects an explicit column list and leaves snapshot OUT on purpose (approvals/service.php:74, a comment I had READ earlier this same session while proving the feedback chip dead). So the branch was dead on arrival and silently fell through to the iframe every time - proven by the admin probe showing IFRAME_OPENED true. It now fetches the one set by the token the row already carries, which is strictly cheaper than the iframe it replaces since that loaded the same payload plus the whole client page. MEASURED after the fix: NOTION_OPENED true, IFRAME_OPENED false, rows ["Lane Draft","Created 4 Aug 2026"], title "uppgift", 1039px of a 1105px viewport, across repeated runs. (5) A DEFECT I INTRODUCED AND CAUGHT. That fetch measured 1.4-2.9 s on this host (PHP-FPM pm.max_children=2), and my loading branch rendered NOTHING - a click looked like it had missed. The document shell now opens immediately carrying the name the board row already knows, with an "Opening document..." state line (a state statement, not instructional chrome). VERIFIED: harness 88/88, tsc 59 pre-existing ZERO new and none in any file touched, build OK 28.6s, admin path executed in Chrome eight times, probe removed and confirmed inert - LOCAL ONLY
- C1 THE BOARD ROW LEARNS WHAT IT CONTAINS [approvals-collections] (owner order 2026-08-05, plan e923552): the board card could never say what was inside it, because list_sets_by_user omits `snapshot` on purpose - longtext that can carry embedded base64 images. It now also returns `itemCount` and an `items[] {id,type,title}` SUMMARY computed in SQL, so a card can show its contents and expand with NO per-card request. MEASURED live before writing any PHP: summary 86 bytes against a 261-byte snapshot, and the gap widens sharply once a card holds an image. JSON_TABLE is used deliberately instead of the naive '$.custom[*].title' path form, because that form SKIPS elements missing the key and silently drifts ids out of alignment with titles; JSON_TABLE walks the array and yields NULL instead (MySQL 8.0.35 verified). ONE definition drives it: a private ITEM_BUCKETS map owns bucket->title-key, and both the SQL builder and the row formatter read it, so a fifth bucket is one line rather than two edits that can disagree. ALSO FIXED, A LIVE DEAD-CODE BUG THIS EXPOSED: format_set_row json_decodes snapshot, so with the column unselected every board row was shipping `"snapshot": []` - an empty ARRAY where the type declares an object - and SetCard.tsx:114 read `set.snapshot.brandName` off it, getting undefined on every card forever, carried only by its ?? fallback. List rows now carry no snapshot key at all, which is the truth, and the three places that read it (SetCard, PreviewDialog, FeedbackDialog x2) are corrected - without that they would have thrown once the key vanished. `ApprovalSet.snapshot` is now optional in TS, which is what it always was in fact. VERIFIED BY EXECUTION: GET /approvals/sets returns 200 with itemCount 1, items[{id,type,title}], HAS_snapshot false, 685 bytes for two rows; the admin board then rendered both cards with zero console errors. harness 88/88, tsc 59 pre-existing ZERO new none in Approvals, build OK 27.6s. Login probe created under the standing mu-plugin protocol, then deleted and confirmed inert - LOCAL ONLY
- C2 THE CARD EXPANDS, AND IT COSTS NOTHING [approvals-collections] (owner order 2026-08-05, plan e923552): a board card now states how many items it holds and reveals them in place. THE GATE WAS THE POINT AND IT IS MEASURED: clicking the disclosure fired ZERO network requests (counter wrapped around both fetch and XMLHttpRequest from the first document, delta = 0, requestsMade = []). It renders entirely from the items[] summary C1 put on the row, which is why it opens instantly on a host where the sets list itself took 43s in an earlier run. STRUCTURE: the card became a column - a clickable `.row` plus the revealed list - and the absolutely-positioned checkbox and action overlay were re-anchored to the ROW, because anchoring them to the whole card would have centred them against the expanded list. The selected-state tint moved to the row for the same reason: an expanded card would otherwise look as though every sub-item were selected. The disclosure sits at the end of the title line as information rather than an action (the hover chips remain the actions), stays quiet until hover, and rotates one chevron instead of swapping glyphs so the control never changes shape. NEW: modules/Approvals/assetTypes.ts - ONE registry owning id/label/icon/approvalKey per type, and it accepts BOTH vocabularies (the board summary sends the bucket `articles`, the client view's merged list sends the singular `article`), so no caller has to know which it is holding; unknown types fall back to a generic label rather than vanishing, because a card that silently drops an item is worse than one labelled generically. VERIFIED BY EXECUTION: clicked "Show the 1 item in ok" - card expanded, sub-asset rendered as "Document ok", chevron computed transform matrix(0,1,-1,0,0,0) = 90 degrees, zero requests. harness 88/88, tsc 59 pre-existing ZERO new none in Approvals, build OK 28.2s. Probe deleted, mu-plugins empty, key inert, wp-admin 302s to wp-login - LOCAL ONLY
