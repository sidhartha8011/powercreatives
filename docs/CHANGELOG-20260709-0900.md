# CHANGELOG — 2026-07-09 09:00 — [section-ux-redo-and-serving-parity]

Owner corrections after the first live test, plus the ROOT-CAUSE fix for
"saved but the page never changes" — fact-traced end-to-end on the owner's
own failing edit (powerleads.local, post 1, rule id 3). **Draft/Publish is
PAUSED by owner decision** (was half-built this session; reverted cleanly —
zero dormant code; the spec stays on file for a later pair).

## ROOT CAUSE (proven, then fixed, then proven fixed)
- The owner's section save reached the hub AND the connector, but serving
  refused it (connector counters: applied 0, missed 2). Live diff showed the
  saved fingerprint had 4 paragraphs, the real section 3 — the outline's
  DISPLAY interleave had folded a comments-area paragraph into the section.
- FIX (contracts v2 amended): section membership now comes from the connector
  scan's own anchors (the serving engine's definition), never from display
  layout. Duplicate anchors = k-th contiguous run → k-th matching heading.
- ALSO fixed (the related class, found by review): serving now compares
  against CHROME-EXCLUDED blocks (pre-<body> + header/nav/footer/aside),
  matching what the scan fingerprinted — connector **2.8.1** + reference
  matcher `chrome_spans`/`content_blocks` + regression test (cookie-banner
  <p> can no longer break a match).
- **LIVE PROOF:** replicated the fixed pipeline on the real page + the real
  stored replacement → membership = 3 ¶ → serve APPLIED (new heading + new
  body serve, chrome byte-identical); the old broken fingerprint stays
  refused (guard intact).

## The window (SectionModal v2 — owner sketch, nothing else)
- Opens BELOW the clicked row (draggable from the header; buttons never drag).
- ONE constant shape: white, compact (440px, fixed-height text area,
  12px scale) — the shape never changes because there IS no mode: the text
  is DIRECTLY editable on first click (Edit button removed).
- Header: `¶ <heading>` + **Ask AI** (free instruction → Enter runs it) +
  **Re-write** (AI rewrite straight into the editor) + X.
- Persistent toolbar: B · I · U · Link · H1 · H2 · bullet list.
- Footer: **✓ Acceptera** (saves) · **↶ Ångra** (restores last saved state).
- **Clicking OUTSIDE saves too** (when changed) and closes; Esc closes
  without saving. Baseline is the editor's normalized HTML so an untouched
  window never phantom-saves.
- Ask-AI instruction rides the `{{topic}}` placeholder; templates that
  predate it (already-seeded/user-edited) get the instruction appended at
  run time — never silently dropped, templates never rewritten.

## The table (HeadingsPanel)
- **P rows removed** (owner: unnecessary). ONE `¶ N` row per section under
  its heading — shows the SERVED text (section rule > paragraph-rule folds >
  original), primary dot when optimized, click opens the editor below the
  click. Heading rows unchanged (H-switch, one-click optimize, hover expand).
- Sections whose anchor heading isn't in the heading scan (e.g. the theme's
  comments title) render as muted standalone ¶ rows — addressable, never
  silently mis-grouped again. Orphan text before the first heading = one
  read-only ¶ row.
- Added sections stay as NEW rows at their served spot; “+ Add section” stays.
- Dead code deleted with the P rows: per-paragraph inline editor, paragraph
  optimize path, HTML popup, display-interleave merge.

## Verified
php -l x5 OK · generated connector source extracted + linted · matcher smoke
22/22 · connector parity 17/17 (chrome fns included) · **live proof PASSED**
(above) · tsc 59 = baseline (zero new) · build OK.

## Owner verification (powerleads or powerstock, connector → 2.8.1, Ctrl+F5)
1. Expand a page → ¶ rows instead of P rows; click one → window opens BELOW
   the click, white, compact, directly editable; drag it; table stays live.
2. Edit → click outside → toast says saved → live page serves it (Ctrl+F5).
3. Re-open → Ångra/Acceptera behave; edit back to original → "Reverted".
4. Re-save the broken Hello! section once — it starts serving (the stored
   rule from the broken flow stays honestly refused until re-saved).
5. Ask AI with an instruction; Re-write; + Add section → FAQ.
