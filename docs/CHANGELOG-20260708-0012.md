# Changelog — 2026-07-08 00:12 — Deliveries batch: dynamic lanes, universal search, multi-select, compact Kanban, name-cell actions

Five pairs, all approved by PO 2026-07-07 (confirmed bullet list in chat).

## [kanban-compact-cards] (e78bee5 → AFTER)
- Shared Kanban density TOKENS shrunk (tokens.css only): card min-h 32→28,
  pad 6/10→4/8, gap-column 6→4, lane-padding 8→6, radius-card 10→8.
- Applies to EVERY board on the primitive (Approvals + Deliveries) — no
  consumer card code touched.

## [deliveries-name-cell-actions] (e58bfe9 → AFTER)
- DataTable rows carry `group`; the open-card icon is smaller (h-5/h-3 icon)
  and hover-only (CSS hover, accepted trade-off, focus-visible fallback).
- Delete moved OUT of the name cell into a new far-right `actions` column
  (44px, hover-revealed trash, admin-only). Project sub-row ✕ matches
  (hover-revealed). Sub-rows/colSpans realigned (+1 column).

## [deliveries-universal-search] (daf788a → AFTER)
- The bar search is UNIVERSAL + DYNAMIC: `buildDeliveryFilters` walks every
  value on the row (strings/numbers/arrays/nested — assignee names included),
  so future backend fields are searchable with zero wiring; display-only
  labels (brand NAME, type LABEL) are appended via live lookups the board
  supplies. Case-insensitive contains (engine's text filter).

## [deliveries-multiselect-bulk] (a7d27b2 → AFTER)
- DataTable gains optional CONTROLLED `selection` (leading checkbox column,
  header = select-all-visible, row tint when selected). Sub-row renderers
  account for the extra cell.
- DeliveriesTable (admin-only): delivery selection + PROJECT sub-row
  selection; one shared BulkActionBar (portal, reused from Copy/Image):
  deliveries → "Set status…" select + Delete N (confirm dialog, sequential
  deletes — optimistic snapshots race in parallel); projects → "Remove N from
  delivery" (assets.setProjectDelivery null + cache invalidate).

## [kanban-dynamic-lanes] (a204baf → f0e964c)
- "Lanes: Status/Type/Brand/Lead/Client" dropdown in the bar (Kanban view,
  persisted `pcm:deliveries:lanes`). Lanes derive live: Status = fixed lane
  declaration; Type = ALL presets (drag into an empty type) with TYPE_PALETTE
  accents; Brand/Lead/Client = values present in the data (unfiltered, so
  lanes don't flicker while searching) + a "No X" lane that CLEARS the value
  on drop.
- Drops WRITE the field. New optimistic helpers in useDeliveries —
  `updateFieldsOptimistic` (type/brand/client) and `setLeadOptimistic`
  (assignees patch + PATCH /lead) — both follow updateStatus's flushSync
  contract (required or dnd snaps the card back; see CHANGELOG-20260527-0425).
- Modules (multi-value) and Updated (derived) deliberately NOT laneable.

## Verification
- tsc 56 = baseline after every pair; vite build OK.
- Pending live (user): lane switch + drag-writes per field (incl. drop on
  "No X" and a lead lane), search for a brand name/module/assignee, select-all
  + bulk status + bulk delete + bulk remove projects, compact cards on BOTH
  boards, hover-only open icon + actions column.

## Rollback
Per pair: e78bee5 / e58bfe9 / daf788a / a7d27b2 / a204baf.
