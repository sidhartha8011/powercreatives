# Changelog — 2026-08-06 11:25

- Corrected the Approvals document typography cascade so the Notion-scale title, metadata, controls, body text, and checklist styling render from one shared token contract in both admin and portal contexts.
- Isolated Approvals document controls from the app-wide forced Inter rule so the title, content, checkboxes, actions, and selection popover use one font family.
- Placed the document control-font override in the app's base cascade layer, where it correctly outranks the layer's global forced Inter rule without affecting controls outside the Approvals sheet.
