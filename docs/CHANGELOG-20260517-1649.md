# CHANGELOG 2026-05-17

## Ads Module — New Module (Ad Composer)
- Created `modules/Ads/types.ts` — AdCreative, MediaSlot, TextSlot, AdsPhase types
- Created `modules/Ads/adsConfig.ts` — production defaults, localStorage keys
- Created `modules/Ads/hooks/useAdsOrchestration.ts` — state machine orchestrating Copy SSE + Image parallel generation
- Created `modules/Ads/components/AdCard.tsx` — composed ad card with image preview + inline-editable text
- Created `modules/Ads/components/AdsResultsGrid.tsx` — responsive grid with empty/loading/progress/populated states
- Created `modules/Ads/components/AdsSidebar.tsx` — sidebar reusing ContextPanel, EnhancedBrandSection, ThemeSelector
- Created `modules/Ads/index.tsx` — module entry point (split-panel layout)
- Added `'ads'` to ModuleId union in `types/index.ts`
- Added AdsModule to Shell registry + fullBleedModules in `Shell.tsx`
- Added "Ads" nav item with Megaphone icon to `Sidebar.tsx`

### Code Review Fixes
- **P1** Extracted SSE parser to `utils/sseTextParser.ts` — hook now 380 lines (was 561)
- **P1** Fixed setState-during-render: auto-select moved to `useEffect`
- **P1** Removed dead code `modelInfo` variable (never used)
- **P2** Wrapped `imageModelsByTier` in `useMemo` to prevent unnecessary re-renders
- **P2** Fixed cross-origin image download: uses `fetch→blob→createObjectURL` with fallback
- **P3** Removed unused `Sparkles` import from AdsResultsGrid
- **P3** Replaced mutable shared array with functional `setMediaSlots(prev => ...)` updaters
