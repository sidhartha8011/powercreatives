# CHANGELOG — 2026-05-21

## 2026-05-21 17:37 — Premium Audience Tabs & Compact Copy Cards

### Inline Editable Card Components
- `app/src/modules/Copy/components/InlineEditableCard.tsx` — Reduced card wrapper padding from `2.25rem 2.5rem` to a highly compact `'1.25rem 1.5rem'` to create a light, scan-friendly grid. Replaced generic copy tags with a premium descriptive header format: `Copy ${index + 1}: ${variation.headline}`. Conditionally hid the redundant Audience and Angle tags in the footer when rendering in `readOnly` mode.

### Projects Module Detail View
- `app/src/modules/Projects/index.tsx` — Grouped saved copy variations dynamically by `audienceName`. Rendered a beautiful, custom-built sub-tab navigation bar styled to match the Copy module. Wrapped the card elements inside a robust, bordered rounded grid matching the Copy module grid style, preventing excessive column stretching on wider viewports.
