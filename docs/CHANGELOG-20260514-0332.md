# Changelog 2026-05-14

## Per-Tab Checkbox in Image Results

| Time | Change |
|------|--------|
| 03:32 | `useImageGeneration.ts` — `selectAllForTab(tabId: string)` now accepts an explicit tab id so any tab (not only the active one) can be toggled. Removed `activeTab` from the callback's deps. |
| 03:32 | `ImageResultsGrid.tsx` — Replaced the single shared "select all in active tab" button with a per-tab checkbox rendered inside each tab. Each tab now wraps its own checkbox + label in a flex container that owns the active-state border. Checkbox is hidden when a tab has zero completed assets. |

## Brand Fetch JSON Markdown Fallback

| Time | Change |
|------|--------|
| 05:52 | `copy/service.php` — `extract_business_info()` now routes the LLM response through `PCM_LLM::extract_json()` before `json_decode()`. Defensive fallback for Claude/Gemini sometimes wrapping the JSON in ```` ```json ... ``` ```` despite the "no markdown" system instruction. No-op for already-clean JSON (OpenAI). |
