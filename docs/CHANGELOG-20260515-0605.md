# Changelog - 2026-05-15 06:05

## Added
- Added `EnhancedBrandSection` shared component matching minimalist "shadcn-style" aesthetics, combining business info fields and visual brand assets (Colors, Logo, Reference Subjects).
- Added `sessionReferenceImages` state tracking to Copy module.
- Added `formValues` state tracking to Image module.
- Added payload extraction for `visualAssets` inside `useCopyGeneration`.

## Changed
- Replaced redundant `DynamicSection` for business info in Copy module (`Copy/index.tsx`) with the new global `EnhancedBrandSection`.
- Integrated `EnhancedBrandSection` into Image module sidebar (`ImageSidebar.tsx`) to serve as the unified brand control center.
- Updated Image module's `useImageGeneration` hook to strictly respect the `contextData.brandToggles` for colors and summary inclusion.
- Centralised Image generation "Reference Subjects" into the brand orchestration layer by removing the duplicate UI in "Generation Assets".

## Verified
- "Worst-case" empty scenarios handle missing brand context gracefully via explicit toggle logic.
- UI consistency is guaranteed across modules due to standard component reuse.
- All implementation goals outlined in the Phase 1-3 roadmap have been met.
- 2026-05-15 07:13: Implemented interactive brand assets (logo upload/remove, color picker/remove) inside EnhancedBrandSection using useBrandAssets custom hook.
- 2026-05-15 07:34: Fixed UX issues in EnhancedBrandSection: added Add button for colors to prevent onChange spam, made Logo removal button permanently visible, and added a Replace Logo upload button.
- 2026-05-15 07:46: Refactored EnhancedBrandSection to use shared BrandColorSection and BrandLogoSection components from the Brands module. Added global delete functionality to BrandLogoSection.
- 2026-05-15 17:15: Completed Certifications Refactoring: Added role to BrandAsset in PHP backend and TypeScript types. Replaced legacy ImageSidebar Trust Badges and Allow Text with a permanent Certifications block inside EnhancedBrandSection. Updated useBrandAssets hook to support role-based multipart file uploads. Default generation toggles set to only active useLogo.
