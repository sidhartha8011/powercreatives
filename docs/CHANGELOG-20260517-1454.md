# Changelog 2026-05-17 14:54

- Removed `mShots` dependency to eliminate asynchronous timeouts and unreliable placeholder screenshots.
- Implemented **Foolproof Brand Extraction Strategy**:
  - Re-prioritized Google Favicon API as the primary Source of Truth for core brand colors.
  - Implemented heuristic CSS Whitelisting to exclusively extract semantic CSS variables (`--primary`, `--brand`, `--accent`, `--main`, `--theme`).
  - Removed generic `<style>` block color parsing to permanently eradicate CSS framework pollution (Bootstrap/Tailwind default variables).
