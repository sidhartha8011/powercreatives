# CHANGELOG 2026-07-07 02:27 — Sites: Removed shadows from filter bar and DataTable

Removed the default shadows from both the Sites filter bar and the DataTable container to match design requirements and maintain a flat, clean aesthetic.

## UI Changes / UX Refinements

- **Removed Shadows:**
  - Removed `shadow-sm` from the Sites filter bar wrapper container.
  - Added `wrapperClassName="shadow-none"` to the `DataTable` call on the Sites page to disable the table's container shadow.

## Files changed
1. `app/src/modules/Sites/index.tsx` — Removed filter wrapper shadow and disabled DataTable wrapper shadow.

## Verification
- `npm run build`: Successfully built client environment in 28.78s.
