# CHANGELOG — 2026-05-20

## 2026-05-20 06:25 — Shared Copy Generation Settings for Ads Module

### New Shared Components
- `shared/copySettingsConfig.ts` — Reusable option sets (TONE, EMOJI, CTA, LANGUAGE, SEASON, POST_FORMAT), generic types (FieldConfig, SectionConfig), shared section definitions (ADVANCED_COPY_OPTIONS), and helper functions (isSectionVisible, isFieldVisible, buildDefaultFormValues)
- `shared/DynamicSection.tsx` — Config-driven collapsible section with dynamic field rendering (text, textarea, select, slider, number). Extracted from Copy module
- `shared/ReferenceAdsSection.tsx` — Dynamic list of reference ad textareas with add/remove. Extracted from Copy module
- `shared/CopyTypeSelector.tsx` — Inline checkbox selector for copy types. Extracted from Copy module

### Updated Copy Module
- `Copy/copyConfig.ts` — Options arrays and helpers now re-exported from shared/copySettingsConfig instead of duplicated
- `Copy/components/DynamicSection.tsx` — Re-exports shared version for backward compatibility
- `Copy/components/ReferenceAdsSection.tsx` — Re-exports shared version for backward compatibility
- `Copy/components/CopyTypeSelector.tsx` — Re-exports shared version for backward compatibility

### Updated Ads Module
- `Ads/index.tsx` — Added state for selectedTypes, audiences, angles, genSettings. Passes dynamic values to orchestration
- `Ads/components/AdsSidebar.tsx` — Added CopyTypeSelector, ModeListBox (audiences), GroupedAnglesList (angles), DynamicSection (tone/emoji/CTA), ReferenceAdsSection. Removed old angles slider from production parameters
- `Ads/hooks/useAdsOrchestration.ts` — AdsGenerateParams now accepts dynamic copyTypes, audiences config, and angles config. runTextPhase passes these to backend instead of hardcoded values
