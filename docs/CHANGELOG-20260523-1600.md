# CHANGELOG — 2026-05-23 16:00

## Fixes

- **FIX: EnhancedBrandSection rogue logo resolver** — replaced `|| !a.role` fallback with canonical `getBrandLogo()` to prevent wrong logo displaying when switching modules (2026-05-23)

## Features

- **Tabbed layout for Ads visuals** — images now grouped by creative concept with pill tabs, matching Image module's AdVersion pattern (2026-05-23)
- **"All" tab** — added to both visuals and copy sections in Ads module for unfiltered view (2026-05-23)
- **`conceptName` on MediaSlot** — new field linking generated images to their creative concept for tab grouping (2026-05-23)

## Refactoring

- **PillTabBar shared component** — extracted ~143 lines of duplicated tab JSX into reusable `PillTabBar` component in `components/shared/` (2026-05-23)

## UX Improvements

- **Ads sidebar segmented control** — replaced flat section headers with `[Image | Copy | Video]` tab switcher, showing one output panel at a time (2026-05-23)
- **Unified accordion sections** — Brand and Creative Brief now use consistent collapsible AccordionSection wrappers matching the rest of the sidebar (2026-05-23)
- **Generate button moved to header** — now in the module header bar with progress indicator, matching Image module pattern (2026-05-23)
