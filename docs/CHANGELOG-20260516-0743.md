# Changelog 2026-05-16 07:43

- FIXED: Replaced short brief with the fully resolved backend prompt in the frontend Asset state, so the full prompt is transparently displayed in the UI.
- ENHANCED: Image models lacking image input capability are now greyed out (disabled) instead of completely hidden when "Use Logo" or "Reference Subjects" are enabled.
- ADDED: Tooltips explaining why models are disabled, guiding the user to turn off image inputs to re-enable them.
