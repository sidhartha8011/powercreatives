# CHANGELOG 2026-05-26 10:10 — Client Review Board: Comment Typography & Hierarchy Refinements

Refined and polished the comments inspector drawer typography scale, visual weights, spacing, colors, and controls to pixel-perfect premium standards.

Plugin: v1.4.7 → v1.4.8

## UX Refinement & Enhancements

- **Typographic Scale & Spacing:**
  - Modified `.pcm-comment` to render as a white card (`background: #ffffff`) with an extremely soft border (`border: 1px solid rgba(0, 0, 0, 0.04)`) and micro-shadow (`box-shadow: 0 1px 2px rgba(0,0,0,0.02)`) for high-end SaaS presentation.
  - Aligned replies (`is-reply`) with a clean `28px` margin-left and soft light-tint background.
  - Set `.pcm-comment-author` to semibold `12.5px` and body text (`.pcm-comment-text`) to clean `13px` regular with `1.5` line-height and `-0.005em` letter-spacing.
  - Refined timestamps (`.pcm-comment-time`) to `10.5px` muted warm gray.

- **Status Badges & Dropdowns Overhaul:**
  - Overhauled `.pcm-comment-status` to render with modern transparent RGBA backgrounds and thin low-opacity borders matching their text colors (`is-new` blue, `is-team-reply` amber, `is-done` green), ensuring soft, harmonized visual badges.

- **Actions & Controls Polishing:**
  - Redesigned `.pcm-comment-reply-btn` to be a clean, minimalist inline action link without the bulky grey pill background.
  - Aligned the delete cross button (`.pcm-comment-delete-btn`) to match the minimalist layout style.

## Files changed
1. `app/src/modules/Approvals/client-review.css` — Typographical scale, colors, spaces, and badge dropdown styles overhauled.
2. `power-creatives.php` — Version bumped from 1.4.7 to 1.4.8.

## Verification
- `npm run build`: Success in 26.28s.
