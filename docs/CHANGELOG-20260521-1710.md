# CHANGELOG — 2026-05-21

## 2026-05-21 17:10 — Save Copy to Projects Integration

### Database & Schema Updates
- `power-creatives.php` — Bumped `PCM_DB_VERSION` to `'1.5.0'` to trigger delta db migration.
- `includes/core/db/class-pcm-schema.php` — Added `projectId` column and index key `idx_projectId` to `wp_pcm_copy_results` database table definition.

### Backend Copy Service & REST API
- `includes/modules/copy/service.php` — Implemented `save_to_project` for bulk assignment and `get_results_for_project` for queries. Updated `get_results_for_job` to map database `projectId` values.
- `includes/modules/copy/controller.php` — Exposed REST routes `POST /copy/save-to-project` and `GET /copy/project/<projectId>` with robust input validation and permissions checking.

### Frontend tRPC Bindings
- `app/src/lib/trpc.ts` — Added `copy.saveToProject` and `copy.getProjectResults` to mapped route definitions in client proxy router.

### Frontend Module Enhancements
- `app/src/modules/Copy/components/ResultsPanel.tsx` — Added "Save to Project..." option to multi-select actions dropdown menu. Implemented modern, high-fidelity `SaveToProjectDialog` popup supporting lazy project list queries and new project creations.
- `app/src/modules/Projects/index.tsx` — Redesigned Project detail view with high-usability tab switcher ("Media Assets" vs "Campaign Copy"). Rendered campaign copies in premium Facebook-ad style visual cards matching the Copy module theme, complete with clipboard copying functionality.
