# CHANGELOG — 2026-05-14 07:52

## Architecture Alignment Fixes

### NECESSARY
- **Shared selection hook**: Replaced custom `Set<string>` selection logic in `useImageGeneration.ts` (~110 lines) with shared `useRowSelection<string>()`. Same hook now powers all bulk selection across Brands, Templates, Keywords, and Image modules.
- **Generified `useRowSelection`**: Hook now accepts `<T extends string | number = number>`. Added `addAll()` and `removeAll()` for partial selection merging. Default type is `number` — zero breaking changes for existing consumers.
- **Extracted `BulkSaveToProject`**: Moved 158-line component from `index.tsx` (was 471 lines) into `components/BulkSaveToProject.tsx`. `index.tsx` is now ~308 lines, well under the 400-line rule.

### IMPORTANT
- **Memoized tab filter**: Replaced inline `allAssets.filter()` inside render loop with pre-computed `completedByTab` Map via `useMemo`. Eliminates O(tabs * assets) per render.
- **Video JSON fix**: Applied `PCM_LLM::extract_json()` before `json_decode()` at 2 locations in `video/controller.php` (lines 260, 509). Prevents concept generation and multi-angle composition failures with Claude/Gemini models.

### NICE
- **Explicit parseInt filter**: Replaced `parseInt(a.id)` truthy check with `Number.isFinite(Number(a.id))` in `BulkSaveToProject` for clarity.

### Files Changed
- `app/src/hooks/useRowSelection.ts` — Generified to `<T>`
- `app/src/modules/Image/hooks/useImageGeneration.ts` — Selection refactored to shared hook
- `app/src/modules/Image/index.tsx` — BulkSaveToProject extracted
- `app/src/modules/Image/components/BulkSaveToProject.tsx` — NEW
- `app/src/modules/Image/components/ImageResultsGrid.tsx` — Memoized tab filter
- `includes/modules/video/controller.php` — JSON markdown fallback (2 locations)
