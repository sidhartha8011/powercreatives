# Changelog — 2026-07-07 05:40 — Entity card v3 (Notion-reference look)

## Summary
Card geometry and table styling matched to the Notion/Slite reference: tall fixed-height
card with a narrow centered column, huge title with an "Edited Xh ago" meta-line, and a
shared feather-light table recipe (grey header, hairlines, compact rows) for every table
inside a card.

## Shared primitive (components/shared/EntityCard/)
- `EntityCard`: fixed `h-[88vh]`, ~880px, rounded-xl, one scroll context, centered
  `max-w-[640px]` content column with generous padding.
- `EntityCardTitle`: 32px bold, optional `meta` line above (12px muted).
- `EntityCardSection`: section spacing 32px.
- NEW `tableRecipe.ts`: `CARD_TABLE_WRAPPER/HEAD/ROW/CELL` — `bg-slate-50` header row
  (normal weight), hairline `slate-200` borders including verticals, 36px rows, 13px text.
- NEW `relTime.ts` (moved from DeliveryDialog): "2h ago" helper for meta lines + logs.

## Deliveries
- Card shows "Edited {relTime(updatedAt)}" above the title.
- PropertyTable and the projects table both wear the shared table recipe.

## Verification
- tsc: 56 = exact baseline, 0 in touched files; vite build OK. Live pending hard refresh.

## Rollback
`git reset --hard 3ebee88` (BEFORE IMPLEMENTATION OF [entity-card-v3]).
