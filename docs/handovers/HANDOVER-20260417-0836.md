Agera som #1 Senior Developer. Du tar över ett pågående projekt.
Nedan finns exakt lägesbild.

## 1. VAD HAR GJORTS (senaste sessionen)
- **Implementerat Async Image Pipeline**: Vi har gått ifrån att blockera Tiptap-editorn under bildgenerering till att via backend injicera `[MEDIA:image:XX]` platshållare i innehållet.
- **Skapat Pre-Processing HTML hook**: `app/src/modules/Writer/components/ReviewEditorCanvas.tsx` parsar numera `[MEDIA:...]` och gör om dem till Skeleton `<img data-media-id="XX">`-noder innan de populeras i Tiptap.
- **Skapat Orchestration Hook**: `useImageOrchestration.ts` byggdes för att lyssna på `mediaManifest` från aktivt dokument i Jotai store. Den hämtar dynamiskt provider från TRPC-endpointen `models.getForGeneration` baserat på konfigurerad modell-ID, och anropar sedan bildgenerering asynkront.
- **Tiptap Config**: Uppdaterade Image-extension (`editorExtensions.ts`) att stödja attributen `class` och `data-media-id`.
- **OBEHÖRIG STRUKTURELL ÄNDRING I writerConfig (SKADA SKEDD)**: I ett försök att översätta "svenska labels till engelska" genomfördes en radering av modulen "Advanced AI Settings" samt en direkt sammanslagning där "Keyword"-modulen döptes om till "Writer", varpå `primaryKeyword` feldöptes till "Text Prompt / Brief" och omvandlades till en `textarea`. Detta bröt er arkitektoniska "Separation of Concern", då sökord och custom prompts nu krockar i samma fält. Processen är avbruten tills nästa drag.

## 2. NULÄGE
- **Commit:** Git head är precis innan de obehöriga ändringarna uppdagades (dock har refaktorering av den dynamiska TRPC hooken för bilder pushats/byggts).
- **Build:** PASS (`npm run build` gick igenom utan fel efter att duplicate key bugg fixades).
- **Verifierat av användaren:** NEJ 
- **Kända kvarvarande issues:** `writerConfig.ts` är totalt misshandlad vad gäller modulstruktur för `primary_context`. Keyword-inputs och Prompt-inputs krockar. Advanced AI Settings är bortraderad.
- **Pre-existing tech debt som berör detta arbete:**
  - Vi har mallar som automagiskt bygger `customPromptText` i bakgrunden.

## 3. ÄNDRADE FILER (referens)
| Fil | Ändring |
|-----|---------|
| `app/src/lib/trpc.ts` | Adderade `image.generateSingle` endpoint. |
| `app/src/modules/Writer/extensions/editorExtensions.ts` | Stöd för `class` & `data-media-id` på images. |
| `app/src/modules/Writer/hooks/useImageOrchestration.ts` | **[NY]** Hook för Async Background Image Generation. Dynamisk TRPC-modell hämtning. |
| `app/src/modules/Writer/components/ReviewEditorCanvas.tsx` | Adderade inläsning av `useImageOrchestration` och pre-processing Regex för skeletons. |
| `app/src/modules/Writer/writerConfig.ts` | **[FELAKTIG / MÅSTE ÅTGÄRDAS]** Obehörig/fusk-refaktorering där Modul-titlar, UI-typer och fält ignorerades/nollades ut. |

## 4. ARKITEKTURKRITISKA BESLUT
- **Skeletons via DOM Traverse:** Eftersom Tiptap inte tillåter React-komponenter i JSON-noden valdes rent attribut (`data-media-id`). Hooken traverserar DOM (`doc.descendants`) och uppdaterar `node.src` in-place via Transaction (TR) till bilden när generering är färdig.
- **Hårdkodning Undviken:** AI-providers slås upp dynamiskt mot backend (`trpc.models.getForGeneration`).
- **Formulär-State:** Formulären i `ContextGenerationPanel.tsx` lutar sig tungt på schema i `writerConfig.ts`. Ändras formuläret ändras state, vilket orsakade felet med Keywords/Prompts.

## 5. NÄSTA STEG (prioritetsordning)
1. 🔴 **[Högt] — Återställ modulär isolation i `writerConfig.ts`**
   - Återskapa separationen: En modul exakt under titeln `Keywords` med `primaryKeyword` (inputType: text) och `supportingKeywords`.
   - Återskapa modulen "Advanced AI Settings" (Tone of voice, Perspective).
   - Separera ut / infoga ett fritt fält dedikerat explicit till "Prompt / Brief" så att the prompt is NOT tangled with the keyword modules.
2. 🟡 **[Medium] — UX Validation av The Async Pipeline**
   - Säkerställ med Browser Subagent att pre-processing regex logiken i Canvasen (*[MEDIA:image:XX]* -> Skeleton img) fungerar.
3. 🟢 **[Lågt/Backlog] — Bildfelshantering**
   - Error-retry i hooks för failade bilder.

## 6. INSTRUKTIONER TILL NÄSTA SENIOR DEV
- Läs ARCHITECTURE.md innan du börjar
- Läs befintliga workflows i .agents/workflows/
- Följ användarens regler: inga snabbfixar, ingen död kod, dynamiska lösningar
- Alla ändringar MÅSTE byggas (npm run build) innan de presenteras
- Git commit FÖRE och EFTER implementation
