# Handover 2026-04-17 14:47

## 1. VAD HAR GJORTS (senaste sessionen)

- **Phase 2:** Raderade hårdkodade AI-prompts i `service.php`, implementerade `get_default_prompts()`, `get_system_prompt()` (DB override pattern), och `resolve_prompt_placeholders()` för dynamisk variabel-injektion.
- **Phase 3:** Konverterade `controller.php::generate_article()` från blocking REST (`return $this->success()`) till SSE push via `PCM_SSE::start(600)` + `send_done()` + `send_error()`. Hookade `on_chunk => PCM_SSE::llm_callback()` i `service.php` för live heartbeat-tokens.
- **Phase 4:** Skapade delad `lib/sse.ts` SSE-parser. Ersatte `trpc.writer.generate.useMutation()` i `ReviewEditorCanvas.tsx` med authenticated `fetch()` + `parseSSEStream()`. Fixade stale `generateMutation.isPending` → `isGenerating`. **Build: PASS.**

## 2. NULÄGE
- **Commit:** `7481409f - 2026-04-17 14:47 AFTER IMPLEMENTATION OF Phase 4 Frontend SSE Consumer - UNVERIFIED`
- **Build:** PASS (`✓ built in 19.22s`, Vite exit code 0)
- **Verifierat av användaren:** NEJ
- **Kända kvarvarande issues:** Inga kodfel. Behöver E2E-test mot live WordPress.
- **Pre-existing tech debt:**
  - Copy-modulen (`useCopyGeneration.ts`) har fortfarande en inline SSE-parser (rad 393–448) som kan refaktoreras att använda `lib/sse.ts` i en separat PR.

## 3. ÄNDRADE FILER (referens)
| Fil | Ändring |
|-----|---------|
| `includes/modules/writer/service.php` | Zero Hardcoding: `get_default_prompts()`, `get_system_prompt()`, `resolve_prompt_placeholders()`, SSE `on_chunk` hook |
| `includes/modules/writer/controller.php` | SSE Push: `PCM_SSE::start(600)` + `send_done/send_error` |
| `app/src/lib/sse.ts` | **[NY]** Shared SSE stream parser utility |
| `app/src/modules/Writer/components/ReviewEditorCanvas.tsx` | Ersatte tRPC mutation med `fetch()` + `parseSSEStream()` SSE-konsument |

## 4. ARKITEKTURKRITISKA BESLUT
- **Database Override Pattern (Phase 2):** Valde att kopiera Copy-modulens mönster (`prompt_overrides` tabell) för konsistens. Alternativ (config-filer, env-vars) avvisades pga att admins behöver UI-redigering.
- **SSE via PCM_SSE (Phase 3):** Återanvände befintlig infrastruktur istället för Job Queue eller Chunked Transfer. Alternativ avvisades pga over-engineering.
- **Shared lib/sse.ts (Phase 4):** Extraherade SSE-parser istället för copy-paste (DRY). npm-paketet `@microsoft/fetch-event-source` avvisades — onödig dependency.

## 5. NÄSTA STEG (prioritetsordning)
1. 🔴 [Högt] — E2E-verifiering av hela kedjan: Frontend → SSE → LLM → Editor populeras → Bildpipeline kickar
2. 🟡 [Medium] — Refaktorera Copy-modulens inline SSE-parser till `lib/sse.ts`
3. 🟢 [Lågt/Backlog] — AI Chat Bubble (floating inline editing) — se backlog

## 6. INSTRUKTIONER TILL NÄSTA SENIOR DEV
- Läs ARCHITECTURE.md innan du börjar
- Följ användarens regler: inga snabbfixar, ingen död kod, dynamiska lösningar
- Alla ändringar MÅSTE byggas (`npm run build`) innan de presenteras
- Git commit FÖRE och EFTER implementation
- Testa SSE-flödet med `curl -N -X POST` mot `/wp-json/pcm/v1/articles/generate` för att verifiera raw event stream
