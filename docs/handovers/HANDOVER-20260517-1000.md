# Handover — 2026-05-17 10:00

> **Branch:** `image-features`
> **Commits denna session:**
> ```
> 7943710 AFTER Bulk Cleanup - Remove diagnostic logs + T2I race fix + assets[0] resolver migration + template placeholders - UNVERIFIED
> 2db2a67 Handover: Final Prompt fix VERIFIED + Brand Asset Role Contract UNVERIFIED + backlog
> ```

---

## 1. Klart denna session

### 🟡 UNVERIFIED — Bulk Cleanup (`7943710`)
7 filer, 25 tillagda / 25 borttagna rader. Innehåller:

1. **Diagnostikloggar borttagna (11 st)**
   - 4× `[PCM-SOND]` console.log i `useImageGeneration.ts`
   - 3× `[PCM-SOND]` error_log i `image/controller.php`
   - 4× `[KieAI Provider DEBUG]` error_log i `class-pcm-provider-kieai.php`

2. **T2I Auto-Deselect Race Condition — fixad**
   - `useImageGeneration.ts:196` — `selectedModels` tillagd i deps-arrayen
   - Förhindrar att GPT Image 2 fyrar trots att den borde vara disabled vid logo/ref input

3. **Legacy `assets[0]` → `getBrandLogo()` resolver (3 filer)**
   - `Image/index.tsx:104-106` — auto-populate logo via resolver (med fallback till assets[0] under migration)
   - `Image/index.tsx:195-197` — brandLogoUrl via resolver
   - `Copy/index.tsx:374-376` — visualAssets.logo via resolver
   - `ContextPanel/index.tsx:20` — brandLogo via resolver

4. **Template Placeholder-konsistens (1 fil)**
   - `service.php` — `concept_suggestions_user` + `brief_optimization_user` fick `{{seasonEvent}}` + `{{campaignTheme}}`

### Verifierat
- ✅ Build: `npm run build` — lyckas utan fel
- ✅ `grep PCM-SOND` — 0 resultat i hela kodbasen
- ✅ `grep "KieAI Provider DEBUG"` — 0 resultat
- ✅ `video/controller.php` json_decode — redan fixad med `extract_json()` (ej nödvändigt att göra)

---

## 2. Utredningar gjorda

- **Kie.ai API-nyckel** — bekräftat att nyckel `9cfc4825...` finns aktiv i DB. Inte borttagen. Troligtvis UI-cache-issue.
- **Brand Asset Role Contract** — kodgranskning bekräftar att resolver-pattern är korrekt implementerad. 5/5 legacy call-sites nu migrerade (4 i denna session + 1 redan gjord i `useImageGeneration.ts` förra sessionen).

---

## 3. Nästa session — Prioriterad PO-backloglista

### 🔥 A. Verifiering (måste göras först)
- [ ] **Verifiera Brand Asset Role Contract** (`9f947fd`) — generera bild, kolla att logo resolvas korrekt
- [ ] **Verifiera brandCtx Single Source** (`e29edba`) — bekräfta att brandName, phone, url når prompten
- [ ] **Verifiera Bulk Cleanup** (`7943710`) — bekräfta T2I deselect fungerar, inga diagnostikloggar i console/error_log
- [ ] Flagga UNVERIFIED-commits → VERIFIED

### 🎯 B. Image Module — Funktionalitet & UX (PO-features)
- [ ] **Multi-variation per modell** — säkerställ att `variationsPerModel > 1` producerar N bilder per modell, alla synliga i grid
- [ ] **Multi-angle (numVersions > 1)** — säkerställ att varje version skapar distinkt concept-tab som genererar bilder
- [ ] **Bredare Asset Detail-popup** — öka max-width för bättre bildvisning
- [ ] **Klickbar bildförhandsvisning** — klick på bilden i popup → fullscreen eller 80%-overlay
- [ ] **Redesign Asset Detail-popup** — bättre UX-hierarki, snabbare arbete, lättare att förstå. Behåll all funktionalitet (refine, variations, save-to-project, export, logo overlay, navigate-to-video)
- [ ] **Generation History** — persistent storage + UI för att hämta upp, browse och restore tidigare genereringar med alla varianter och redigeringar
- [ ] **Robust modell-filter (live-uppdaterande)** — när logo/reference image togglas ON → visa bara bild-input-modeller. När togglas OFF → filtret uppdateras automatiskt, nya modeller tillgängliga igen
- [ ] **Per-fält brand info-togglar** — istället för all-or-nothing, en toggle per fält (Phone, URL, Niche, Location, Language, Business Summary, Brand Colors etc.) så användaren kan välja exakt vilka fält att inkludera i prompten

### 🟡 C. Teknisk skuld — Kvarvarande
- [ ] `get_brand_logo_url()` i `base-controller.php:358` — queryar oanvänd `brand_assets`-tabell. Beslut: fixa eller ta bort
- [ ] `brand_assets`-tabellen — beslut: dö eller fyll med data

### 🟡 D. Övrig backlog (från tidigare handovers)
- [ ] OpenAI API-nyckel fallback för concept-gen
- [ ] Thinking Model Support (Gemini 2.5 Pro max_completion_tokens)
- [ ] Writer: Bulk Article Generation + WordPress Publish Pipeline
- [ ] AI URL Scraper Phase 2 (ISO/Trust Badges)
- [ ] Deploy Build Script (dev → live ZIP)
