# Handover — 2026-05-14 06:59

> **Branch:** `image-features`
> **Tidigare HEAD-state:** detached från `a7565a9` (rensat upp i sessionen)
> **Senast committat:** `edf3f40 AFTER IMPLEMENTATION OF Brand Fetch JSON Markdown Fallback - UNVERIFIED`

---

## 1. Sessionsöversikt

Sessionen täckte fyra arbetsspår i ordning:

1. **Diagnos: timeout-bug i Image-genereringen (Angle 1)** — utredning, slutsats: hypotes, ej verifierad
2. **Verifiering av kollegans Bulk Save to Project + Select All for Tab** — implementation klar, committad
3. **Per-Tab Checkbox** — fullt implementerad, byggd, committad UNVERIFIED
4. **Brand Fetch JSON Markdown Fallback** — fullt implementerad, committad UNVERIFIED
5. **Asset Pipeline "From Brand"-picker** — analys + lean SpaceX-plan klar, **väntar på grönt ljus innan kodning**

---

## 2. Git-städning (gjort i denna session)

| Åtgärd | Beskrivning |
|---|---|
| Skapade branch | `image-features` från detached HEAD `a7565a9` — alla nya commits skyddade |
| Committat tidigare WIP | 236 rader bulk-save-to-project som låg uncommitted → `1946c4a` |
| BEFORE/AFTER-markers | Skapade enligt processen för båda implementationerna |

**Commits i sessionen (senast först):**
```
edf3f40 AFTER Brand Fetch JSON Markdown Fallback - UNVERIFIED
e4e7438 BEFORE Brand Fetch JSON Markdown Fallback
325b88e AFTER Per-Tab Checkbox - UNVERIFIED
9b7fb4c BEFORE Per-Tab Checkbox
1946c4a AFTER Save to Project bulk action + Select All for Tab - UNVERIFIED
```

---

## 3. Klart men EJ verifierat (väntar test)

### 3a. Per-Tab Checkbox (commit `325b88e`)
- **Filer:** `useImageGeneration.ts`, `ImageResultsGrid.tsx`
- **Vad:** Per-tab checkbox bredvid varje tab-namn istället för en delad
- **Build:** ✅ OK
- **Testchecklista:** finns i konversationen (~ midnatt/03:32-bekräftelsen)

### 3b. Brand Fetch JSON Markdown Fallback (commit `edf3f40`)
- **Fil:** `includes/modules/copy/service.php` rad ~1180-1185
- **Vad:** `extract_business_info()` använder nu `PCM_LLM::extract_json()` innan `json_decode()`
- **Anledning:** Claude/Gemini wrappar ibland JSON i ```` ```json ... ``` ````-markdown — `json_decode` failade → "Failed to parse business info from URL"
- **Build:** Inte nödvändigt (PHP)
- **Testchecklista:**
  - [ ] Fetch Brand med OpenAI → fungerar (no-op)
  - [ ] Fetch Brand med Claude → fungerar (tidigare trasig)
  - [ ] Fetch Brand med Gemini → fungerar (tidigare trasig)
  - [ ] Fetch Brand utan modell satt → fungerar (LLM-steg skippas)

---

## 4. Liknande bug hittad men EJ åtgärdad

**Sweep visade samma `PCM_LLM::invoke()` + `json_decode()`-pattern utan `extract_json()` på 2 ställen:**

| Fil:rad | Funktion |
|---|---|
| `includes/modules/video/controller.php:260` | Video concepts-generation |
| `includes/modules/video/controller.php:509` | Video multi-angle (count > 1) |

Båda kommer att misslyckas med Claude/Gemini om de wrappar svaret i markdown. **Användaren har valt att skjuta detta tills brand-fetchen är verifierad.**

Övriga `invoke()`-callers returnerar TEXT (inte JSON) och är därför opåverkade:
- `assets/controller.php:592`
- `image/service.php:417` (optimize_brief)
- `video/controller.php:362` (enhance_prompt)

---

## 5. Aktiv diagnos (EJ avslutad)

### Image-genereringen: Angle 1 timeout med GPT Image 2

**Status:** Hypotes formulerad, ej verifierad mot loggar.

**Hypotesen i korthet:**
- Fixen 2026-05-13 12:38 (commit `7db9d72`/efterföljande) gjorde att Angle 1 ("Original") använder råa briefen direkt utan strukturell "Product:"-ramning
- Vid `autoOptimizeBrief = false` blir prompten väldigt kort/vag
- GPT Image 2 via Kie.ai polling-timeout är **120s** i `class-pcm-kie-marketplace.php:147`
- Vid vag prompt → modellen tar längre tid → vi timeout:ar innan klart → "📛 Failed (timeout)"

**Vad jag INTE har bevis för:**
- Att GPT Image 2 faktiskt är långsammare för vaga prompts
- Att det är timeout vs. annat fel som visas som timeout

**Nästa steg om frågan tas upp igen:**
1. Hämta `wp-content/debug.log` och leta `[Kie marketplace]`-rader
2. Verifiera HTTP-status från `wait_for_task` på det misslyckade angle 1
3. Beslut: höj timeout till 300s, eller strukturera prompten

---

## 6. På bordet — väntar på Product Owner

### Asset Pipeline "From Brand"-picker (Image-modulen)

**Användarens behov:** När en brand med assets är vald, ska Logo/Subject/Trust-slotterna i Asset Pipeline kunna fyllas med ett klick från brand-bilderna — utan att tappa manuell upload.

**Lean SpaceX-plan (godkänd att presentera, ej godkänd att bygga ännu):**
- ~30 rader totalt, 0 nya filer
- 1 inline-komponent `<BrandPickerButton>` i `ImageSidebar.tsx`
- Återanvänder: `<Popover>` (redan importerat), `setLogoConfig/setSubjectConfig` setters, `contextData.brand.assets[]`, befintlig thumbnail-styling
- Tillägg per slot: 1 rad — `<BrandPickerButton onPick={setX}/>`

**Inväntar:** Användarens grönt ljus (`bekräfta att vi kör ~30-raders versionen`).

---

## 7. Kända öppna saker i kodbasen (utanför sessionen)

Från backloggen — inte rört i denna session:

- 🔴 **Thinking Model Support (Gemini 2.5 Pro)** — `max_completion_tokens=2048` i `copy/service.php:171` är för lågt för thinking-modeller, ger tomma svar
- 🔴 **Writer: Bulk Article Generation** — nästa stora feature på roadmap
- 🔴 **Writer: WordPress Publish Pipeline** — efter bulk
- 🟡 **Writer: imageStyle ignoreras av Kie.ai** — saknar style-parameter mappning
- 🟡 **Deploy Build Script (dev→live ZIP)**
- 🟡 **Auto-set defaultTextModel vid första integration**

---

## 8. Process-anteckningar

- **`task-process.md` finns inte** i kodbasen trots att processen refererar till den. Hoppades i denna session med användarens implicita godkännande.
- **CHANGELOG-fil:** `docs/CHANGELOG-20260514-0332.md` innehåller Per-Tab Checkbox + Brand Fetch JSON Fallback.
- **TodoWrite** användes aktivt för spårning under implementationerna.

---

## 9. Nästa session — gör detta först

1. **Verifiera Per-Tab Checkbox** i UI → committa som VERIFIED om OK
2. **Verifiera Brand Fetch** med Claude/Gemini → committa som VERIFIED om OK
3. **Fråga om video-controller.php behöver samma fix** (samma pattern, 2 ställen)
4. **Vänta in OK på Asset Pipeline "From Brand"-picker** → implementera
5. (Lågprioriterat) Återbesök Angle 1 timeout-frågan med faktisk logg
