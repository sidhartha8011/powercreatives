# HANDOVER — 2026-07-09 01:15 (short — where we are RIGHT NOW)

**Read with:** `HANDOVER-20260708-0230.md` (env, contract, domain — still valid),
`docs/IMPLEMENTATION-PLAN-20260708.md` (THE LAW for this build),
`docs/DYNAMIC-OPTIMIZATION-ARCHITECTURE.md` (frozen contracts v1),
newest `docs/CHANGELOG-*`, SESSION_LOG bottom entries.

## Build lock (owner, 2026-07-08)

THIS BUILD = **phase 1 only: dynamic paragraph optimization + dynamic
approval.** NOT the AI optimizer, NOT SERP (cut entirely), NOT licensing, NOT
the version-selector UI. **Owner approval required PER PAIR — bug reports and
questions are NOT approval** (hard rule, violated once 2026-07-08, never again).
Old `- UNVERIFIED` labels from 07-07/08 declared non-blocking by owner.

## Shipped this session (each BEFORE→AFTER, pushed)

1. **Pair 1** `[seo-paragraph-rows-readonly]` 4342ef8→27a7d4f — paragraph rows
   in the SEO outline (LOCAL, read-only), node-identity contract v1 frozen,
   `headingIndex` keeps heading endpoints untouched.
2. **Sites Connector column** 929587d→eddb7c0 — per-site installed connector
   version + inline per-site update button (single version reader in
   PCM_Sites_Service; SEO delegates). Owner uses it — works.
3. **Pair 2** `[dynamic-rules-engine-pair2]` 9bac3a8→fba6797 — connector
   **2.7.0**: render-time rule engine (try/catch fail-to-original, per-post
   options storage, kill-switch option, schema-v1 validation, purge) +
   scan-content paragraph inventory; hub: `seo_dynamic_rules` (**DB 1.37.0**),
   PCM_Text_Matcher (pure, fixture-tested, tests/unit/TextMatcherTest.php),
   push + capability check; remote paragraph rows interleaved (display anchors).
   Contracts frozen FIRST (744ff66): normalization v1, rule schema v1 (+
   re-anchor context), serving v1, scan-content v1. VERIFIED: core
   WP_HTML_Tag_Processor CANNOT swap block inner HTML → boundary matching on
   non-nestable tags; core parser reserved for future href targets.
4. **Pair 2.5** `[connector-scan-resilience]` 56e0579→f0604fc — connector
   **2.7.1**: scan cache (10 min, busted on save_post + rules push),
   site-wide single-flight loopback lock (heading scan shares it), fallback
   tiers `the_content`→raw (source-flagged), loopback 20s→8s, panel serializes.
   Root cause was LocalWP loopback/worker starvation (owner-verified symptom).
5. **Pair 3** `[paragraph-edit-optimize]` ecd0c68→2ea70e3 — remote paragraph
   rows editable via dynamic rules: textarea edit + ✦ AI (staged accept/
   reject), UPSERT rule (identity site+post+matchText+occurrence), clean
   revert-on-identical (rule deleted), push-fail ROLLS BACK the DB row,
   capability check before any write; served-text overlay + primary dot; P
   chip opens HTML popup; `paragraph` prompt section seeded as user template;
   run_prompt_section gained multiline mode (single-line sanitize would drop
   sentences). LOCAL paragraphs stay read-only (honest tooltip).
6. `[seo-title-preview-icon]` 859f3f9→cc00409 — Eye (opens existing preview
   modal — authenticated remote fetch SHOWS served rules; has open-in-new-tab)
   + edit pen in the title cell, both row-hover-revealed.

## Hard-won facts (do not relearn)

- **Lint the GENERATED connector source** (extract the nowdoc template from
  seohub/service.php, bake placeholders, php -l). Hub-file lint does NOT cover
  it. Now a mandatory checklist step.
- **powerstock.local's connector self-update is broken** ("Upgrade did not
  complete." = WP upgrader returned false — likely Windows/LocalWP FS write
  test → FS_METHOD 'direct' would fix; owner does one-time manual reinstalls
  instead). Sites Connector column is the update surface.
- **tsc baseline is now 59** (drifted 56→59 via the other machine:
  SaveBrandButton dup, streamdown, Templates unknown[] — verified by
  stash-count on HEAD). Zero new errors remains the rule.
- This box's CLI PHP lacks mbstring — matcher case-fold degrades ASCII-only
  there; SAFE by construction (mismatch = miss = original serves). Documented
  in the class.
- Scan inventory (?pcm_cscan) is EXCLUDED from rule serving on purpose: rules
  match ORIGINAL text; the UI overlays served text from the hub's rule rows.
- Owner style: answer short, PO-language, no UI bloat, FACTS with file:line —
  guessing gets called out hard. Never touch code without explicit go.

## IN DISCUSSION RIGHT NOW (not specced, not approved)

**Paragraph UX redesign — the comprehension problem.** Owner: many small
`<p>` rows are unreadable; a human must be able to READ the content. Direction
being converged on (my last sketch, awaiting his "ja"): click a paragraph row
→ popup shows the paragraph as READABLE FORMATTED text (bold/links rendered,
NOT raw HTML), **many small `<p>`s under one heading presented as ONE coherent
block**, with [✦ Optimera] one-click + [Redigera själv]; table rows possibly
collapsed to one row per section ("¶ stycke, 4 delar"). Engineering note:
coherent multi-`<p>` editing needs a `paragraphGroup` rule target (sequence
swap) — schema is target-agnostic by design, this is an extension. An earlier
"visual editor inside the preview modal" idea was MISUNDERSTOOD scope — owner
means the paragraph popup, not the preview iframe.

## Owner verification pending

Pairs 2/2.5/3 on powerstock: reinstall connector → **2.7.1** in the Connector
column → paragraphs appear (fallback tiers work without loopback) → edit a
paragraph → live page serves it → edit back → rule gone. Ctrl+F5 always.

## Parked (unchanged)

Approval rails/changesets (pair 5 of the plan), switches/indicator (pair 4),
Copy handleBrandSaved 1-liner, Brand & Template workstream, one-click login
route (asked about — does NOT exist; specced verbally, no go).
